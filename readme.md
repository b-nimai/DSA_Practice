# 🧠 DSA Pattern-Based Practice Roadmap (C++)

> **Part of the 6-month switch plan.** Sibling roadmaps:
> - 🧩 **LLD:** [`lld-roadmap.md`](./lld-roadmap.md) — 10 concept sections + 11 problems
> - 🚀 **Project + 📨 Apply:** [`project-roadmap.md`](./project-roadmap.md) — Loom-lite AI in 12 weeks + apply phase
> - 📅 **Daily execution:** [`daily-tracker.md`](./daily-tracker.md) — day-by-day integrated tasks across all tracks
> - Project build details: [`PROJECT_PLAN.md`](./PROJECT_PLAN.md) · LLD curriculum source: [`lld_content_index.md`](./lld_content_index.md)
>
> **DSA goal:** recognize the pattern behind any new problem within 60 seconds, then implement. ~200 problems → ~6 months. Mornings 6:30–8:30.

---

## How to use this roadmap

1. **Pick today's problem** from the next unchecked row in the current pattern.
2. **Read the title only** — try to predict the pattern from the title before reading the problem.
3. **Solve in C++** in `solutions/<pattern>/<slug>.cpp`. Aim for an optimal solution.
4. **Before checking your answer**, write your own one-line **trigger phrase** in the table (e.g., *"sorted array → find pair"*). This is what locks in pattern recognition.
5. **Mark the checkbox** (`☐` → `✅`) and fill the **Date**.
6. **Weekly (Sunday)**: redo 1 random problem from the past week without looking at your old code.
7. **After all 20 patterns**: jump to **Mixed Practice** at the bottom — predict the pattern *before* expanding the answer.

Legend: 🟢 Easy · 🟡 Medium · 🔴 Hard

---

## 🧭 Pattern Recognition Cheat Sheet

| If the problem says / has…                                              | Reach for…                          |
| ----------------------------------------------------------------------- | ----------------------------------- |
| Sorted array, pair/triplet, in-place                                    | **Two Pointers**                    |
| Longest/shortest/min/max **substring or subarray** with a constraint    | **Sliding Window**                  |
| Linked list cycle, middle, kth from end                                 | **Fast & Slow Pointers**            |
| "Subarray sum equals K", range sum, count of subarrays                  | **Prefix Sum + Hashing**            |
| Sorted input, OR "minimum/maximum value such that…"                     | **Binary Search** (incl. on answer) |
| Next greater/smaller, valid parentheses, histogram, span                | **Stack / Monotonic Stack**         |
| Reverse, merge, reorder a linked list                                   | **Linked List Manipulation**        |
| Path/sum/diameter in a tree, recursion returns info up                  | **Tree DFS**                        |
| Level-by-level, shortest path in tree/unweighted graph                  | **Tree/Graph BFS**                  |
| Connected components, islands, course schedule, dependencies            | **Graph BFS/DFS / Topo Sort**       |
| "Top K", "K closest", "K largest", median of stream                     | **Heap / Two Heaps**                |
| All permutations / combinations / subsets / paths                       | **Backtracking**                    |
| Intervals (merge/insert), "minimum number of …" with sorting            | **Greedy / Intervals**              |
| "Number of ways", "max profit", choose/skip — 1 dimension               | **DP — 1D**                         |
| Edit distance, LCS, grid paths, palindromes, two strings                | **DP — 2D**                         |
| Prefix lookup / autocomplete / dynamic connectivity                     | **Trie / Union-Find**               |
| Weighted shortest path, "cheapest", connect-all-cost                    | **Weighted Graphs (Dijkstra/MST)**  |
| "Appears once", count bits, add without `+`, power-of-two               | **Bit Manipulation**                |
| Rotate / spiral / set-zeroes a 2-D grid in place                        | **Matrix Simulation**               |
| Implement a class: LRU/LFU cache, O(1) insert/delete/random             | **Design**                          |
| Pow / sqrt / atoi / Roman, digit-cycle, overflow-careful parsing        | **Math / String**                   |

---

# 1. Two Pointers

**When to use it (recognition cues):**
- Input is **sorted** (or can be sorted) and you need a pair/triplet.
- Working from both ends of an array/string toward the middle.
- In-place removal/modification of an array.

**Core idea:** Two indices move toward each other (or in same direction) based on a comparison, reducing O(n²) brute force to O(n).

**Template (C++):**
```cpp
int l = 0, r = nums.size() - 1;
while (l < r) {
    int sum = nums[l] + nums[r];
    if (sum == target) return {l, r};
    else if (sum < target) ++l;
    else --r;
}
```

**Problems:**

| ✓ | Date | # | Problem | Diff | Trigger phrase |
|---|------|---|---------|------|----------------|
| ✅ | 07/05/26 | 167 | [Two Sum II – Input Array Is Sorted](https://leetcode.com/problems/two-sum-ii-input-array-is-sorted/) | 🟡 | Sorted array, need to find pair |
| ✅ | 07/05/26 | 125 | [Valid Palindrome](https://leetcode.com/problems/valid-palindrome/) | 🟢 | Needs to compare both end of the String |
| ✅ | 07/05/26 | 26  | [Remove Duplicates from Sorted Array](https://leetcode.com/problems/remove-duplicates-from-sorted-array/) | 🟢 | In-place dedup on sorted; write lags read |
| ✅ | 07/05/26 | 283 | [Move Zeroes](https://leetcode.com/problems/move-zeroes/) | 🟢 | Move target value to end; slow/fast partition |
| ✅ | 07/05/26 | 11  | [Container With Most Water](https://leetcode.com/problems/container-with-most-water/) | 🟡 | Max area between two lines; move shorter wall |
| ✅ | 09/05/26 | 15  | [3Sum](https://leetcode.com/problems/3sum/) | 🟡 | Sort + fix i + inner 2-ptr; skip dups |
| ✅ | 12/05/26 | 16  | [3Sum Closest](https://leetcode.com/problems/3sum-closest/) | 🟡 | Triplet sum closest to target; sort + 2-ptr, track best, move by sum vs target |
| ✅ | 12/05/26 | 18  | [4Sum](https://leetcode.com/problems/4sum/) | 🟡 | Unique quadruplets = target; fix i,j + inner 2-ptr; skip dups ×4; long long sum |
| ✅ | 09/05/26 | 75  | [Sort Colors](https://leetcode.com/problems/sort-colors/) | 🟡 | Sort 3 distinct values; Dutch flag lo/mid/hi |
| ✅ | 09/05/26 | 633 | [Sum of Square Numbers](https://leetcode.com/problems/sum-of-square-numbers/) | 🟡 | a²+b²=c; range 2-ptr on [0, √c] |
| ✅ | 09/05/26 | 680 | [Valid Palindrome II](https://leetcode.com/problems/valid-palindrome-ii/) | 🟢 | Palindrome with ≤1 deletion; on mismatch try both |
| ✅ | 09/05/26 | 42  | [Trapping Rain Water](https://leetcode.com/problems/trapping-rain-water/) | 🔴 | Water on elevation map; l/r + lMax/rMax |

---

# 2. Sliding Window

**When to use it (recognition cues):**
- Asking for **longest / shortest / max / min** subarray or substring satisfying some condition.
- Contiguous range and the brute force is "try every window".
- "At most K", "exactly K", "no repeating", "frequency".

**Core idea:** Maintain a window `[l, r]`; expand `r`, shrink `l` while invariant breaks.

**Template (C++):**
```cpp
int l = 0, best = 0;
unordered_map<char,int> freq;
for (int r = 0; r < s.size(); ++r) {
    freq[s[r]]++;
    while (/* window invalid */) { freq[s[l]]--; ++l; }
    best = max(best, r - l + 1);
}
```

**Problems:**

| ✓ | Date | # | Problem | Diff | Trigger phrase |
|---|------|---|---------|------|----------------|
| ✅ | 13/05/26 | 643 | [Maximum Average Subarray I](https://leetcode.com/problems/maximum-average-subarray-i/) | 🟢 | "subarray of length exactly k" → fixed window; slide, track max sum, divide once |
| ✅ | 13/05/26 | 209 | [Minimum Size Subarray Sum](https://leetcode.com/problems/minimum-size-subarray-sum/) | 🟡 | shortest subarray with sum ≥ target (positives) → grow right / shrink left while valid |
| ✅ | 13/05/26 | 3   | [Longest Substring Without Repeating Characters](https://leetcode.com/problems/longest-substring-without-repeating-characters/) | 🟡 | longest substring with no duplicates → grow right / shrink left while `cnt[s[r]] > 1` |
| ✅ | 14/05/26 | 424 | [Longest Repeating Character Replacement](https://leetcode.com/problems/longest-repeating-character-replacement/) | 🟡 | longest substring of one letter with ≤ k replacements → window valid iff `len - maxFreq ≤ k`; `if`-shrink (window grows by 1/step), don't recompute `maxFreq` on shrink |
| ✅ | 16/05/26 | 567 | [Permutation in String](https://leetcode.com/problems/permutation-in-string/) | 🟡 | "s2 contains a permutation/anagram of s1" → fixed window width `s1.len`; compare letter freqs, or 1 array + `found` counter (add `>0`, remove `>=0`) for O(1)/step |
| ✅ | 16/05/26 | 438 | [Find All Anagrams in a String](https://leetcode.com/problems/find-all-anagrams-in-a-string/) | 🟡 | SubString with same characters count as `s1` → fixed window | 
| ✅ | 16/05/26 | 1004| [Max Consecutive Ones III](https://leetcode.com/problems/max-consecutive-ones-iii/) | 🟡 | "longest run of 1's, flip ≤ k 0's" → same template as 424: window valid while `#zeros ≤ k`; grow `right`, slide `left` over budget. `while`-shrink+`maxi` or `if`-slide + return `n-left` |
| ✅ | 17/05/26 | 904 | [Fruit Into Baskets](https://leetcode.com/problems/fruit-into-baskets/) | 🟡 | I have to find subarray |
| ✅ | 17/05/26 | 1456| [Maximum Number of Vowels in a Substring of Given Length](https://leetcode.com/problems/maximum-number-of-vowels-in-a-substring-of-given-length/) | 🟡 | fixed size substring |
| ✅ | 18/05/26 | 992 | [Subarrays with K Different Integers](https://leetcode.com/problems/subarrays-with-k-different-integers/) | 🔴 | "count subarrays with exactly K distinct" → no monotone window for "exactly K"; `exactly K = atMost(K) − atMost(K−1)`. `atMostK`: count-window `+= right-left+1`, `k`-as-budget (no erase) |
| ✅ | 18/05/26 | 76  | [Minimum Window Substring](https://leetcode.com/problems/minimum-window-substring/) | 🔴 | "shortest substring of `s` covering all of `t` with duplicates" → variable window, grow `right` / shrink `left` while valid; O(1) validity via `formed == required` counter (bump only when `window[c] == need[c]`), never `map.empty()` |
| ✅ | 19/05/26 | 239 | [Sliding Window Maximum](https://leetcode.com/problems/sliding-window-maximum/) | 🔴 | "max of every fixed-size-`k` window" → max isn't `+in/-out` decomposable → **monotonic deque of indices** (values decreasing front→back); expire front by index `<= i-k`, pop back while `nums[back] <= nums[i]`, emit `nums[front]` once `i >= k-1`. Each index pushed/popped once → O(n) |

---

# 3. Fast & Slow Pointers (Floyd's)

**When to use it (recognition cues):**
- Linked list cycle / cycle entry / length.
- Finding middle, kth-from-end of a linked list in one pass.
- "Happy number" / cycle in a function iteration.

**Core idea:** `slow` moves 1 step, `fast` 2 steps. They meet iff a cycle exists.

**Template (C++):**
```cpp
ListNode *slow = head, *fast = head;
while (fast && fast->next) {
    slow = slow->next;
    fast = fast->next->next;
    if (slow == fast) break;
}
```

**Problems:**

| ✓ | Date | # | Problem | Diff | Trigger phrase |
|---|------|---|---------|------|----------------|
| ✅ | 20/05/26 | 141 | [Linked List Cycle](https://leetcode.com/problems/linked-list-cycle/) | 🟢 | "does linked list contain a cycle?" → Floyd's: `slow+=1`/`fast+=2` from head, meet ⇒ cycle, `fast` hits null ⇒ none. O(1) space vs hash-set's O(n) |
| ✅ | 20/05/26 | 142 | [Linked List Cycle II](https://leetcode.com/problems/linked-list-cycle-ii/) | 🟡 | "return the node where the cycle begins" → Floyd's phase 1 (detect) + phase 2 (reset one pointer to head, walk both at speed 1 — collide at entry). `a ≡ L-b (mod L)` |
| ✅ | 20/05/26 | 876 | [Middle of the Linked List](https://leetcode.com/problems/middle-of-the-linked-list/) | 🟢 | "return the middle node, one pass" → `slow+=1`/`fast+=2` from head; when `fast` falls off, `slow` is middle. Returns 2nd middle on even; for 1st middle start `fast = head->next` |
| ✅ | 22/05/26 | 234 | [Palindrome Linked List](https://leetcode.com/problems/palindrome-linked-list/) | 🟢 | "is singly linked list a palindrome in O(1) space" → can't walk backwards: find midpoint (slow/fast, guard `fast->next && fast->next->next`), reverse second half `slow->next` in place, compare front-to-back, then reverse back to restore. Loop on shorter half (`while right`) |
| ✅ | 22/05/26 | 202 | [Happy Number](https://leetcode.com/problems/happy-number/) | 🟢 | "repeat digit-square-sum until 1 or loops forever" → it's Floyd's on `x → f(x)`, NOT a list: the sequence is eventually periodic so an endless loop is a cycle. `do { slow=f(slow); fast=f(f(fast)); } while(slow != fast); return slow==1`. Met at 1 ⇒ happy, else non-1 cycle. O(1) space vs hash-set's O(n) |
| ✅ | 22/05/26 | 19  | [Remove Nth Node From End of List](https://leetcode.com/problems/remove-nth-node-from-end-of-list/) | 🟡 | "remove the n-th node from the end, one pass" → fixed-gap two pointers: pre-advance `fast` by `n`, walk both till `fast` hits end ⇒ `slow` on predecessor. Use a **dummy → head** with gap `n+1` so head-removal needs no special case; `slow->next = slow->next->next`, return `dummy.next` |
| ✅ | 23/05/26 | 143 | [Reorder List](https://leetcode.com/problems/reorder-list/) | 🟡 | "reorder to `L0→Ln→L1→Ln-1→…` in O(1) space, nodes not values" → 3-step combo: find middle (slow/fast, `fast=head->next` so first half is the longer/equal one), cut, **reverse** second half, then **interleave-merge** the two — save both `next`s before rewiring each pair; loop on shorter list (`while start2`) |
| ✅ | 24/05/26 | 287 | [Find the Duplicate Number](https://leetcode.com/problems/find-the-duplicate-number/) | 🟡 | "`n+1` ints in `[1,n]`, one repeats, find it without modifying array, O(1) space" → read array as function `f(x)=nums[x]` (not a list!); pigeonhole ⇒ cycle, **entrance = duplicate**. Floyd's phase 1 (`do/while`) + phase 2 (reset to `nums[0]`, walk both 1×). O(n)/O(1) |
| ✅ | 25/05/26 | 457 | [Circular Array Loop](https://leetcode.com/problems/circular-array-loop/) | 🟡 | "circular array, each value = signed jump length, find a cycle that's all one direction and length > 1" → Floyd's on `next(i)=((i+nums[i])%n+n)%n`, but add two guards: reject a **sign flip** (`nums[a]*nums[next(a)] ≤ 0` ⇒ crossed forward/backward) and a **self-loop** (`next(i)==i` ⇒ k=1). `fast` hops twice so validate both hops; mark dead paths `0` for O(n)/O(1) |
| ✅ | 24/05/26 | 2095| [Delete the Middle Node of a Linked List](https://leetcode.com/problems/delete-the-middle-node-of-a-linked-list/) | 🟡 | "delete the middle node (`⌊n/2⌋`, 0-indexed), return head" → find-middle slow/fast but stop on the **predecessor** to unlink: carry `prev` behind `slow`, or pre-offset `fast=head->next->next` so `slow` halts one early; `prev->next=slow->next; delete slow`. Guard single-node (`!head->next ⇒ nullptr`) |

---

# 4. Prefix Sum & Hashing

**When to use it (recognition cues):**
- "Subarray sum equals K" / "count of subarrays with property X".
- Range sum / range XOR queries.
- Need O(1) lookup of "have I seen this complement before?".

**Core idea:** `prefix[i]` = aggregate up to `i`. Then `range(l..r) = prefix[r] - prefix[l-1]`. Combine with a hashmap of seen prefix values.

**Template (C++):**
```cpp
unordered_map<int,int> cnt{{0, 1}};
int sum = 0, ans = 0;
for (int x : nums) {
    sum += x;
    ans += cnt[sum - k];
    cnt[sum]++;
}
```

**Problems:**

| ✓ | Date | # | Problem | Diff | Trigger phrase |
|---|------|---|---------|------|----------------|
| ✅ | 26/05/26 | 1   | [Two Sum](https://leetcode.com/problems/two-sum/) | 🟢 | "two indices summing to target, can't reuse element" → one-pass hash map `value→index`; for each `nums[i]` look up complement `target-nums[i]` **before** inserting, so duplicates like `[3,3]` don't self-pair |
| ✅ | 26/05/26 | 303 | [Range Sum Query – Immutable](https://leetcode.com/problems/range-sum-query-immutable/) | 🟢 | "many `sumRange(l, r)` on immutable array" → precompute `ps` of size `n+1` with `ps[0]=0`; query is `ps[r+1] - ps[l]` in O(1). `n+1` shift kills the `l==0` edge case. Prefer `vector` over `unordered_map` for dense integer keys |
| ✅ | 27/05/26 | 304 | [Range Sum Query 2D – Immutable](https://leetcode.com/problems/range-sum-query-2d-immutable/) | 🟡 | "many `sumRegion(r1,c1,r2,c2)` on immutable matrix" → 2D prefix sum sized `(m+1)×(n+1)` with zero border; build and query are the same `+ − − +` inclusion–exclusion over four top-left rectangles. O(m·n) build / O(1) query |
| ✅ | 27/05/26 | 560 | [Subarray Sum Equals K](https://leetcode.com/problems/subarray-sum-equals-k/) | 🟡 | "**count** subarrays with sum = K, negatives allowed" → running prefix sum + frequency map of past prefixes; `count += cnt[sum − k]` then `cnt[sum]++`. Seed `cnt[0]=1` for empty prefix, lookup before insert so an element can't self-pair when `k=0`. Two-pointer only works if `nums[i] > 0` (or `≥ 0` via `atMost(k) − atMost(k−1)`) |
| ✅ | 28/05/26 | 974 | [Subarray Sums Divisible by K](https://leetcode.com/problems/subarray-sums-divisible-by-k/) | 🟡 | "**count** subarrays with sum divisible by K, negatives allowed" → modular twin of 560: bucket prefixes by `rem = ((sum%k)+k)%k`, `count += freq[rem]++`. Seed `freq[0]=1`. Use `vector<int>(k)` over `unordered_map` (remainders dense in `[0,k-1]`); **normalize C++ negative `%`** or equivalent prefixes land in different buckets |
| ✅ | 29/05/26 | 523 | [Continuous Subarray Sum](https://leetcode.com/problems/continuous-subarray-sum/) | 🟡 | "**exists** subarray (len ≥ 2) with sum divisible by k" → equal prefix remainders bracket a sum ÷ k; map `rem → earliest index`, return true when `i − firstIdx ≥ 2`. Store **only the first** index per remainder (don't overwrite — gap must grow), seed `{0, -1}` to drop the `rem==0` special case. Earliest-index = exists/longest; frequency-count = "count" twin (560/974) |
| ✅ | 29/05/26 | 525 | [Contiguous Array](https://leetcode.com/problems/contiguous-array/) | 🟡 | "**longest** subarray with **equal #0s and #1s**" → recode `0 → −1`, `1 → +1` so equal counts = the running **balance repeats** (segment sums to 0); store `balance → earliest index`, seed `{0,-1}`, on a repeat `res = max(res, i − firstIdx)`. **Insert only on first sight** (longest needs earliest index). Longest twin of 523; generalizes to "longest subarray with sum k" via `cnt[sum−k]` |
| ✅ | 31/05/26 | 1248| [Count Number of Nice Subarrays](https://leetcode.com/problems/count-number-of-nice-subarrays/) | 🟡 | "**count** subarrays with **exactly k odd numbers**" → recode `odd→1, even→0` and it's literally 560 on parities: running `odds`, `res += cnt[odds − k]` then `cnt[odds]++`, seed `cnt[0]=1`. Either seed the sentinel **or** special-case `if(odds==k) res++` — exactly one. All-positive recode also enables window `atMost(k) − atMost(k−1)` in O(1) space |
| ✅ | 31/05/26 | 437 | [Path Sum III](https://leetcode.com/problems/path-sum-iii/) | 🟡 | "**count** downward tree paths summing to `target`, start/end anywhere" → 560 lifted onto root→node paths: DFS carrying `sum` + prefix-freq map; `count += freq[sum − target]`, `freq[sum]++`, then **`freq[sum]--` (erase on 0) on backtrack** so sibling subtrees stay isolated. Seed `freq[0]=1`. Two traps: **`long long` sum** (path ≈ 1e12 overflows int → phantom paths) and **frequency map not a set** (count occurrences, else `[0,1,1]` undercounts); use `find` not `[]` |
| ✅ | 01/06/26 | 49  | [Group Anagrams](https://leetcode.com/problems/group-anagrams/) | 🟡 | "**group strings equivalent under rearrangement**" → bucket by a **canonical key** in `unordered_map<sig, group>`; each word's **26-letter count signature** is invariant across anagrams. `mpp[key].push_back(word)`, return the values. Count key is **O(N·K)**, beats the sorted-string key's O(N·K log K); fixed-width raw-byte signature needs no `#` separator |
| ✅ | 01/06/26 | 128 | [Longest Consecutive Sequence](https://leetcode.com/problems/longest-consecutive-sequence/) | 🟡 | "**longest run of consecutive integers**, unsorted, must be **O(n)**" → sorting is the O(n log n) trap; dump all into `unordered_set`, then walk a run **only from its head** (`x-1` absent ⇒ start), counting `x+1, x+2, …`. Head-only guard keeps the nested walk O(n) not O(n²). Pure-membership cousin of Two Sum — "is `x±1` present?" not a complement |
| ☐ |      | 1590| [Make Sum Divisible by P](https://leetcode.com/problems/make-sum-divisible-by-p/) | 🟡 |  |

---

# 5. Binary Search (classic + on answer)

**When to use it (recognition cues):**
- Sorted input → log(n).
- Monotonic predicate: "smallest/largest X such that f(X) is true".
- "Minimize the maximum" / "maximize the minimum".

**Core idea:** Halve the search space using a monotonic check.

**Template (C++):**
```cpp
int l = lo, r = hi;
while (l < r) {
    int m = l + (r - l) / 2;
    if (check(m)) r = m;
    else l = m + 1;
}
return l;
```

**Problems:**

| ✓ | Date | # | Problem | Diff | Trigger phrase |
|---|------|---|---------|------|----------------|
| ✅ | 02/06/26 | 704 | [Binary Search](https://leetcode.com/problems/binary-search/) | 🟢 | "**sorted** array → exact index of `target` in **O(log n)**" → closed interval `[low, high]`, loop `while (low <= high)`; `mid = low + (high-low)/2` (overflow-safe); `==`→return `mid`, `>`→`high = mid-1`, `<`→`low = mid+1`. Return `-1` after the loop. `mid ± 1` (never `mid`) makes the window strictly shrink; `<=` (not `<`) so the size-1 window is still checked. The exact-match seed of the whole pattern |
| ✅ | 02/06/26 | 35  | [Search Insert Position](https://leetcode.com/problems/search-insert-position/) | 🟢 | "sorted distinct → index of `target`, **or where it'd be inserted**" → it's the 704 loop with `return -1` swapped for **`return low`** (the insertion point = #elements `< target`). Or the pure **lower-bound** template: `high = n`, `while (low < high)`, `nums[mid] < target ? low = mid+1 : high = mid`, return `low`. This is the reusable seed for "min X such that check(X)" / binary-search-on-answer. Watch: don't return `-1`; lower-bound's `high` is `n` not `n-1`, and pairs `high = mid` (no `-1`) with `low < high` |
| ✅ | 03/06/26 | 278 | [First Bad Version](https://leetcode.com/problems/first-bad-version/) | 🟢 | "versions `1..n`, once one is bad **all later ones are bad** → first bad version in the **fewest API calls**" → the bad flags form `F…F T…T`, so binary-search the **leftmost `T`**. It's the lower-bound loop from 35 with the predicate swapped from a value compare to an **API call**: `low = 1, high = n`, `while (low < high)`, `mid = low + (high-low)/2`; `isBadVersion(mid)` → `high = mid` (keep — mid may be first bad), else `low = mid + 1`; return `low`. First "binary-search-on-a-**predicate**", not on array values. Watch: `n` up to `INT_MAX` makes overflow-safe `mid` mandatory; `high = mid` (no `-1`) pairs with `low < high`; call the API once per loop |
| ✅ | 03/06/26 | 33  | [Search in Rotated Sorted Array](https://leetcode.com/problems/search-in-rotated-sorted-array/) | 🟡 | "ascending **distinct** array **rotated** at an unknown pivot → index of `target` in **O(log n)**" → it's the 704 closed-interval loop (`low=0, high=n-1`, `while (low <= high)`, overflow-safe `mid`) with one extra decision: the array isn't globally sorted, but **at least one half always is**. Test `nums[low] <= nums[mid]` (`<=`, for the `low==mid` case) → left half sorted: if `nums[low] <= target < nums[mid]` go left (`high=mid-1`) else right (`low=mid+1`); otherwise right half sorted: if `nums[mid] < target <= nums[high]` go right else left. Hit (`nums[mid]==target`) checked first; endpoint comparisons inclusive, `mid` side exclusive. Watch: strict `nums[low] < nums[mid]` breaks size-1 windows; only range-check against the half you've **proven** sorted; relies on distinct values (dups → LC 81, O(n) worst case) |
| ✅ | 04/06/26 | 153 | [Find Minimum in Rotated Sorted Array](https://leetcode.com/problems/find-minimum-in-rotated-sorted-array/) | 🟡 | "ascending **distinct** array **rotated** at an unknown pivot → return the **minimum** in **O(log n)**" → the min *is* the rotation pivot, so binary-search for it. Half-open window `low=0, high=n-1`, `while (low < high)`, overflow-safe `mid`; compare `nums[mid]` to **`nums[high]`** (not `nums[low]` — that's always `<= nums[mid]` on a sorted array, tells you nothing): `nums[mid] > nums[high]` → dip is strictly right (`low = mid+1`), else pivot is at `mid` or left so keep it (`high = mid`, **no `-1`** — `mid` is a live candidate). `low == high` lands on the min → `return nums[low]`. Watch: `high = mid` pairs with `low < high` (closed loop + `mid-1` can skip the answer); relies on distinct values (dups → LC 154, `high--` on tie, O(n) worst). Alt: compare to `nums[low]` but then you need an explicit `mini` tracker + closed `low <= high` loop |
| ✅ | 04/06/26 | 34  | [Find First and Last Position of Element](https://leetcode.com/problems/find-first-and-last-position-of-element-in-sorted-array/) | 🟡 | "sorted array **with duplicates** → the **first and last index** of `target`, else `[-1,-1]`, in **O(log n)**" → run the 704 loop **twice** with an `index = -1` sentinel; the trick is to **not stop on a hit**. First pass (lower bound): on `nums[mid] == target` save `index = mid` then `high = mid-1` (keep probing left); else `>` → `high = mid-1`, `<` → `low = mid+1`. Second pass (upper bound): identical but on a hit `low = mid+1` (probe right). The **tie-break direction on `==` is the only difference**. Return `{lower, upper}`. Watch: empty array makes `n = size()-1 = -1`, and `low <= high` (`0 <= -1`) no-ops the loop so you never touch `nums[-1]`; absent target falls out as `[-1,-1]` from both passes; a one-hit-then-linear-expand approach is O(n) on all-equal arrays (violates log n) | 
| ✅ | 05/06/26 | 162 | [Find Peak Element](https://leetcode.com/problems/find-peak-element/) | 🟡 | "find **any** peak (`> both neighbors`) in **O(log n)** on an array that's **not sorted**, `nums[i] != nums[i+1]`" → can't binary-search a value with no sorted order, so binary-search the **slope**: half-open `low=0, high=n-1`, `while (low < high)`, overflow-safe `mid`; compare `nums[mid]` to **`nums[mid+1]`** — `>` ⇒ falling, peak at `mid` or left (`high = mid`, **no `-1`**, `mid` is a live candidate), else rising ⇒ peak strictly right (`low = mid+1`); `low == high` lands on a peak → `return low`. The imaginary `-∞` walls at both ends make a peak **unavoidable** (walking uphill must hit one), so there's no "not found" case and any peak is accepted. `mid+1` is always in range because `low < high` ⇒ `mid < high`. The single right-neighbor test subsumes all the `mid==0` / `mid+1==n` / `n==1` / `n==2` edge cases of the check-both-neighbors version. Same `high=mid`/`low=mid+1` mechanics as 153 but the signal is the **local slope**, not a global pivot — that's why it works with zero sorted structure |
| ✅ | 06/06/26 | 74  | [Search a 2D Matrix](https://leetcode.com/problems/search-a-2d-matrix/) | 🟡 | "`m x n` matrix, **each row sorted** AND **first of each row > last of the previous row** → is `target` present in **O(log(m·n))**" → the two properties together make the row-major read **one globally sorted array of length `m*n`**, so it's plain 704 over an imaginary 1D array. Closed window `low=0, high=rows*cols-1`, `while (low <= high)`, overflow-safe `mid`; decode the flat index to a cell with `value = matrix[mid / cols][mid % cols]` — **divide & mod by the number of COLUMNS (= row length), never the rows** — then standard `==`→`true`, `>`→`high=mid-1`, `<`→`low=mid+1`, fall out → `false`. The *only* new idea vs 704 is the index↔(row,col) arithmetic. Watch: divmod by the **row count** instead of cols silently goes out-of-bounds whenever `cols > rows` (e.g. `[[1,1]], target=2` → reads non-existent `matrix[1][0]`) and only "works" by luck on square/tall grids — name the vars `rows`/`cols` so the divisor is obviously the row length; pair `<=` with `high=mid-1` (exact-match, not the half-open `high=mid`). **Don't confuse with LC 240** (rows and columns each sorted *independently*, not globally) — that one can't be flattened and needs the top-right staircase walk in O(m+n) |
| ✅ | 06/06/26 | 875 | [Koko Eating Bananas](https://leetcode.com/problems/koko-eating-bananas/) | 🟡 | "**minimum integer speed `k`** so Koko clears all piles in `≤ h` hours, where a pile of `p` takes `ceil(p/k)` hours" → there's **no array to search** — binary-search the **answer itself**. The cost `hours(k) = Σ ceil(piles[i]/k)` is **monotonically non-increasing in `k`**, so feasibility (`hours ≤ h`) flips false→true exactly once → lower-bound the first feasible speed. Range `[1, max(piles)]` (`low = 1` — speed 0 is meaningless **and divides by zero**; `high = max(piles)` is tight since at that speed every pile is 1 hr, total `= n ≤ h`, always feasible). Predicate `canFinish(k)`: sum `(p + k - 1) / k` (integer ceil, no floats), **early-`return false` once the running sum > h** (also caps it). Loop: half-open `while (low < high)`, on feasible `high = mid` (keep mid — no `-1`), else `low = mid + 1`, return `low`. Watch: **`long` accumulator** — `n·ceil` reaches ~10¹³, overflows `int`; the predicate must encode "fast enough" (`hours ≤ h`) not "too slow" (`hours >= h`) or the search runs the wrong direction and returns the max speed; closed `<=` pairs with `high = mid-1`, half-open `<` with `high = mid` — never mix. The **binary-search-on-answer** archetype (same lower-bound shape as 35/278 but the predicate is a *computed cost*, not an array compare or API) — siblings 1011, 410 |
| ✅ | 06/07/26 | 1011| [Capacity to Ship Packages Within D Days](https://leetcode.com/problems/capacity-to-ship-packages-within-d-days/) | 🟡 | "**minimum ship capacity** so packages shipped **in order** (no reordering/splitting) fit within `days` days, where each day greedily loads consecutive packages up to the capacity" → **binary-search the answer**, not an array. Range `[max(weights), sum(weights)]` — `lo = max` because a capacity below the heaviest package can never carry it; `hi = sum` is everything in one day. Predicate `possible(cap)`: walk left→right with a running `load`, start `reqDay = 1` (first day already in progress), and when `load + w > cap` **close the day** (`reqDay++`, `load = 0`) before adding `w` — **`load += w`, never `load = w`** (overwriting undercounts days and returns a too-small answer). Early-`return false` once `reqDay > days`. Days-needed is **monotonically non-increasing in cap**, so feasibility flips false→true once → lower-bound it: closed `while (lo <= hi)`, on feasible `hi = mid - 1`, else `lo = mid + 1`, return `lo`. Watch: it's a **sequential greedy fill**, not per-item `ceil` (that's the split-allowed model — wrong here); closed `<=` pairs with `hi = mid-1`, half-open `<` with `hi = mid` — never mix; kill stray `cout`s (corrupt stdout → WA). Koko 875's twin — same lower-bound-on-answer shape, cost is a partition instead of a per-item ceil; sibling 410 |
| ✅ | 06/08/26 | 410 | [Split Array Largest Sum](https://leetcode.com/problems/split-array-largest-sum/) | 🔴 | "**split `nums` into `k` contiguous subarrays so the largest subarray sum is minimized**" → the **"minimize the maximum"** phrasing is the binary-search-on-answer tell. Flip it: *given a cap `S`, can we split into ≤ k pieces each ≤ S?* Pieces-needed is **monotonically non-increasing in `S`**, so feasibility flips false→true once → lower-bound the smallest feasible cap. Range `[max(nums), sum(nums)]` — `lo = max` (a piece must hold the biggest element alone; this also guarantees every `num ≤ mid` so the greedy never stalls), `hi = sum` (whole array as one piece). Predicate `possible(S)`: walk left→right, `count = 1` (first piece already open), on `sum + num > S` **cut** (`count++`, `sum = 0`) then `sum += num` (**`+=` not `=`**), early-`return false` once `count > k`. "≤ k" ≡ "== k" here because a coarser split can always be cut further (since `k ≤ n`) without raising any sum. Closed `while (lo <= hi)`, on feasible `hi = mid - 1`, else `lo = mid + 1`, return `lo`. Watch: `count = 1` not 0 (the `++` counts only cuts); `lo = max(nums)` not 0/1; sequential greedy fill, **not** per-item `ceil`; overflow is fine here (`sum ≤ 10⁹` fits `int`) but use `long`/`sum > S - num` if limits grow. **Literally [[10-1011-capacity-to-ship-packages]] with `days → k`** — same code, same `[max, sum]` range; both are Koko 875's descendants. (DP `dp[i][j]` in O(n²k) also works — slower, use if you need the partition.) |
| ✅ | 06/08/26 | 4   | [Median of Two Sorted Arrays](https://leetcode.com/problems/median-of-two-sorted-arrays/) | 🔴 | "**median of two sorted arrays in O(log(m+n))**" → the `O(log)` bound kills the merge; **binary-search the partition**, not a value. Split the union into a left half of `(m+n+1)/2` elements (the `+1` parks the odd extra on the left → median = `max(left)`) and a right half with `max(left) ≤ min(right)`. Pick `cut1` from `nums1`; `cut2 = (m+n+1)/2 − cut1` is forced. **Binary-search `cut1 ∈ [0, n1]` over the SMALLER array** (swap if `n1>n2`, else `cut2` goes negative). Boundaries with sentinels: `l1=cut1>0?a[cut1-1]:INT_MIN`, `r1=cut1<n1?a[cut1]:INT_MAX`, and likewise `l2`/`r2` with `cut2` (INT_MIN/MAX make take-none/take-all legal partitions). Cross-check `l1≤r2 && l2≤r1`: valid → even `(max(l1,l2)+min(r1,r2))/2.0` (use `2.0`!), odd `max(l1,l2)`; `l1>r2` ⇒ `high=cut1-1`, else `low=cut1+1`. **#1 bug: never cross `mid1`/`mid2`** — every `nums1` access (index *and* guard) uses `mid1`, every `nums2` access uses `mid2`; `nums2[mid1-1]` or a `mid1<n2` guard reads `nums2[-1]`/`nums2[n2]` OOB. Unlike 875/410 (search a value-range) this searches *where to cut* and the answer falls out of the boundaries. Alt: two-pointer merge-to-middle O(m+n) if log isn't required | O(log(min(m,n))) | O(1) |

---

# 6. Stack & Monotonic Stack

**When to use it (recognition cues):**
- Matching pairs (parentheses, tags).
- "Next/previous greater/smaller element".
- Histogram / rectangles / stock span / temperatures.

**Core idea:** Push while increasing/decreasing; pop on violation. Each element pushed/popped once → O(n).

**Template (C++):**
```cpp
stack<int> st; // indices
vector<int> nge(n, -1);
for (int i = 0; i < n; ++i) {
    while (!st.empty() && nums[st.top()] < nums[i]) {
        nge[st.top()] = nums[i]; st.pop();
    }
    st.push(i);
}
```

**Problems:**

| ✓ | Date | # | Problem | Diff | Trigger phrase |
|---|------|---|---------|------|----------------|
| ✅ | 09/06/26 | 20  | [Valid Parentheses](https://leetcode.com/problems/valid-parentheses/) | 🟢 | "are `()[]{}` balanced **and** correctly nested?" → LIFO: push openers, on a closer pop iff `top` is its matching opener (guard `!empty()` first — bare closer is UB), valid iff stack ends empty. Match the **type**, not just "closed something" (`"([)]"` must fail); leftover openers ⇒ false. Counter-only can't see order. Odd length ⇒ instant false |
| ✅ | 09/06/26 | 155 | [Min Stack](https://leetcode.com/problems/min-stack/) | 🟡 | "stack that also returns its **min in O(1)**" → can't recompute (O(n)); **store the running min beside each element** so it's always at the top. Pair-stack `{value, minAtOrBelow}` — push `min(v, top.second)`, pop frees the prior min automatically. O(1)-space variant encodes a sentinel `2v−mini` on a new min, decodes `2mini−x` on pop — but needs **64-bit everywhere** (`int x = top()` truncates the sentinel → fails on INT_MIN/MAX). Single-min-var can't restore the min after popping it |
| ✅ | 11/06/26 | 150 | [Evaluate Reverse Polish Notation](https://leetcode.com/problems/evaluate-reverse-polish-notation/) | 🟡 | "evaluate a **postfix / RPN** expression (no parens)" → operands wait on a stack, an operator consumes the **last two**. Push numbers; on `+ - * /` pop `b` (top = **right**), then `a` (left), push `a op b`. **Order matters** for `-` and `/` — swapping passes `+`/`*` but fails the rest. C++ `int/int` already truncates toward zero (free); don't classify by `s[0]=='-'` — `"-11"` is a negative number, compare the whole string. Final lone `top()` is the answer |
| ✅ | 11/06/26 | 739 | [Daily Temperatures](https://leetcode.com/problems/daily-temperatures/) | 🟡 | "days until a **strictly warmer** temp?" = **next greater element**, but return the **distance** not the value → **monotonic stack of indices**, temps decreasing. L→R: while `T[i] > T[st.top()]` pop `j`, set `res[j]=i-j`; push `i`; unresolved stay 0. Store **indices** (answer is a gap); **strict** warmer = mind the `<`/`<=` boundary (equal temp doesn't count). Each index pushed/popped once ⇒ O(n) |
| ✅ | 12/06/26 | 496 | [Next Greater Element I](https://leetcode.com/problems/next-greater-element-i/) | 🟢 | "first **greater element to the right**, queried from a subset array" → precompute NGE for **all** of `nums2` in one monotonic-stack pass, store in a `value → answer` map (legal: values **unique**), answer each `nums1[i]` by O(1) lookup ⇒ O(n1+n2). R→L: pop while `top <= num` (an equal element is never anyone's *greater*), record `top` or `-1`, push `num`. Answer is a **value** → stack of values OK; with duplicates or distances, switch to indices |
| ✅ | 12/06/26 | 503 | [Next Greater Element II](https://leetcode.com/problems/next-greater-element-ii/) | 🟡 | "next greater element, but the array is **circular**" → same monotonic-stack template scanned **twice**: loop `i = 2n−1 … 0` on `nums[i % n]`, pop while `top <= num`, write `res[i]` **only when `i < n`** (first sweep just seeds wrap-around candidates), push every step ⇒ O(n), each element pushed/popped ≤ 2×. **Duplicates exist** → `<=` pop is load-bearing and answers go **by index** (496's value-map trick is illegal here). Max always gets −1 |
| ✅ | 13/06/26 | 901 | [Online Stock Span](https://leetcode.com/problems/online-stock-span/) | 🟡 | "prices stream in **one at a time**; for each, how many **consecutive days back** (incl. today) were `≤` today?" = distance to the **previous strictly-greater** price → **decreasing monotonic stack**, pop while `top ≤ price` (`≤`, since equal days *count*) **accumulating their spans** into today's. Start `span=1` (today). Editorial stores `{price, span}` (self-contained); index variant stores `{price, day}` and returns the gap `i - top.day`. **Online** = no peeking ahead, all work in `next`; previous-greater (mirror of 739's next-greater). O(1) amortized |
| ✅ | 13/06/26 | 71  | [Simplify Path](https://leetcode.com/problems/simplify-path/) | 🟡 | "canonicalize a Unix path — collapse `//`, drop `.`, `..` goes up a level" → **stack** of dir names: split on `/`, a real name **pushes**, `..` **pops** (iff non-empty — can't go above root, `/../`→`/`), `.`/empty **ignore**. `...`/`....`/`_` are **valid names** (match *exactly* `.`/`..`). Append a **sentinel `/`** so the last segment flushes in-loop (else the post-loop word re-introduces the `..`/`.` bug). Join front→back; empty ⇒ `"/"`. Use a **local** `vector` (not a member deque). O(n)/O(n) — optimal (must remember dirs for `..` to undo) |
| ✅ | 14/06/26 | 394 | [Decode String](https://leetcode.com/problems/decode-string/) | 🟡 | "expand `k[encoded]`, possibly **nested** (`3[a2[c]]`)" → **stack the context**: keep `current` string + `num`; digit → `num=num*10+(ch-'0')` (**multi-digit**); `[` → push `num` & `current` to two stacks, reset; `]` → pop `k` & `prev`, `current = prev + current*k`; letter → `current+=ch`. Two stacks (count + string) beats single-string stack (no `stoi`, no `[`-marker, **append** not prepend → no O(n²), no `to_string`/`isdigit(top)`/empty-`stoi` traps). Innermost-first = LIFO = recursion made explicit | O(N_out) | O(N_out) |
| ✅ | 15/06/26 | 22  | [Generate Parentheses](https://leetcode.com/problems/generate-parentheses/) | 🟡 | "generate **all** well-formed combinations of `n` pairs" → backtracking DFS, **not** generate-then-filter. Carry `open`/`close` = parens **remaining**; **add `(` when `open > 0`**, **add `)` when `close > open`** (unmatched `(` in string = `close − open`, must be `> 0`). Both 0 ⇒ push (no validity check needed — guards guarantee it). Append → recurse → `pop_back()` to backtrack the shared `string&` (or pass by value). Bugs: dangling `return` outside braces; inverted guard `open > close` → `)` never fires → **empty result**; missing `pop_back`. Count = **Catalan(n)** ⇒ O(4ⁿ/√n), prune beats 2^(2n) |
| ✅ | 15/06/26 | 84  | [Largest Rectangle in Histogram](https://leetcode.com/problems/largest-rectangle-in-histogram/) | 🔴 | "fixed-width bars, biggest rectangle?" → each bar is the **shortest** bar of some rectangle, which stretches to its **previous-smaller** and **next-smaller** boundaries → one **increasing** monotonic stack of **indices**. When bar `i` pops `j`: `i` is `j`'s right wall, the new top is `j`'s left wall, `area = h[j] × (i − top − 1)` (or `i` if stack empties). **Sentinel 0** at `i==n` flushes the stack (no separate drain). Pop on `≥` not `>` (equal heights), guard empty-stack width. Reuse row-by-row for Maximal Rectangle (85). O(n)/O(n) |
| ✅ | 16/06/26 | 85  | [Maximal Rectangle](https://leetcode.com/problems/maximal-rectangle/) | 🔴 | "largest all-1s rectangle in a binary matrix" → sweep rows top→bottom building `heights[]` per column (`+1` on `'1'`, **reset 0** on `'0'`), run **Largest Rectangle in Histogram (84)** on each row, take the max. The Hard 1-D monotonic-stack routine lifted to 2-D. O(rows·cols)/O(cols) |

---

# 7. Linked List Manipulation

**When to use it (recognition cues):**
- Reverse / partial reverse / k-group reverse.
- Merge / sort / split a list.
- Pointer rewiring without extra space.

**Core idea:** Use dummy head + 3 pointers (prev/curr/next). Draw arrows on paper before coding.

**Template (C++):**
```cpp
ListNode dummy(0); dummy.next = head;
ListNode *prev = &dummy, *curr = head;
while (curr) { /* rewire */ }
return dummy.next;
```

**Problems:** _Basics already drilled (reverse / merge / partition / cycle done earlier) — only the MAANG-staple hards retained here. Scheduled in the Week 17 breadth block._

| ✓ | Date | # | Problem | Diff | Trigger phrase |
|---|------|---|---------|------|----------------|
| ☐ |      | 25  | [Reverse Nodes in k-Group](https://leetcode.com/problems/reverse-nodes-in-k-group/) | 🔴 |  |
| ☐ |      | 138 | [Copy List with Random Pointer](https://leetcode.com/problems/copy-list-with-random-pointer/) | 🟡 |  |
| ☐ |      | 23  | [Merge k Sorted Lists](https://leetcode.com/problems/merge-k-sorted-lists/) | 🔴 |  |
| ✅ | Aug 18 | 146 | [LRU Cache](https://leetcode.com/problems/lru-cache/) | 🟡 | "**least recently used**" + "**evict**" + an explicit "**`get`/`put` in O(1) average**" → hash map (lookup) + **doubly** linked list (recency) with head/tail **sentinels**; front = MRU, `tail->prev` = victim. **Node stores the KEY** so eviction can `mpp.erase(victim->key)` in O(1) (else an O(n) map scan) — copy/erase it **before** `delete`. `put` on an existing key = **update + move-to-front, never evict**. Doubly because unlinking needs the predecessor (also in #19 Design) |

---

# 8. Tree DFS

**When to use it (recognition cues):**
- Path / sum / diameter / depth in a binary tree.
- Need info from both subtrees combined upward.
- "Validate" a tree property.

**Core idea:** Recurse; the function returns information that the parent uses to combine left + right.

**Template (C++):**
```cpp
int dfs(TreeNode* node) {
    if (!node) return 0;
    int L = dfs(node->left), R = dfs(node->right);
    // combine L, R, node->val
    return /* value to parent */;
}
```

**Problems:**

| ✓ | Date | # | Problem | Diff | Trigger phrase |
|---|------|---|---------|------|----------------|
| ✅ | Jun 16 | 104 | [Maximum Depth of Binary Tree](https://leetcode.com/problems/maximum-depth-of-binary-tree/) | 🟢 | "how deep/tall is the tree" → post-order, each node returns `1 + max(L, R)`, null returns 0 |
| ✅ | Jun 16 | 100 | [Same Tree](https://leetcode.com/problems/same-tree/) | 🟢 | "two trees identical (shape + values)" → parallel DFS, 3 base cases (both null/one null/val differ), recurse L-L & R-R |
| ✅ | Jun 17 | 226 | [Invert Binary Tree](https://leetcode.com/problems/invert-binary-tree/) | 🟢 | "mirror/flip the tree left↔right" → swap each node's two children, recurse both; order irrelevant. Mutate-in-place DFS, return root |
| ✅ | Jun 18 | 543 | [Diameter of Binary Tree](https://leetcode.com/problems/diameter-of-binary-tree/) | 🟢 | "longest path between any two nodes, in edges, may skip root" → height recursion, at every node update global `max(L+R)`, return `1+max(L,R)` |
| ✅ | Jun 18 | 110 | [Balanced Binary Tree](https://leetcode.com/problems/balanced-binary-tree/) | 🟢 | "every node's two subtree heights differ by ≤1" → height recursion + `-1` sentinel: return `-1` if any child is `-1` or `abs(L-R)>1`, else `max(L,R)+1`; balanced iff root ≠ `-1` |
| ✅ | Jun 19 | 112 | [Path Sum](https://leetcode.com/problems/path-sum/) | 🟢 | "is there a root-to-leaf path summing to target" → DFS carrying remaining budget (`target - val`), at a leaf test `remaining == 0`, bubble up `left \|\| right`; null tree → false |
| ✅ | Jun 21 | 98  | [Validate Binary Search Tree](https://leetcode.com/problems/validate-binary-search-tree/) | 🟡 | "is this a valid BST (strict <, recursive)" → DFS carrying an inherited `(low, high)` range; node valid iff `low < val < high`, left tightens high→val, right tightens low→val, `&&` both; seed `long long ±∞` so `INT_MIN/MAX` fit. Local parent-child check is **not** enough |
| ✅ | Jun 21 | 230 | [Kth Smallest Element in a BST](https://leetcode.com/problems/kth-smallest-element-in-a-bst/) | 🟡 | "kth smallest value in a BST" → in-order DFS (left→node→right gives sorted), share `k` **by reference**, `--k` per visit, return `val` when `k==0`, `-1` sentinel bubbles up. Bug: recursing into by-value `k` loses the count. Follow-up: augment subtree sizes for O(h) |
| ✅ | Jun 21 | 236 | [Lowest Common Ancestor of a Binary Tree](https://leetcode.com/problems/lowest-common-ancestor-of-a-binary-tree/) | 🟡 | "LCA of two nodes in a **plain** binary tree" → post-order DFS returning the found node; base `!root \|\| root==p \|\| root==q → root`; if **both** children return non-null, this node is the LCA, else bubble up whichever side hit. Match-returns-node handles self-ancestor; no BST shortcut (that's 235) |
| ✅ | Jun 22 | 105 | [Construct Binary Tree from Preorder and Inorder Traversal](https://leetcode.com/problems/construct-binary-tree-from-preorder-and-inorder-traversal/) | 🟡 | "rebuild tree from preorder + inorder" → march one shared `preIdx` through preorder (each elem = next root), use a `value→inorder index` map to split into left range `[s, mid-1]` / right range `[mid+1, e]`; recurse **left before right**, base `s > e`. Inorder gives the split → uniqueness; O(n) |
| ✅ | Jun 22 | 124 | [Binary Tree Maximum Path Sum](https://leetcode.com/problems/binary-tree-maximum-path-sum/) | 🔴 | "max path sum, path bends anywhere, may skip root" → post-order; each child arm clamped `max(0, dfs(child))` (drop negative branches), update global `best = val + left + right` (bend here), **return** `val + max(left, right)` (straight arm for parent). Init `best = INT_MIN` (single negative node valid). Clamp the **arms**, not `val`. Value twin of 543 Diameter |
| ✅ | Jun 23 | 297 | [Serialize and Deserialize Binary Tree](https://leetcode.com/problems/serialize-and-deserialize-binary-tree/) | 🔴 | "serialize/deserialize a tree, convert tree ↔ string" → pick **one** traversal (preorder DFS or level-order BFS), emit `#` for **every null** (that's what makes shape unambiguous), decode by consuming tokens in the **same** order. Build **left before right**; split on `,` not `-` (negatives); handle empty tree. Mirror of 105 but with explicit nulls → one traversal suffices |

---

# 9. Tree BFS

**When to use it (recognition cues):**
- "Level order", "right side view", "zigzag".
- Minimum depth / shortest path in unweighted tree.
- Process tree level-by-level.

**Core idea:** Queue + iterate one level at a time using `queue.size()` snapshot.

**Template (C++):**
```cpp
queue<TreeNode*> q; q.push(root);
while (!q.empty()) {
    int sz = q.size();
    for (int i = 0; i < sz; ++i) {
        auto n = q.front(); q.pop();
        if (n->left)  q.push(n->left);
        if (n->right) q.push(n->right);
    }
}
```

**Problems:**

| ✓ | Date | # | Problem | Diff | Trigger phrase |
|---|------|---|---------|------|----------------|
| ✅ | Jun 23 | 102 | [Binary Tree Level Order Traversal](https://leetcode.com/problems/binary-tree-level-order-traversal/) | 🟡 | "level order, group nodes by depth, process level-by-level" → queue + **snapshot `sz = q.size()` before** the inner loop (queue *is* the level at that instant), drain `sz` nodes into a per-level vector, push non-null children for the next level. Empty tree → `[]`. The founding Tree BFS skeleton — zigzag/right-view/level-averages are all this + one tweak |
| ✅ | Jun 23 | 107 | [Level Order Traversal II](https://leetcode.com/problems/binary-tree-level-order-traversal-ii/) | 🟡 | "bottom-up level order, leaf level first" → do plain 102 level order, then **`reverse(result.begin(), result.end())`** once (flips outer list of levels, O(#levels); inner left→right stays). Avoid front-insert per level — that's O(levels²). 102 + one reverse |
| ✅ | Jun 24 | 103 | [Zigzag Level Order Traversal](https://leetcode.com/problems/binary-tree-zigzag-level-order-traversal/) | 🟡 | "zigzag / snake / serpentine by level, left→right then right→left alternating" → plain 102 BFS (enqueue stays left→right!), but **pre-size `vector<int> level(sz)`** and write each value at `index = leftFirst ? i : sz-1-i`, flipping `leftFirst` per level. No `reverse` needed — placement *is* the flip. Don't reverse the traversal (mirrors next level's children) |
| ✅ | Jun 25 | 199 | [Binary Tree Right Side View](https://leetcode.com/problems/binary-tree-right-side-view/) | 🟡 | "right side view, rightmost node per level, silhouette seen from the right" → plain 102 BFS but collect only **one** value per level: `q.back()->val` (the level's last node, read **before** pushing its children). "Right side" ≠ always-go-right — a missing right child exposes the deepest left descendant, which level order handles automatically. Left view = `q.front()`. DFS alt: recurse **right-first**, take the first node seen per depth (O(h) space) |
| ✅ | Jun 25 | 637 | [Average of Levels in Binary Tree](https://leetcode.com/problems/average-of-levels-in-binary-tree/) | 🟢 | "average / sum per level, one aggregate per depth" → plain 102 BFS but fold each level into a single number: snapshot `size`, sum the level's values, push `sum / size`. **Sum into a `double`** (or `long long`) — values span the full `int` range and a level can be wide, so a bare `int`/`long` sum overflows. `double / int` yields real division. DFS alt: depth-indexed `sum[]`/`cnt[]`, divide at end (O(h) space) |
| ✅ | Jun 26 | 111 | [Minimum Depth of Binary Tree](https://leetcode.com/problems/minimum-depth-of-binary-tree/) | 🟢 | "minimum depth, shortest path to the nearest leaf" → plain 102 BFS but **return `depth` at the first leaf popped** (`!left && !right`); BFS reaches the shallowest leaf first, so it short-circuits (DFS must explore everything). **One-child trap:** a node with a single child is **not** a leaf — naive `1 + min(left, right)` wrongly returns via the null side; recurse only into real children or special-case it. DFS alt: `(!l_child‖!r_child) ? 1+l+r : 1+min(l,r)`, O(h) space. Empty tree → 0 |
| ✅ | Jun 27 | 116 | [Populating Next Right Pointers in Each Node](https://leetcode.com/problems/populating-next-right-pointers-in-each-node/) | 🟡 | "connect each node to its right neighbour, next-right pointer, per-level linked list" → plain 102 BFS but instead of collecting, link node `i` to the next in queue: `if (i < size-1) n->next = q.front()` (front is the right sibling since children push to the back); last node keeps its initial `NULL` (no need to set it). **Follow-up O(1) space:** drop the queue — the tree is **perfect**, so use the `next` pointers already built to walk each level: `head->left->next = head->right; if(head->next) head->right->next = head->next->left`, then `leftmost = leftmost->left`. Only works because perfect (LC 117 isn't → needs a dummy node per level) |
| ✅ | Jun 27 | 117 | [Populating Next Right Pointers II](https://leetcode.com/problems/populating-next-right-pointers-in-each-node-ii/) | 🟡 | "connect each node to its right neighbour on **any/general** binary tree (ragged levels)" → **same task as 116 but tree isn't perfect**, so the BFS-with-queue code is *identical* (link node `i` to `q.front()`). The O(1) trick breaks — any of `head->left/right/next->left` can be null — so use a **dummy node**: walk the current level via `next`, thread each real child onto a `dummy → prev` chain, then `curr = dummy.next` to descend. `dummy.next` is the next level's head; fresh `dummy` per level. This dummy-node code also solves 116 (the general answer). DFS alt: recurse **right-subtree first** so `next` is set before children query it |
| ✅ | Jun 27 | 515 | [Find Largest Value in Each Tree Row](https://leetcode.com/problems/find-largest-value-in-each-tree-row/) | 🟡 | "largest / max value in each row, one aggregate per depth, per-level reduce" → plain 102 BFS but fold each level into a single number: seed `maxi = INT_MIN`, take `maxi = max(maxi, node->val)` over the level, push `maxi`. **Seed `INT_MIN`, never `0`** — values span the full `int` range incl. negatives (down to `INT_MIN`), so a `0` seed wrongly reports 0 for an all-negative level. Twin of 637 (`max` instead of `sum`) but *easier* — comparing has no overflow/`double` trap. DFS alt: recurse carrying `depth`, push a new slot on first reach else `res[d] = max(res[d], val)`, O(h) space |
| ✅ | Jun 29 | 1161| [Maximum Level Sum of a Binary Tree](https://leetcode.com/problems/maximum-level-sum-of-a-binary-tree/) | 🟡 | "smallest level x with maximal sum, argmax level by sum, return the depth not the value" → 102 BFS, sum each level like 637 but **return the level index**, not the sum. Track `bestLevel` with **strict `if (maxSum < sum)`** so a tie keeps the **smallest** level — `<=` would return the *last* max level (the signature bug). **Levels 1-indexed** (`++level` at top, root = 1). Seed `maxSum = INT_MIN` so an all-negative tree still picks a real level. Sum fits in `int` (≤ ~10^9). DFS alt: depth-indexed `sums[]`, argmax with strict `>`, `+1`, O(h) space |

---

# 10. Graphs — BFS / DFS / Topological Sort

**When to use it (recognition cues):**
- Grid / matrix → 4-directional / 8-directional traversal (islands, regions, rotting).
- Connected components, bipartite check.
- "Course schedule", dependencies → topological sort.
- Shortest path in unweighted graph → BFS.

**Core idea:** BFS for shortest unweighted path; DFS/Union-Find for components; Kahn's BFS for topo.

**Template (C++) — Topo (Kahn's):**
```cpp
vector<int> indeg(n, 0), order;
for (auto& e : edges) indeg[e[1]]++;
queue<int> q;
for (int i = 0; i < n; ++i) if (!indeg[i]) q.push(i);
while (!q.empty()) {
    int u = q.front(); q.pop(); order.push_back(u);
    for (int v : adj[u]) if (--indeg[v] == 0) q.push(v);
}
```

**Problems:**

| ✓ | Date | # | Problem | Diff | Trigger phrase |
|---|------|---|---------|------|----------------|
| ✅ | Jul 1 | 200 | [Number of Islands](https://leetcode.com/problems/number-of-islands/) | 🟡 | "count connected blobs/regions of `1`s in a grid" → #islands = #connected components. Scan cells; each unvisited land cell → `++count` then flood-fill (DFS sink to `'0'` / BFS) its whole 4-adjacent island. **Mark visited on discovery**, not on pop. 4 dirs only. 300×300 ⇒ 90k-deep DFS can overflow → BFS. Dynamic-add → Union-Find (305) |
| ✅ | Jul 1 | 695 | [Max Area of Island](https://leetcode.com/problems/max-area-of-island/) | 🟡 | "largest island / biggest connected region / max area of `1`s" → same scan-and-flood as 200, but DFS **returns a size** (`1 + Σ dfs(neighbour)`) and outer loop keeps the `max`. Mark visited on entry; seed `maxArea = 0`. Sink-in-place = O(1) aux, `visited` grid = non-destructive. 4 dirs only |
| ✅ | Jul 1 | 994 | [Rotting Oranges](https://leetcode.com/problems/rotting-oranges/) | 🟡 | "every minute all rotten cells infect neighbours **simultaneously**; min minutes till none fresh" → **multi-source BFS, level = time** (NOT DFS). Seed queue with all rotten + count fresh; drain **ring by ring**, freeze `sz=q.size()`, mark rotten on discovery (`grid=2`). `fresh==0`→0, end `fresh>0`→-1 |
| ✅ | Jul 2 | 130 | [Surrounded Regions](https://leetcode.com/problems/surrounded-regions/) | 🟡 | "capture regions **fully enclosed** by `X`; a region touching an **edge** survives" → **invert it: flood from the border, not the interior.** An `O` is safe iff connected to a border `O`. BFS/DFS-seed every border `'O'`, tag reachable with temp `'#'`; then sweep: `'O'`→`'X'` (captured), `'#'`→`'O'` (restore). Marker = visited set. Prefer BFS at 200×200 (deep DFS overflows). Same border-flood as Pacific Atlantic (417) |
| ✅ | Jul 2 | 133 | [Clone Graph](https://leetcode.com/problems/clone-graph/) | 🟡 | "**deep copy** a graph with **cycles / shared neighbours**" → **DFS + `oldToNew` map (Node*→clone)**. On first visit `new Node(val)` and **store in map BEFORE recursing** on neighbours — the entry is both memo and cycle-breaker (store after the loop → infinite recursion). Link each cloned neighbour into `copy->neighbors`. `node? clone : nullptr` for empty graph. Key by pointer. BFS variant = same map + queue |
| ✅ | Jul 3 | 207 | [Course Schedule](https://leetcode.com/problems/course-schedule/) | 🟡 | "given **prerequisites / dependencies**, can you finish all courses / is there a valid order?" → **topological sort as cycle detection (Kahn's BFS)**. Edge `b → a` per `[a,b]` (b before a), `indegree[a]++`. Seed queue with all **indegree-0** nodes, peel one at a time (`count++`), decrement dependents, push at 0. `count == numCourses` ⇒ acyclic ⇒ `true` (a cycle never reaches indegree 0). Need the *count*, not the order. Alt: DFS 3-color, in-progress neighbour = back edge = cycle |
| ✅ | Jul 5 | 210 | [Course Schedule II](https://leetcode.com/problems/course-schedule-ii/) | 🟡 | "given **prerequisites / dependencies**, output **a valid order** (empty if impossible)" → **topological sort — same `b → a` DAG as 207, now emit the order.** **Kahn's BFS:** append each node **on pop** — pop sequence *is* the topo order; `topo.size() != numCourses` ⇒ cycle ⇒ `{}`. **DFS alt:** 3-color, collect **post-order** then **reverse** (prereqs finish first); in-progress neighbour = back edge = cycle ⇒ `{}`. BFS = append-on-pop (no reverse); DFS = post-order + reverse. Binary visited can't detect cycles |
| ✅ | Jul 7 | 785 | [Is Graph Bipartite?](https://leetcode.com/problems/is-graph-bipartite/) | 🟡 | "split nodes into **two groups** so **every edge crosses** between them (no edge inside a group)" → **2-coloring: bipartite ⇔ 2-colorable ⇔ no odd cycle.** Color a start `0`, force each neighbour to the **opposite** color (`!color`); a neighbour **already your color** = odd cycle ⇒ `false`. `colors` init `-1`. **Loop over all nodes** (disconnected). Conflict = *same-color*, not just visited. DFS or BFS, both O(V+E). Even cycles fine — only **odd** cycles break it |
| ✅ | Jul 8 | 417 | [Pacific Atlantic Water Flow](https://leetcode.com/problems/pacific-atlantic-water-flow/) | 🟡 | "cells from which water reaches **both** oceans (targets on opposite borders)" → **reverse the flow: flood inward from each ocean's border**, keeping equal-or-**higher** neighbours (guard `heights < prevH → return`); two visited grids `pac`/`atl`, seed Pacific from top+left & Atlantic from bottom+right, answer = `pac && atl`. Naive per-cell flood is O((m·n)²) TLE. Same border-anchored inward flood as Surrounded Regions (130) |
| ✅ | Jul 13 | 1091| [Shortest Path in Binary Matrix](https://leetcode.com/problems/shortest-path-in-binary-matrix/) | 🟡 | "shortest clear path corner→corner, move **8-directionally**, count **cells** visited" → **single-source BFS on an unweighted grid**. Seed `{(0,0), 1}` (length = cells, start at **1**), expand all **8** neighbours ring by ring; first *pop* of `(n-1,n-1)` = shortest. Guard blocked start/end → `-1` up front; **mark visited on push**; bounds before value. **DFS + backtracking = exponential TLE**, no memo fixes it — shortest path + unweighted ⇒ BFS always |
| ✅ | Jul 13 | 127 | [Word Ladder](https://leetcode.com/problems/word-ladder/) | 🔴 | "transform `beginWord`→`endWord` one letter at a time, each step a dictionary word; **length of shortest** chain" → **BFS on an implicit unweighted graph** (words = nodes, one-letter edits = edges). Generate neighbours by **mutating each position over 'a'–'z'** + hash-lookup — O(26·L) per word, never pairwise O(N²·L). The **set = dictionary + visited**: `erase` on enqueue (not pop → TLE). Length counts **words** so start level at **1**; `endWord ∉ set` → `0`; first pop of endWord = answer. Optimize: **bidirectional BFS** |
| ✅ | Jul 13 | 269 | [Alien Dictionary](https://leetcode.com/problems/alien-dictionary/) | 🔴 | "words are **sorted** in an **unknown alphabet**; recover the **letter order**" → **build a DAG from sorted words, then topo-sort (Kahn's).** Only **adjacent** word pairs constrain order and each gives **one** edge — the **first differing char** `s1[j] → s2[j]`, then break; `indegree[s2[j]]++`. Seed queue with **present** indegree-0 letters, peel & append; not all peeled ⇒ **cycle** ⇒ `""`. **Prefix rule:** `s1` longer than `s2` with `s2` a prefix (`["abc","ab"]`) ⇒ invalid `""`. **Index by letter `ch-'a'` (0–25), size to 26** — sizing by distinct count but indexing by letter = OOB. Only present letters in the answer. Same topo engine as 207/210, new part = *deriving* the edges |

**Weighted Graphs (Dijkstra / Bellman-Ford / MST):** edges have costs → shortest path is no longer plain BFS. Min-heap by distance (Dijkstra), relax-K-times (Bellman-Ford), or grow the cheapest tree (Prim/Kruskal MST). _Scheduled in the Week 17 breadth block._

| ✓ | Date | # | Problem | Diff | Trigger phrase |
|---|------|---|---------|------|----------------|
| ✅ | 2026-07-16 | 743 | [Network Delay Time](https://leetcode.com/problems/network-delay-time/) | 🟡 | Signal reaches all nodes: min-heap Dijkstra, answer = **max** of shortest dists, INT_MAX ⇒ -1 |
| ✅ | 2026-07-21 | 787 | [Cheapest Flights Within K Stops](https://leetcode.com/problems/cheapest-flights-within-k-stops/) | 🟡 | "cheapest `src→dst` with **at most `k` stops**" → hop-limited shortest path ⇒ **BFS on `{cost, stops, node}` with a plain FIFO `queue`**, `if (stops > k) continue;` at **pop** (`k` stops = `k+1` edges), no `visited[]`. **Dijkstra fails**: cost-only pruning lets a cheaper-but-too-long path evict the pricier fewer-hop path that could still reach `dst` — FIFO is safe because pops advance hops monotonically. Alt: Bellman-Ford `k+1` rounds with a `temp = dist` copy |
| ✅ | 2026-07-21 | 1584 | [Min Cost to Connect All Points](https://leetcode.com/problems/min-cost-to-connect-all-points/) | 🟡 | "connect **all** points at **min total cost**" (no `src`/`dst`) → **MST, not Dijkstra**. Graph is **complete + implicit**: node = index, weight = Manhattan. Prim heap pushes **`w`, not `dis + w`**; mark visited on **pop**. Complete graph ⇒ **dense Prim** O(n²)/O(n) beats heap O(n² log n)/O(n²) |
| ✅ | 2026-07-21 | 778 | [Swim in Rising Water](https://leetcode.com/problems/swim-in-rising-water/) | 🔴 | "water rises to `t`, pass a cell only if **value ≤ t**; **min `t`** to reach the corner" → **bottleneck / minimax path**: path cost = its **worst cell**, not the sum ⇒ **Dijkstra with `max` instead of `+`** (`max(time, grid[nr][nc])`), first **pop** of the corner = answer. Answer ≥ `max(grid[0][0], grid[n-1][n-1])`. Prim's-shaped variant (push raw elevation, take **running max over pops**) is equally correct — pops are **not** monotone (`0→5→1`), so that `max` is load-bearing. Alt: binary search `t` + BFS; **optimal**: values are a permutation of `0..n²-1` ⇒ **DSU threshold sweep**, no sort, O(n² α) |

---

# 11. Heap / Top-K / Two-Heaps

**When to use it (recognition cues):**
- "Top K", "K largest/smallest", "K closest to X".
- Median of running stream → two heaps.
- Merge K sorted things.
- Schedule with priorities / next event.

**Core idea:** A heap gives O(log n) insertion + O(1) min/max access. For "top K largest", keep a min-heap of size K.

**Template (C++):**
```cpp
priority_queue<int, vector<int>, greater<int>> minHeap; // size K
for (int x : nums) {
    minHeap.push(x);
    if (minHeap.size() > k) minHeap.pop();
}
```

**Problems:**

| ✓ | Date | # | Problem | Diff | Trigger phrase |
|---|------|---|---------|------|----------------|
| ✅ | Jul 15 | 215 | [Kth Largest Element in an Array](https://leetcode.com/problems/kth-largest-element-in-an-array/) | 🟡 | "kth largest, avoid sorting / stream" → size-K min-heap (top = kth largest); static array → quickselect O(n) |
| ✅ | Jul 15 | 703 | [Kth Largest Element in a Stream](https://leetcode.com/problems/kth-largest-element-in-a-stream/) | 🟢 | "kth largest so far after each add / live cut-off" → size-K min-heap kept as member; pop only if size > k (heap can start < k since k ≤ len+1) |
| ✅ | Jul 17 | 347 | [Top K Frequent Elements](https://leetcode.com/problems/top-k-frequent-elements/) | 🟡 | "k most frequent, beat O(n log n)" → hash-map count + size-K min-heap of {freq, num}; O(n) via bucket sort by frequency |
| ✅ | Jul 17 | 692 | [Top K Frequent Words](https://leetcode.com/problems/top-k-frequent-words/) | 🟡 | "k most frequent words, lexicographic ties" → 347 + custom comparator; min-heap = "worst on top" so BOTH keys invert; fill result backwards |
| ✅ | Jul 24 | 973 | [K Closest Points to Origin](https://leetcode.com/problems/k-closest-points-to-origin/) | 🟡 | "k closest points / k smallest by a metric, any order" → size-K **max**-heap keyed by dist²=x²+y² (top = farthest, evict it); never sqrt; static array → quickselect/`nth_element` O(n) |
| ✅ | Jul 27 | 658 | [Find K Closest Elements](https://leetcode.com/problems/find-k-closest-elements/) | 🟡 | "k closest to x, output sorted, input already sorted" → answer is a **contiguous window**, so binary-search the start `lo ∈ [0, n-k]` (drop `arr[mid]` vs gain `arr[mid+k]`, strict `>` = tie goes left) O(log(n-k)+k); heap fallback = size-K max-heap of `{|num-x|, num}` + final sort |
| ✅ | Jul 27 | 1046| [Last Stone Weight](https://leetcode.com/problems/last-stone-weight/) | 🟢 | "repeatedly take the largest two, transform, put the result back" → heap as a **simulation frontier** (not Top-K): pop `y`, pop `x` (max-heap gives `x ≤ y` free), push `y-x` if `x != y`; guard `empty()` → 0. Contrast **1049** = you pick the order ⇒ subset-sum DP, greedy wrong |
| ✅ | Jul 28 | 621 | [Task Scheduler](https://leetcode.com/problems/task-scheduler/) | 🟡 | "cooldown of n between identical tasks, minimise total time" → **greedy math, not a heap**: the most frequent task is the skeleton, `slots = (maxFreq-1)*(n+1) + maxCount`, answer = `max(slots, tasks.size())` (gaps can overflow ⇒ zero idles) O(N)/O(1); heap+cooling-queue simulation is the general version that yields the actual schedule |
| ✅ | Jul 29 | 1167| [Minimum Cost to Connect Sticks](https://leetcode.com/problems/minimum-cost-to-connect-sticks/) | 🟡 | "merge two items at a cost = their sum, repeat until one remains, minimise total cost" → **Huffman**: mirror of 1046, pop the two **smallest** from a min-heap, `cost += a+b`, **push the sum back**; cost = Σ(stick × depth) ⇒ smallest go deepest ⇒ greedy optimal. `while(size() > 1)` handles `n==1` for free O(n log n)/O(n); heap-free alt = sort + two queues (merged sums arrive sorted) |
| ✅ | Jul 30 | 378 | [Kth Smallest Element in a Sorted Matrix](https://leetcode.com/problems/kth-smallest-element-in-a-sorted-matrix/) | 🟡 | "kth smallest in a matrix with rows **and** columns sorted, memory better than O(n²)" → the memory clause **outlaws the size-K heap** (`k ≤ n²`). Canonical = **k-way merge**: min-heap of row heads `{val,r,c}`, pop k times, push the **right** neighbour → O(k log n)/**O(n)**. Optimal = **binary search the VALUE** + **staircase count** from the top-right (`c` never resets ⇒ O(n)/count) → O(n log range)/**O(1)**; `lo+(hi-lo)/2` (±1e9 overflow), `hi=mid`, converged `lo` is always a real element |
| ✅ | Jul 30 | 295 | [Find Median from Data Stream](https://leetcode.com/problems/find-median-from-data-stream/) | 🔴 | "median of everything **so far** on a stream / running median" → **two heaps**: `left` max-heap (small half) + `right` min-heap (large half), invariants `max(left) ≤ min(right)` and `\|left\| ∈ {\|right\|, \|right\|+1}` so the odd median is always `left.top()` (and `left` empty ⇒ `right` empty, so `top()` is safe). `addNum` = 3 unconditional ops (push left → move `left.top()` to right → move back if `right` grew); `findMedian` = `/2.0` mean of the tops when even. **add O(log n), median O(1)**, O(n). Brute = sort per query → TLE. **Follow-up [0,100]** → `cnt[101]` + prefix walk = O(1)/O(1) space; **99% in range** → buckets + two outlier heaps. No `erase` on a `priority_queue` ⇒ **LC 480 needs two `multiset`s** |
| ✅ | Jul 31 | 480 | [Sliding Window Median](https://leetcode.com/problems/sliding-window-median/) | 🔴 | "median of **every window of size k**" (sliding window ⇒ deletion, median ⇒ two halves) → **295 + lazy deletion**, because `priority_queue` has no `erase` — mark the expiring value in `delayed{value→count}`, decrement **your own `smallSize`/`largeSize`** (never `.size()`, it holds garbage), pop it only when it **surfaces to a top**. `erase` routes **by value** (`num <= small.top()` ⇒ it lived in `small`, valid since every small ≤ every large); **prune after every pop AND immediately when the erased value equals the top** — that keeps pending marks strictly `v < small.top() <= large.top()`, which is the only thing stopping duplicates from corrupting the sizes. **Insert before erase** in the driver. **O(n log n)/O(n)** (garbage ⇒ **not** O(k)). Alt-optimal = `multiset` + mid iterator, **O(n log k)/O(k)**, but `erase(value)` kills all duplicates. Trap: **`(double)a + (double)b`** — values reach `2^31-1` so the **sum** overflows before the divide. Contrast **LC 239** = window *extreme* → monotonic deque |

---

# 12. Backtracking

**When to use it (recognition cues):**
- Generate **all** subsets / permutations / combinations / paths.
- Constraint satisfaction (N-Queens, Sudoku, word search).
- "Return all possible…" — exponential output.

**Core idea:** Recursively build a partial solution; on dead-end, undo the last choice (backtrack). Skeleton: choose → explore → unchoose.

**Template (C++):**
```cpp
void bt(vector<int>& cur, int start) {
    res.push_back(cur);
    for (int i = start; i < n; ++i) {
        cur.push_back(nums[i]);
        bt(cur, i + 1);
        cur.pop_back();
    }
}
```

**Problems:**

| ✓ | Date | # | Problem | Diff | Trigger phrase |
|---|------|---|---------|------|----------------|
| ✅ | Aug 3 | 78  | [Subsets](https://leetcode.com/problems/subsets/) | 🟡 | "return **all** subsets / the **power set**, elements unique, any order" → output is 2ⁿ ⇒ enumerate. **Loop-from-`start` template** (record at **every** node, `push → helper(i+1) → pop_back`) — prefer it over pick/not-pick because Subsets II / Combination Sum / Permutations are one-line deltas from it. `i + 1` **not** `start + 1`. Unique input ⇒ **no dedup**; `[]` is a valid answer. **Bitmask alt:** `mask ∈ [0, 2ⁿ)` *is* a subset (bit `i` ⇒ take `nums[i]`) — same O(2ⁿ·n) but **O(1) auxiliary**, no call stack (`1 << n` overflows at n ≥ 31). Cascading alt: seed `{{}}`, append `x` to every existing subset — snapshot `size()` before growing. Traps: missing `pop_back()`; recording only at leaves; can't beat O(2ⁿ) — only the *space* is optimisable |
| ✅ | Aug 3 | 90  | [Subsets II](https://leetcode.com/problems/subsets-ii/) | 🟡 | "all subsets but input **may contain duplicates**, no duplicate subsets out" → **78 + one guard line**, and that line is the pattern's universal dedup rule: **`sort` first** (copies must be adjacent — input can be `[4,4,4,1,4]`), then `if (i > start && nums[i] == nums[i-1]) continue;`. **`i > start` not `i > 0`** — the first copy of a run always survives, only equal **siblings** are skipped; `i > 0` kills `[2,2]`. **Pick/not-pick shape needs a different rule**: declining an element declines its **whole run** (`while (nums[j] == nums[j-1]) j++`) — `continue` there won't even compile. Output = **Π(cnt+1)** not 2ⁿ (`[1,2,2]` → 6). Alt = freq-map cascading (0..c copies per distinct value, never creates a dup, O(1) aux); brute = 78 + `set<vector<int>>` |
| ✅ | Aug 4 | 46  | [Permutations](https://leetcode.com/problems/permutations/) | 🟡 | "return **all permutations / orderings / arrangements** of **distinct** elements" → order matters ⇒ **drop the `start` index, loop `i` from 0** (that fork *is* the pattern) and **record only at the leaf** (`temp.size() == n`). n·(n−1)·… = **n! leaves**. Shape **(a) `used[]`**: `if (used[i]) continue;` → mark + push → recurse → **un-choose both**; shape **(b) swap in place**: `swap(nums[index], nums[i])` → `helper(index+1)` → **swap back**, `nums` itself is the answer at `index == n` ⇒ no `temp`/`used`, O(1) auxiliary. Prefer (a) — only it upgrades to 47 via `!used[i-1]`; the swap version needs a per-level `set` (swapping kills sortedness) and its output is **not lexicographic**. Traps: looping from `start` (collapses to Subsets ⇒ 1 answer); recording at every node; forgetting the restore-swap / half un-choosing; quoting O(n!) instead of **O(n!·n)**. Alts: insert-into-all-gaps (iterative, O(1) aux) · `next_permutation` loop (lexicographic, dups free) · Heap's algorithm. Distinct input ⇒ **no dedup guard** |
| ✅ | Aug 4 | 47  | [Permutations II](https://leetcode.com/problems/permutations-ii/) | 🟡 | "all **unique** permutations, input **may contain duplicates**" → 46 + a dedup guard, but **the guard is template-specific** — that's the whole lesson. **`used[]` shape:** `sort` + `if (i > 0 && nums[i] == nums[i-1] && !used[i-1]) continue;` (equal copies consumed **left-to-right**; `used[i-1]` without `!` is also correct, just prunes deeper). **Swap shape:** the adjacency guard **breaks** — `swap` drops the old `nums[index]` back into position `i`, possibly *between* two equal values, so `nums[i]==nums[i-1]` checks the wrong neighbour ⇒ use a **per-level `unordered_set`**, `if (!seen.insert(nums[i]).second) continue;` (keys on **values** so no sort needed; O(n²) aux; `seen` **must** be per-frame or you lose permutations). Brute-verified trap: the two-guard patch gives **18 vs 12** on `[1,2,3,2]` and fails 2700/25377 inputs even sorted — never test a guard on `[1,1,2]`. Count = **n!/Π(cnt!)**. Alts: freq-map (loop distinct values, dedup by construction, branching = d not n) · `sort`+`next_permutation` (O(1) aux, dups free) · brute = 46 + `set<vector<int>>` |
| ✅ | Aug 5 | 39  | [Combination Sum](https://leetcode.com/problems/combination-sum/) | 🟡 | "all combinations summing to **target**, each number reusable an **unlimited** number of times, candidates **distinct**" → **78's loop-from-`start` template with two deltas**: recurse with **`i` not `i + 1`** (that one character *is* unlimited reuse — `i + 1` silently solves the no-reuse problem, `start` re-emits permutations), and record at the **`remain == 0`** base case instead of every node. **No dedup needed at all** — distinct input + forward-only index makes uniqueness **structural** (the `start` index is a **canonical-order device**, not "what's left"); the duplicates live *inside* an answer, not *across* answers, so reaching for a `set` signals you don't see the template. `sort` + **`if (c[i] > remain) break;`** = the prune. **`c[i] ≥ 2` is the termination proof** (a `0` ⇒ `helper(i, remain)` loops forever). Pick/not-pick shape also works, but check `remain == 0` **before** `index >= n` and pass `remain` down instead of a member `sum`. **O(N^(T/M))** / **O(T/M)** — depth is `target/min`, so *not* O(2ⁿ). Ask "**how many**" or "**fewest**" instead ⇒ stop enumerating, it's unbounded knapsack DP (`dp[t] += dp[t-c]`, O(N·T); `for c: for t` counts combinations, `for t: for c` counts permutations = LC 377) |
| ✅ | Aug 6 | 40  | [Combination Sum II](https://leetcode.com/problems/combination-sum-ii/) | 🟡 | "all **unique** combinations summing to **target**, each number used **only once**, input **may contain duplicates**" → **39's recursion fused with 90's dedup guard**: recurse with **`i + 1`** (each position consumed once) and add **`sort`** + **`if (i > start && c[i] == c[i-1]) continue;`**. `sort` is **mandatory** here, unlike 39 — the guard compares neighbours and the input can be `[2,5,2,1,2]`. **`i > start` NOT `i > 0`**, else `[1,1,6]` never forms (duplicates *inside* an answer are legal, duplicate *answers* aren't). **The `set<vector<int>>` post-filter TLEs, and that's the trap the constraints are built around** (`n ≤ 100`, `target ≤ 30`): a `set` dedups the **output** but never prunes the **tree** — on `[1]×100, t=30` there is exactly **one** answer, reached **C(100,30) ≈ 3×10²⁵** ways. Dedup must happen **during** the search, not after it. `continue` for the equal-sibling skip (a later, larger value may still fit) vs **`break`** for `c[i] > remain` (nothing after can) — swapping them silently loses answers. Pick/not-pick shape works too, but its not-pick branch must decline the value's **whole run** (`while (nums[next] == nums[index]) ++next;`) — three fiddly lines vs one, which is why the loop form is the interview answer. **O(2ⁿ · k)** / **O(n)**. Alt worth naming: **frequency map** (pick `j ∈ [0, count]` copies of each distinct value ⇒ dedup by construction, branching = distinct count). Ask "**how many**" instead ⇒ 0/1 knapsack DP, `for item: for t descending: dp[t] += dp[t-c]`, O(n·T) |
| ✅ | Aug 6 | 17  | [Letter Combinations of a Phone Number](https://leetcode.com/problems/letter-combinations-of-a-phone-number/) | 🟡 | "return **all strings** the digits could spell on a phone keypad, any order" → **fixed-depth Cartesian product**: one recursion level per digit, loop that digit's letters, record **only at the leaf** (`index == n`) — recording at every node would emit prefixes (`"a"`, `"ad"` for `"23"`). **No `start` index and no dedup guard**: different digits map to **disjoint** letter sets, so two paths can never collide — uniqueness is structural for a *different* reason than 39's forward-only index, and reaching for 40/90's `sort`+skip reflex here is pure noise. **The trap is the constraint floor — `0 ≤ digits.length`, so `"" → []` and NOT `[""]`**; guard in the **caller**, never inside `helper` (the buffer is legitimately empty at the root of every call). Read each group's length from the string: **`7 → pqrs` and `9 → wxyz` have 4 letters**, not 3. Pass the path by **reference** with `push_back`/`pop_back` — `s + c` by value costs O(n) time *and* O(n) space **per node** (O(4ⁿ·n) allocations vs O(4ⁿ)), the classic "works but doesn't actually backtrack" tell. **O(4ⁿ · n)** / **O(n)**. Alt: **iterative BFS cascade** — seed `{""}`, then per digit replace the frontier with `frontier × letters`; same complexity, **no call stack**, but seeding `{""}` reproduces the `[""]` bug unless guarded. Ask "**how many**" instead ⇒ one-line product `Π len(group(d))`, O(n) |
| ✅ | Aug 7 | 79  | [Word Search](https://leetcode.com/problems/word-search/) | 🟡 | "does **word** exist in the **grid**, cells **4-directionally adjacent**, **same cell may not be reused**" → **the folder's first constraint-satisfaction problem, and it breaks every habit of the pattern**: no `start` index, no dedup guard, nothing recorded — the output is a single `bool`, so an early `true` propagates straight up. Try **every cell as an origin** and enter the DFS at **`idx = 1`** (the origin already matched `word[0]`); `helper(i, j, 0)` silently matches the wrong strings. **The grid is a graph, not a tree** — a cell is reachable from several paths — so the **visited marking is what makes the search terminate** (`A→B→A→B…` otherwise) and the **un-mark on the failing path is what makes it a tree**: reuse is forbidden **within one path**, not globally. **Forgetting the restore is the #1 bug** — the board accumulates phantom obstacles and later start cells report **false negatives** (`"ABCCED"` passes while `"SEE"` mysteriously fails). Put the base case **`idx == word.size()` ABOVE the marking** so **`word.length == 1`** succeeds without taking a step. **Canonical space win: mark in place with `'#'`** rather than a `visited` matrix — `'#'` is never a letter, so `board[nr][nc] == word[idx]` already excludes on-path cells ⇒ guard loses a clause and auxiliary space drops to **O(L)**; stash the old char in a local and restore *that*. **Complexity is O(m·n·3^L), not 4^L** — after the first step you never step back onto the parent. **Follow-up ("pruning for a larger board") = two independent wins:** (a) **frequency pre-check** — if any letter appears more often in `word` than in `board`, return `false` in O(m·n + L) with zero recursion; (b) **search the rarer end first** — the tree size is dominated by how many cells match the **first** character, so **reverse `word`** when `word.back()` is scarcer than `word.front()`. Ask for a **list** of words instead ⇒ stop re-running `exist` per word, it's **trie + this exact DFS** (LC 212). Other traps: 8-direction `dr/dc` copy-pasted from a flood-fill; sharing one `visited` across starts |
| ✅ | Aug 11 | 131 | [Palindrome Partitioning](https://leetcode.com/problems/palindrome-partitioning/) | 🟡 | "**partition** the string so that **every** substring is a palindrome, return **all** partitionings" → **the partition shape: the choice at each node is WHERE THE CURRENT PIECE ENDS, not which element to take.** A partition of `n` characters *is* a **subset of the `n − 1` gaps**, so this is 78's skeleton with the palindrome test as the **prune**. `helper(index)` = "everything before `index` is already cut into valid palindromes; partition the suffix" ⇒ loop the **end** `i` from `index`, keep the branch only `if (isPal(index, i))`, push `s.substr(index, i - index + 1)`, recurse at **`i + 1`**, `pop_back`. **`i + 1`, not `index + 1`** (that only ever emits the all-singletons partition — 1 answer instead of `2^(n-1)`) and **not `i`** (infinite recursion; contrast 39, where `i` is deliberately correct because reuse is allowed). Record **only at `index == n`** — an internal node holds a *partial* partition, so recording there emits garbage like `[["a"]]` for `"aab"`. **There is no dedup guard and none is needed**: each answer is uniquely determined by its cut-set and the loop enumerates each cut-set exactly once — `["a","a"]` and `["aa"]` are **different** answers, so the 90/40 sort+skip reflex is pure noise here (and `sort` would destroy the string). **The result is never empty** — every single character is a palindrome, so the all-singletons cut always survives; the **worst case is `"aaaa…a"`**, where *all* `2^(n-1)` cut-sets are valid, which is exactly why the constraint is `n ≤ 16` (`2¹⁵ = 32 768`). **The follow-up is making the prune O(1): precompute `pal[i][j] = (s[i] == s[j]) && (j - i < 2 \|\| pal[i+1][j-1])` with `i` descending** (or expand-around-centre) — O(n²) time and space, and it turns the on-the-fly two-pointer check from O(n) into O(1) ⇒ **O(n² · 2ⁿ) collapses to O(n · 2ⁿ)** / O(n²). Memoising the partitions per suffix does **not** help — the output is already exponential and the cache blows up memory. Traps: `substr` length is **`i - index + 1`**, not `i - index`; palindrome-testing the whole prefix or the accumulated `path` instead of the **new piece**; quoting a flat O(2ⁿ) (each answer costs O(n) to copy). Ask for the **fewest cuts** instead ⇒ stop enumerating — that's **LC 132**, pure DP `cut[i] = min(cut[j-1] + 1)` over palindromic `s[j..i]`, O(n²) and fine to `n = 2000`. Swap the palindrome test for a dictionary lookup ⇒ **Word Break II (LC 140)**; fix the piece count at 4 with a numeric validity test ⇒ **Restore IP Addresses (LC 93)** |
| ✅ | Aug 11 | 93  | [Restore IP Addresses](https://leetcode.com/problems/restore-ip-addresses/) | 🟡 | "insert dots into a digit string so it becomes a **valid IP** — exactly **4** parts, each **0–255**, **no leading zeros**, return **all** ways" → **131's cut-loop with two hard bounds bolted on: every piece is 1–3 chars and there are exactly 4 pieces**, so the entire search tree is `3³ = 27` leaves — constant, not exponential. `helper(index, seg)` = "`s[0..index)` is already `seg` valid octets; finish the suffix with the remaining `4 − seg`"; loop the piece **length `1..3`**, keep it only if it's a legal octet, recurse at **`index + len`**, and undo with **`ip.resize(keep)`** — one call that drops the piece **and** its dot. **The fourth octet is FORCED, not enumerated:** the base case at `seg == 3` takes the whole remainder, which is what lets you append `piece + '.'` freely without ever stripping a trailing dot. That same shortcut creates the **#1 wrong submission**: the forced tail still needs the **full** validity check — length ≤ 3, no leading zero, ≤ 255 — otherwise `"255255111135"` happily emits `"2.5.5.255111135"`. **Validity, in this exact order:** size 1..3 → **leading zero (`len > 1 && s[start] == '0'` — `"0"` is valid, `"00"` and `"01"` are not; the naive `s[start] == '0'` kills `"0.0.0.0"`)** → **value ≤ 255** (only a 3-digit piece can exceed it, so compare digits arithmetically and skip `stoi`, which overflows if the size check ever drifts below it). **Prune every frame** with `left = n − index`, `slots = 4 − seg`, `if (left < slots || left > 3 * slots) return;` — it kills dead branches at depth 1 and subsumes the `n < 4 || n > 12` guard. Note the constraint is **`n ≤ 20`**, so the problem *deliberately* hands you unsolvable inputs and **`[]` is a common correct answer** — the exact opposite of 131, whose result is never empty. **No dedup guard exists or is needed** (one answer per cut-set; `sort` would destroy the string). Because the depth is a **constant 4**, a **triple nested loop over lengths 1..3** is a legitimate iterative twin — unrolling is illegal in 131, where depth is `n`; say that out loud, then write the backtracking anyway since it's what survives the follow-up. Brute = choose 3 cut positions out of `n − 1` gaps and validate all four parts, O(n⁴) / O(1) — passes at `n ≤ 12`, but does **zero** early rejection and re-tests a bad first octet inside every remaining combination. DP/memoisation buys nothing (no overlapping subproblem at depth 4). Traps: recursing at `index + 1`; forgetting the dot in the undo; enumerating all four segments and leaving `"1.0.10.23."`; recording at an internal node. The real escalation isn't a count variant — it's **IPv6 (8 groups, 1–4 hex chars, `::` compression) ⇒ Validate IP Address, LC 468**: same skeleton, far richer predicate |
| ✅ | Aug 11 | 51  | [N-Queens](https://leetcode.com/problems/n-queens/) | 🔴 | "place `n` queens on an `n × n` board so that **no two attack each other**, return **all distinct** configurations" → **37's constraint-satisfaction machinery, but this one COLLECTS**: the helper returns `void`, records at `row == n`, and has **no early exit** — returning `bool` here quietly hands back 1 board instead of 92. **Two economies carry the whole solution, and interviewers probe both. (a) One queen per row is STRUCTURAL** — it's encoded in the recursion signature (`helper(row)`, loop over **columns**), so same-row boards are never even generated, you never need a "row" check, and the search collapses from `C(n², n)` placements to a **permutation of columns, ≤ n!**. **(b) `safe()` must scan UPWARD ONLY** (`i--` in all three loops) — rows below `row` are empty by construction and the current row is impossible by (a), so scanning down or sideways is pure waste *and* tempts you into a bogus row test. The three checks you do need are **column, ↖ diagonal, ↗ diagonal** — and each has a **constant key**, which is the expected O(1) follow-up: `cols[col]`, **`anti[row + col]`** (↗, since the sum is invariant along it) and **`diag[row - col + n - 1]`** (↘, the `+ n − 1` only shifts the range non-negative — size these arrays **`2n − 1`**, not `n`, or distinct diagonals collide). **The trap that refactor introduces: the undo must clear all three flags *and* `board[row][col]`** — one leaked flag is a phantom attack line that silently drops valid boards, the same class of bug as the mask undo in 37. The elegant variant worth naming is **bitmasks**: `avail = ~(cols | d1 | d2) & ((1 << n) - 1)`, take `bit = avail & -avail`, recurse with `(d1 | bit) << 1, (d2 | bit) >> 1` — **the shifts *are* the diagonal propagation** (a diagonal threat moves one column per row), so there's no index arithmetic and no explicit un-choose at all (state is passed by value), O(1) auxiliary. Further: carry `pos[row]` (column per row) and materialise the strings only at the leaf, which makes the board **output-only** and drops auxiliary space to O(n). **`n = 2` and `n = 3` legitimately return `[]`** — the only empty-result cases; counts run `1, 0, 0, 2, 10, 4, 40, 92, 352` for `n = 1..9` with **no closed form**, which is precisely why enumeration is the intended answer. Brute force = all `C(n², n)` placements (≈ 4.4 × 10⁹ at n = 8) validated one by one — fixed by the single "two queens can't share a row" observation; the intermediate rung is **all `n!` column permutations then check diagonals**, same exponent but **zero pruning** (a conflict between rows 0 and 1 still enumerates every one of the `(n−2)!` completions beneath it, which the backtracking kills at depth 2). Mirror symmetry (row 0 restricted to `[0, n/2)`, reflect) halves the work — **say it, don't write it**: fiddly for odd `n` and it scrambles output order. Ask for the **count** instead ⇒ **LC 52 N-Queens II**, identical search with no board at all, O(1) space with bitmasks; ask for **one** solution ⇒ the early exit comes back and the helper returns `bool`, i.e. it becomes 37's shape. Traps: checking only one diagonal (passes `n = 1`, breaks at `n = 4`); forgetting the `+ n − 1` shift (out-of-bounds, not a wrong answer, so it may "work" in debug); recording at every node; quoting a flat O(n!) when each solution costs O(n²) to build as strings |
| ✅ | Aug 11 | 37  | [Sudoku Solver](https://leetcode.com/problems/sudoku-solver/) | 🔴 | "**fill the empty cells** so every row, column and 3×3 box contains 1–9 exactly once; the board is modified **in place** and the function returns `void`" → **constraint satisfaction, solve-and-stop: nothing is ever collected, the helper returns `bool`, and the mutated board *is* the answer** — the same shape as 79, except the constraint is **global** (row/col/box) rather than path-local. Walk to the first `'.'`, try `'1'..'9'`, keep the digits that don't already appear in that row / column / box (origin **`(row/3)*3, (col/3)*3`**), then `if (helper()) return true;` else reset the cell to `'.'` and try the next. **The load-bearing line is `return false`, and it must sit INSIDE the "this cell is empty" branch, immediately after the candidate loop** — it means *"I found an empty cell and nothing legal fits ⇒ this partial board is dead"*, which is the signal that makes the caller undo its own guess. Hoist it one level out, past the scan loops, and the search quietly walks on to the *next* empty cell and can **`return true` on a board that still has holes in it** — the #1 wrong submission. **Mirror detail: on the success path you return `true` WITHOUT undoing** — the filled digits are the output, so backtracking only ever happens on the failing branch (same early-exit-without-restore as 79). **Making `helper` return `void` is fatal**: with no success/failure signal you can't stop, and you can't undo correctly — the `bool` return *is* the algorithm. **The expected follow-up is turning the O(27) `safe()` scan into an O(1) test: three `int` bitmasks `rows[9] / cols[9] / boxes[9]`** (box index **`(i/3)*3 + j/3`**, *not* `(i/3)+(j/3)`, which collides boxes), prefilled from the givens, checked as `(rows[i] | cols[j] | boxes[b]) & bit`. The trap it introduces: **the undo must reset all three masks *and* `board[i][j]`** — a single leaked bit is a phantom constraint that makes a solvable board report unsolvable. Pushed further ⇒ **MRV (most-constrained-variable)**: instead of the *first* empty cell, pick the one with the fewest candidates (`__builtin_popcount(~used & 0x1FF)`) — a cell with one candidate has branching factor **1**, it's forced rather than guessed, and that's what separates "solves easy boards" from "solves adversarial boards instantly". Beyond that, name but don't write: **constraint propagation** (naked/hidden singles — Norvig's version solves most published puzzles with *no search at all*) and **Dancing Links / Algorithm X** exact cover (729×324 matrix; the theoretically right answer, far too long to code live). The **single-solution guarantee** is what licenses stopping at the first `true`. Brute force = fill all `m` blanks every possible way then validate, **O(9^m · 81)** — identical exponent with **zero** pruning, so a conflict in the first two cells still enumerates 9^(m−2) completions beneath it. Ask "**is this board valid**" instead ⇒ stop searching entirely: that's **LC 36**, one O(81) pass over the same three mask families, no recursion. Don't quote O(81!) or O(9⁸¹) — it's **O(9^m)** over the *empty* cells, and say out loud that pruning makes the bound bear no relation to the real runtime |

---

# 13. Greedy & Intervals

**When to use it (recognition cues):**
- Intervals: merge / insert / overlap / meeting rooms.
- "Minimum number of …" or "maximum number of …" with sortable input.
- A locally optimal pick is globally optimal (often after sorting by start/end).

**Core idea:** Sort first, then make the locally best choice.

**Template (C++):**
```cpp
sort(intervals.begin(), intervals.end());
vector<vector<int>> merged;
for (auto& iv : intervals) {
    if (merged.empty() || merged.back()[1] < iv[0]) merged.push_back(iv);
    else merged.back()[1] = max(merged.back()[1], iv[1]);
}
```

**Problems:**

| ✓ | Date | # | Problem | Diff | Trigger phrase |
|---|------|---|---------|------|----------------|
| ✅ | Aug 15 | 56  | [Merge Intervals](https://leetcode.com/problems/merge-intervals/) | 🟡 | "**merge all overlapping intervals**" / "return the intervals that **cover** all the input" (the output is a **set of intervals**, overlap is **pairwise** so the naive read is O(n²), and reordering the input is allowed) → **the ROOT of the pattern — every other interval problem is this loop with the operator or the sort key changed**: `sort()` (default lexicographic on `vector<int>` = by **start**, tie-broken by end — exactly the key needed), then one pass `if (result.empty() \|\| result.back()[1] < it[0]) result.push_back(it); else result.back()[1] = max(result.back()[1], it[1]);`. **Why start-sort collapses O(n²) → O(n):** after sorting every later interval starts at or after `it[0]`, so if the current one doesn't touch the running `end` **no future one can either** — the gap is permanent ⇒ a single running `end` replaces comparing against all kept intervals. **`end = max(end, it[1])`, NEVER `end = it[1]`** — *the* signature bug: start-order ≠ end-order, so a **contained** interval shrinks the run (`[[1,9],[2,3]]` → `[[1,3]]` instead of `[[1,9]]`). **`end < it[0]` strict** — touching **merges** here (`[[1,4],[4,5]]` → `[[1,5]]`), the **opposite** convention from 252/435 and from 452's `>`. In the explicit `start`/`end` form the **trailing `push_back` is mandatory** (a run is only closed by finding the *next* gap ⇒ `[[1,3]]` → `[]` without it); the `result.back()` form needs neither the flush nor the `intervals[0]` seed. Watch: **not sorting at all** (inputs are deliberately shuffled — `[[4,7],[1,4]]` is example 3 — and it's a **wrong answer, not a crash**); a subtracting comparator (`a[0]-b[0]`) is signed-overflow UB by habit; `sort` **mutates the caller's array** (say so). Ladder: brute "merge any overlapping pair, repeat until stable" O(n³) → **sort+sweep O(n log n)/O(1)** → **bucket/sweep-line O(n+V)**, which *beats* the sort since `0 ≤ s ≤ e ≤ 10⁴` (the answer to "can you do better than O(n log n)?" — dies on unbounded/fractional coords) → **input already sorted ⇒ O(n)/O(1)** (the common follow-up). Streaming arrivals ⇒ `std::map`, O(log n)/insert = **352 / 715**; one new interval = **57 Insert Interval**; intersection twin = **986** |
| ✅ | Aug 15 | 57  | [Insert Interval](https://leetcode.com/problems/insert-interval/) | 🟡 | "insert `newInterval` into a **sorted, non-overlapping** list, **merging if necessary**" (the input is *already* sorted **and** already merged ⇒ **any sort you write is wasted work**; only **one** intruder ⇒ only **one** merge region) → **56 with the sort already done for you ⇒ O(n), not O(n log n)**: sorted + disjoint means the array splits into **three contiguous zones**, emitted by three sequential loops sharing one index `i` that **never resets** — (1) `while (i<n && intervals[i][1] < lo) push(intervals[i++]);` (2) `while (i<n && intervals[i][0] <= hi) { lo = min(lo, intervals[i][0]); hi = max(hi, intervals[i][1]); i++; }` (3) `push({lo,hi});` then `while (i<n) push(intervals[i++]);`. **Why three flat loops are legal — the overlapping zone is CONTIGUOUS**: sorted *and* mutually disjoint ⇒ everything the intruder touches is one unbroken run, never scattered ⇒ no index is revisited ⇒ O(n) (nesting or resetting `i` means you misread the guarantee). **The two boundary operators are opposites on purpose** — zone 1 **strict** `<`, zone 2 **non-strict** `<=` — that pair encodes "*share at least one point* ⇒ overlapping", so an interval ending exactly where `newInterval` starts is **absorbed, not skipped**; flip either and `[[1,3],[6,9]], [3,6]` → `[[1,3],[3,6],[6,9]]` instead of `[[1,9]]`. **The push MUST live outside loop 2** — inside it emits one copy per absorbed interval and **drops `newInterval` entirely** when the loop never runs; outside it fires exactly once, which is why **`n == 0` needs no special case**. **`0 ≤ intervals.length` — the list CAN BE EMPTY** (unlike 56's `n ≥ 1`), so anything seeding from `intervals[0]` is UB. **Keep the `min`** — `newInterval` can start *after* an interval it overlaps (`[[1,5]], [2,3]` → `[[1,5]]`); it can only ever fire on the *first* absorbed interval since starts are sorted (the "tighten it" answer). Don't mutate the caller's `newInterval` — copy to `lo`/`hi`. Ladder: **append + sort + reuse 56's sweep O(n log n)** — say it first, then note it *throws away the sorted-input gift*, which is exactly what's being tested → **three-zone sweep O(n)/O(1)** → binary-search the two boundaries (**O(log n) to locate, still O(n) to build** — no asymptotic win). Repeated inserts ⇒ `std::map`, O(log n)/op = **715 / 352**; drop the non-overlapping guarantee ⇒ straight back to **56**. **If you write `sort()` in 57, you've solved 56 by accident** |
| ✅ | Aug 12 | 252 | [Meeting Rooms](https://leetcode.com/problems/meeting-rooms/) | 🟢 | "can **one** person / **one** room attend **all** these intervals?" (bool answer, resource count fixed at 1) → **the seed of the pattern: sort by start, then check only ADJACENT pairs.** Adjacency suffices because after sorting `arr[j][0] >= arr[i+1][0] >= arr[i][1]` for any `j > i+1`, so far pairs are automatically safe ⇒ the O(n²) all-pairs check collapses to one linear scan. Loop `i = 1..n-1`, `if (arr[i][0] < arr[i-1][1]) return false;`, else `true`. **`<` not `<=`** — the spec says "start **≥** previous end is attendable", so intervals are **half-open** `[s,e)` and `[1,4],[4,7]` is legal; a problem that calls shared endpoints a conflict flips it to `<=`. `n == 1` → `true` for free (the loop never runs). Watch: **not sorting** at all (both sample inputs are deliberately shuffled); `i < arr.size()-1` is an **unsigned** expression — safe only because `n ≥ 1`, prefer `size_t i = 1` + `arr[i-1]`; sorting by *end* also works for this bool but is the wrong habit to build; it **mutates the caller's array** (worth saying out loud). Brute = all pairs `max(s)<min(e)`, O(n²), TLEs at n=10⁵. Alt = **sweep line** (`+1` on a start, `-1` on an end, ties process `-1` first, fail if the counter exceeds 1) — overkill here but it's the general machine. **Ask "how many rooms" instead ⇒ 253**: a *pairwise* question becomes a **peak-concurrency** question ⇒ min-heap of end times (pop while `top() <= start`, answer = max heap size) or a two-array chronological sweep |
| ✅ | Aug 12 | 253 | [Meeting Rooms II](https://leetcode.com/problems/meeting-rooms-ii/) | 🟡 | "**minimum number of rooms / platforms / machines**" or "**maximum number of overlapping intervals**" → the answer is a **count**, so 252's adjacent-pair bool scan can't produce it: **it's PEAK CONCURRENCY** — at the busiest instant every live meeting needs its own room, and that many rooms provably suffice (a room freed at `t` is reusable at `t`). **Zip `start[i]` with `end[i]` BEFORE sorting** — `sort(start...)` alone destroys the pairing — then sort by start and keep a **min-heap of end times = rooms currently in use**; the min is the room freeing soonest and the only one worth reusing (exchange argument ⇒ one `top()` check replaces scanning all rooms). Per meeting: `if (!pq.empty() && pq.top() <= s) pq.pop();` then **`pq.push(e)` unconditionally**; `return pq.size()`. **Why the bare final size IS the maximum: at most 1 pop and exactly 1 push per iteration ⇒ the heap size is monotone non-decreasing** — that's why it's **`if`, not `while`**; a `while` pops several, breaks monotonicity, and then `return pq.size()` is a genuine **wrong answer** (you'd have to track `best = max(best, pq.size())`). **`<=` not `<`** — the note allows a meeting to start exactly when another ends ⇒ half-open `[s,e)`. **The #1 wrong submission: pre-seeding the heap with all of `end[]` before the loop** — every room is then occupied from time 0, the heap can only shrink, and the peak is never observed (`start=[10,22,11,4,11], end=[14,29,30,12,29]` → **3** instead of **4**, since 4 meetings are live at `t=11`). **Alt-optimal with O(1) space — two-pointer chronological sweep:** sort `start[]` and `end[]` **independently** (legal *only* here, because nothing is ever paired), then `while (i<n) start[i] < end[j] ? (cur++, best=max(best,cur), i++) : (cur--, j++)` — strict `<` is what implements the touching rule. Also worth naming: **event list** `(time, ±1)` sorted with **`-1` before `+1`** on ties (backwards ⇒ `[[1,4],[4,7]]` reports 2), and a **difference array** O(n+T)/O(T) since times ≤ 10⁶. Brute = for each start count how many intervals contain it, O(n²) — the peak always occurs at some start. Watch: default `priority_queue` is a **max**-heap (needs `greater<>` *and* the `vector<int>` middle arg, else `top()` is the latest-finishing room, nothing pops, answer = `n`); missing `!pq.empty()` → UB on the first meeting; answer is **≥ 1**, never 0. Same problem under other names: **Minimum Platforms**, Car Pooling (1094), My Calendar III (732), Maximum CPU Load. The trio: 252 = bool, 253 = count, **435 = which to drop ⇒ sort by END** |
| ✅ | Aug 13 | 435 | [Non-overlapping Intervals](https://leetcode.com/problems/non-overlapping-intervals/) | 🟡 | "**minimum number of intervals to REMOVE** so the rest don't overlap" (or its complement, "**maximum non-overlapping intervals you can KEEP**" — `removals = n − kept`) → unlike 252/253 you get to **choose which intervals survive**, and the moment you're choosing, the sort key flips from **start** to **END**: this is **Activity Selection / earliest-finish-first**. Sort by `a[1] < b[1]`, keep `end = INT_MIN` = the end of the last **kept** interval, then one pass: `if (it[0] >= end) end = it[1]; else numOfErase++;` → return `numOfErase`. **Why END, not start:** the only safe greedy choice is the interval that blocks the future least = the one with the **smallest end**; exchange argument — take any optimal solution, swap its first interval for the earliest finisher, and it's still valid and still the same size. A useful consequence: in end-sorted order every interval you meet has `e >= end`, so a removal needs **no** `min` update. **`>=` not `>`** — the statement says touching is explicitly non-overlapping, so `[[1,2],[2,3]]` → 0 (and this rule **flips** for 452). **`INT_MIN` not `0`** — coordinates run down to `−5·10⁴`, so a `0` sentinel wrongly rejects a first interval that ends negative. **The #1 wrong submission: sort by START and, on every overlap, unconditionally drop the CURRENT interval** — that's only optimal when the already-kept one finishes earlier; on `[[-52,31],[-73,-26],[82,97],[-65,-11],[-62,-49],[95,99],[58,95],[-31,49],[66,98],[-63,2],[30,47],[-40,-26]]` it returns **8** instead of **7**, because it clings to `[-73,-26]` and erases `[-62,-49]`, which ends earlier and would have left room for both `[-40,-26]` and `[-31,49]`. Start-sort **is** salvageable — add `taken[1] = min(taken[1], cur[1])` on every removal — equally O(n log n)/O(1) and equally correct, just one more moving part, so prefer the end-sort live. Alternatives worth naming: **DP longest non-overlapping chain** (LIS-shaped, `dp[i] = 1 + max(dp[j])` over `end[j] <= start[i]`), O(n²) or O(n log n) with binary search, answer `n − max(dp)` — over-engineering here but it's **the** tool once intervals carry **weights** (LC 1235 Weighted Interval Scheduling), where greedy genuinely breaks; brute force = enumerate subsets, O(2ⁿ). Watch: a bare `sort(intervals.begin(), intervals.end())` sorts by **start** silently rather than erroring; returning `kept` instead of `removed` (that's LC 646 Max Length of Pair Chain); counting overlapping **pairs** (three mutual overlaps = 3 pairs but only 2 removals); leaving a debug `cout` in the loop TLEs at `n = 10⁵`. Near-twin **452 Burst Balloons** — same greedy, but there touching **counts as overlap** (`s > end`) and it returns the groups **kept**. The trio: 252 = bool, 253 = count of rooms, **435 = which to drop ⇒ sort by END** |
| ✅ | Aug 13 | 452 | [Minimum Number of Arrows to Burst Balloons](https://leetcode.com/problems/minimum-number-of-arrows-to-burst-balloons/) | 🟡 | "**fewest arrows / points / shots that stab every interval**" — the **minimum piercing set**: how few positions do I need so that every interval contains one of them → same **earliest-finish-first** engine as 435, but you count the **groups that survive** instead of the intervals you delete. Sort by **END** (`a[1] < b[1]`), seed `arrows = 1, end = points[0][1]`, then one pass: `if (points[i][0] > end) { arrows++; end = points[i][1]; }` — otherwise the balloon is already burst and you **leave `end` alone** (in end-sorted order every `e >= end`, so the arrow already sits at the group's true minimum end and no `min` update is needed). **Why the arrow goes at the END:** within a group the arrow must lie in the intersection of all its balloons, and that intersection's **right edge is the smallest end** — anywhere to the left covers strictly fewer of the balloons still to come, anywhere to the right misses the earliest finisher entirely; the exchange argument closes it (slide any optimal solution's leftmost arrow right to that end and nothing is lost). **`>` not `>=`** — balloons are **closed** intervals `[s,e]`, so touching **counts as overlap** and a single arrow at `x = 2` bursts both `[1,2]` and `[2,3]`: `[[1,2],[2,3],[3,4],[4,5]] → 2`. That is the **exact opposite** of 435, where touching is *not* a conflict and the test is `>=` — one character, opposite semantics, and the single most quizzed difference between the twins. **The signature bug of 452: `return a[1] - b[1];` in the comparator** — coordinates span the whole int range (`−2³¹ … 2³¹−1`), so that subtraction **overflows**, which is signed-overflow **UB**, which scrambles the sort and returns a wrong answer on the adversarial test; always *compare* (`a[1] < b[1]`), never subtract. Other traps: seeding `arrows = 0`, or `end = INT_MIN`/`0` (coordinates are negative-capable, and an `INT_MIN` seed can leave the first balloon without its own arrow) — seed from `points[0][1]` with `arrows = 1`; a bare `sort(points.begin(), points.end())` silently sorts by **start**, which is salvageable as an equally-optimal alternative only if you add `end = min(end, points[i][1])` on every covered balloon; returning 0 for a valid input (the answer is always ≥ 1); an O(n²) pairwise grouping TLEs at `n = 10⁵`. Equivalence worth saying out loud: `arrows = n − removals(435 computed with the strict `>` rule)` — 452 literally counts 435's **survivors**. Brute force = try every endpoint as a shot position, O(n²), and "shoot where the most balloons burst" isn't even optimal. **Don't confuse with LC 312 Burst Balloons** — that is O(n³) interval **DP** on the order of popping, nothing to do with this greedy. The quartet: 252 = bool, 253 = count of rooms, 435 = which to drop, **452 = how few points stab them all ⇒ sort by END, `>` boundary** |
| ✅ | Aug 14 | 55  | [Jump Game](https://leetcode.com/problems/jump-game/) | 🟡 | "each element is the **max jump length** from that position — **can you reach the last index?**" (bool reachability along a line where every position advertises a range) → **carry one variable, `reach` = furthest index reachable so far**: `if (i > reach) return false;` else `reach = max(reach, i + nums[i])`, survive the sweep ⇒ true. **One int suffices because you may jump ANY length `1..nums[i]`, so the reachable set is always a contiguous prefix `[0, reach]` with no holes** — there is no choice to make, only a frontier to extend, and taking the longest jump dominates every alternative (that's what collapses the O(n²) DP into a scalar). **`i > reach`, not `>=`** (standing exactly on the frontier is legal). Failure ⟺ a **wall `k < n-1`** that no earlier index reaches past (`[3,2,1,0,4]`); **`nums[i] == 0` is not the test** — you jump *over* zeros (`[2,0,0]` → true, `[1,0,1]` → false). `n ≥ 1` ⇒ `[0]` is **true**. `nums[i] ≤ 10⁵ ≫ n`, so `i + nums[i]` may point past the array: safe to compare, must clamp to subscript. Backwards twin: shrink `goal` from `n-1`, `if (i + nums[i] >= goal) goal = i`, answer `goal == 0`. Ladder: brute O(2ⁿ) → memo/DP O(n²)/O(n) (AC's, but the inner `1..nums[i]` loop is waste — `[n-2,…,1,0,0]` ≈ 5·10⁷ calls) → **greedy O(n)/O(1)**. "How many jumps" ⇒ **45** (same frontier, layered); prefix property dies ⇒ **1306** BFS, **1696** deque-DP. Never sort — order *is* the problem |
| ✅ | Aug 14 | 45  | [Jump Game II](https://leetcode.com/problems/jump-game-ii/) | 🟡 | "**minimum number of jumps** to reach the last index" (reachability **guaranteed** ⇒ the answer is a count, not a bool — shortest moves on an unweighted line ⇒ **BFS**) → **take 55's frontier and cut it into LEVELS**: `jumps = curEnd = farthest = 0`, for `i = 0..n-2` do `farthest = max(farthest, i + nums[i])` **then** `if (i == curEnd) { jumps++; curEnd = farthest; }`. **It's BFS without a queue** — level `k` (everything reachable in exactly `k` jumps) is a **contiguous window** `[curEnd+1, farthest]` because any jump length `1..nums[i]` is legal, so two ints describe the whole queue and `jumps` is the BFS depth ⇒ first arrival is optimal by construction. **Loop to `n-2`, not `n-1`** — *the* signature bug: landing on the last index with `i == curEnd` counts a departure you never make (`[2,3,1,1,4]` → 3 instead of 2, `[0]` → 1 instead of 0); a jump is counted when you **leave** a level. **Never pick which index to jump to** — commit to the next *window* and let `farthest` name the best member; greedily hopping to the furthest index (`i += nums[i]`) gives **3** on `[2,3,1,1,4]` because the optimal first hop is the *nearer* index 1, which launches further. `[0]` → 0 for free. Guarantee dropped ⇒ detect `farthest == curEnd`, never `nums[i] == 0`. Ladder: brute O(2ⁿ) → backwards DP `dp[i] = 1 + min(dp[i+1..i+nums[i]])` O(n²)/O(n) → BFS+visited O(n)/O(n) → **greedy O(n)/O(1)**. Not Dijkstra — every edge costs 1. Same engine as **763**; generalises to **1024 / 1326**; breaks at **1306** (backwards ⇒ BFS) and **1696** (weights ⇒ deque DP) |
| ✅ | Aug 15 | 763 | [Partition Labels](https://leetcode.com/problems/partition-labels/) | 🟡 | "partition into **as many parts as possible** so that **each letter appears in at most one part**" (maximise the count ⇒ cut as early as legal; the parts must concatenate back to `s` ⇒ **can't sort**; lowercase-only ⇒ an O(1) 26-table) → **45's loop with `curEnd` renamed, where the frontier is a LAST-OCCURRENCE instead of `i + nums[i]`**: pass 1 `last[s[i]-'a'] = i` (last write wins), pass 2 `end = max(end, last[s[i]-'a'])` **then** `if (i == end) { res.push_back(i - start + 1); start = i + 1; }`. **The greedy is optimal because the cut is FORCED, not chosen** — letter `c` at `start` drags the part out to `last[c]`, transitively, so every index in `[start, end]` is provably stuck in one part ⇒ `end` is the **earliest legal** cut ⇒ earliest-legal-everywhere maximises the count. That's a **connected-components** argument, stronger than the usual exchange argument. **It's an interval merge in disguise** — each letter is `[first(c), last(c)]` and the parts are the merged groups; because the sweep visits indices in increasing order the intervals arrive **already sorted by start**, which is exactly why this is O(n) and why "sort it" is a wasted reflex. **The signature bug: cutting on `i == last[s[i]]`** instead of the running max — that ignores letters still open from earlier in the window (`"abab…"` would cut at the last `a` while `b` is live). **Loop to `n-1`, NOT `n-2`** — the *opposite* bound from 45, because here the final index must fire to close the last part: 45 counts **departures**, 763 counts **arrivals**. Watch `end - start + 1` (inclusive — the sizes must sum to `n`), `start = i + 1`, and `last` not `first`. **Alt certificate, equally optimal — count open vs closed letters:** `if (count[c]++ == 0) numOfChar++; if (count[c] == freq[c]) filled++; if (numOfChar == filled) cut;` (`numOfChar == filled` ⟺ `i == end`). Ladder: brute "verify every candidate cut" O(n²)/O(n³) (AC's at n ≤ 500) → explicit intervals + merge O(n)/O(1) — **the version to use if the alphabet is unbounded** (hash map, no 26-table) → **sweep O(n)/O(1)**. **Don't reach for DP** — "partition a string" screams DP, but the cut points are forced, so there's nothing to optimise over. Nearest variants **769 / 768 Max Chunks To Make Sorted** (same "window caught its own frontier" test, on prefix-max) |
| ✅ | Aug 16 | 134 | [Gas Station](https://leetcode.com/problems/gas-station/) | 🟡 | "**circular** route, start with an **empty tank**, return the **starting index** — guaranteed **unique** — or `-1`" (every stop has a **gain and a cost** ⇒ collapse the two arrays into one `diff[]`; the constraint is a running total that must stay ≥ 0 at **every** step, not just at the end; `n ≤ 10⁵` rules out simulating from each start) → **`diff[i] = gas[i] - cost[i]`, then the question is literally "find a rotation whose every prefix sum is ≥ 0"**: two accumulators, one pass — `total += diff` (**never reset**), `tank += diff`, and `if (tank < 0) { start = i + 1; tank = 0; }`, ending with `return total < 0 ? -1 : start;`. **Feasibility is decided by ONE number** — if `sum(gas) < sum(cost)` no start works (you'd be manufacturing fuel), and if `sum(gas) >= sum(cost)` a start is **guaranteed to exist**, so you never *search* for existence, you only *locate* the index; the constructive proof is `s = argmin(prefix sums) + 1` — start right after the deepest dip and the whole trough is behind you. **The skip lemma is what buys the O(n):** if starting at `s` the tank first goes negative on arriving at `f`, then **no index in `[s, f]` is a valid start** — coming from `s` you reached every intermediate `k` with `tank ≥ 0`, so starting *at* `k` instead means arriving at `f` **without** that non-negative carried-in surplus, i.e. with ≤ an already-negative value. The entire failed block dies at once, `start` only moves **forward**, and there is no restart. **You never simulate the wraparound** — only the suffix `[start, n-1]` is ever checked, because `total ≥ 0` plus "the suffix never dips" forces the wrapped prefix to stay non-negative too; the `total` test is doing double duty as **feasibility check** *and* **wrap-around certificate**. **The #1 wrong submission: returning `start` without the `total` guard** — `gas=[2], cost=[3]` leaves `start = 1`, an **out-of-range index**, and `gas=[2,3,4], cost=[3,4,3]` returns `2` instead of `-1`; `start == n` is safe *only* because that case provably forces `total < 0`, so know **why** rather than bolting on a defensive `% n`. Other traps: **resetting `total` alongside `tank`** (they have different scopes — one global, one per-candidate, and one shared accumulator can't answer both questions); **`tank <= 0`** instead of strict `< 0` (finishing a hop with exactly 0 fuel is legal — example 1 ends at exactly 0, so `<=` throws away valid starts); rejecting stations where `gas[i] < cost[i]` (a lossy stop is fine if you arrive with surplus — the same shape as the `nums[i] == 0` trap in **55**); re-driving the skipped block by restarting the scan at `start` instead of continuing at `i + 1` (straight back to O(n²), which is exactly what the skip lemma exists to prevent); `i < gas.size()` signed/unsigned compare; and a reflexive `long long` (unnecessary at `\|total\| ≤ 10⁹`, though genuinely needed once `gas[i] ≤ 10⁹` — state the bound, don't guess). Ladder: brute **simulate every start** with `(s+k) % n`, O(n²)/O(1) — say it, then kill it (10¹⁰ steps ⇒ hard TLE), but keep it as the **random-test oracle** → **prefix-minimum `(argmin(P) + 1) % n`**, O(n)/O(1) — the proof-shaped twin, and the cleanest thing to say out loud → **two-accumulator sweep O(n)/O(1)** (the expected answer) → **doubled array + monotonic deque of prefix minima** O(n)/O(n), worse here but the honest tool the moment the tank gains a **finite capacity** or the answer stops being unique ("return *all* valid starts" ⇒ collect every index achieving the prefix minimum). **It's Kadane with an index as the payload** — identical `cur < 0 ⇒ cur = 0` reset, except 53 carries a running max and 134 carries a running start index. Don't reach for DP or a heap: "circular + choose a start" invites over-engineering, but the structure is forced, not chosen. Circular-prefix-sum sibling **918 Max Sum Circular Subarray**; same two-arrays-into-one-diff collapse in **122**. Never sort — order *is* the problem |
| ✅ | Aug 17 | 122 | [Best Time to Buy and Sell Stock II](https://leetcode.com/problems/best-time-to-buy-and-sell-stock-ii/) | 🟡 | "**unlimited transactions**, **hold at most one share**, and you **may buy and sell on the same day**" (those three phrases together *are* the giveaway — the same-day clause is what makes the daily deltas independently choosable) → **sum the positive consecutive differences: `profit = Σ max(0, prices[i] − prices[i-1])` over `i = 1..n-1`**, one pass, one accumulator. **Why it's optimal — telescoping:** any single trade `buy@i, sell@j` earns `p[j] − p[i] = Σ_{k=i+1..j}(p[k] − p[k-1])`, so **every** legal strategy is nothing but some set of non-overlapping day-ranges whose profit is the sum of the daily deltas inside them; the best such sum obviously takes every **positive** delta and no negative one, and because same-day buy/sell is allowed that set is **actually achievable** (chain adjacent up-days into a single trade). Upper bound + achievability = optimality — a two-sentence proof, and exactly what an interviewer wants to hear before you write a line. **"Unlimited transactions" is the load-bearing word:** with a cap of `k` you'd have to *choose* which rises to take (largest, non-overlapping) and greedy dies; with no cap there is nothing to choose, every rise is free. The **peak–valley** phrasing (descend to a local valley `v`, ascend to a local peak `p`, add `p − v`, repeat) gives the identical number because a run of consecutive positive deltas telescopes back into `p − v` — use it to *explain*, use the delta form to *write* (fewer moving parts, no valley/peak state), and note it's the version that survives once a **transaction fee** appears. **The #1 confusion: this is NOT LC 121** — on `[7,1,5,3,6,4]` 122 returns **7** but 121 returns **5**; one transaction ⇒ track a running `minPrice` with `best = max(best, p[i] − minPrice)`, and this delta-sum is flatly **wrong** there. Other traps: **dropping the `max(0, …)` clamp** "to let it cancel out" collapses the whole sum into `p[n-1] − p[0]` (`−3` on example 1) — the clamp *is* the algorithm; starting the loop at `i = 0` reads `prices[-1]`; `i < size() - 1` wraps on unsigned when the vector is empty (loop from `1` and look back instead); flat days contribute 0 so `<` vs `<=` is genuinely free — know that rather than guessing; `n == 1` → `0` with **no special case** (the loop simply never runs); a reflexive `long long` is unnecessary at `3·10⁴ × 10⁴ = 3·10⁸` but state the bound out loud; and **never sort** — order *is* the problem, sorting produces `p[n-1] − p[0]` and destroys the question. Ladder: brute buy/sell/skip recursion **O(2ⁿ)/O(n)** (oracle only) → **peak–valley scan O(n)/O(1)** → **delta-sum greedy O(n)/O(1)** (the expected answer — write this) → **`hold`/`free` state-machine DP**: `hold = max(hold, free − p[i])`, `free = max(free, hold + p[i])`, seed `hold = −p[0]`, answer `free`, **O(n)/O(1)** — overkill here but the tool to reach for the instant a follow-up lands, since it extends verbatim to **714** (fee: `free = max(free, hold + p[i] − fee)`), **309** (cooldown: a third state), and **123 / 188** (`k` transactions: `2k` states, O(nk)). Framing that covers the entire stock family in one line: *"the greedy is the `k = ∞` special case of this DP."* Follow-ups: "how many transactions did you make?" → count maximal ascending **runs**, not positive deltas; "better than O(n)?" → no, every price must be read. Sibling **134 Gas Station**: same collapse-two-arrays-into-one-`diff[]` move, but 134's deltas are **chained** by a prefix-must-stay-non-negative constraint (⇒ you must locate a start index) while 122's are **independent** (⇒ order is irrelevant to the total) |
| ✅ | Aug 17 | 1326| [Minimum Number of Taps to Open to Water a Garden](https://leetcode.com/problems/minimum-number-of-taps-to-open-to-water-a-garden/) | 🔴 | "**cover the whole segment `[0, n]`** with the **minimum number of intervals**, return **`-1` if impossible**" (each item is an **interval, not a point** — tap `i` gives `[i − ranges[i], i + ranges[i]]`; the answer is a **count**, which is exactly why merging is the wrong tool; and a gap ⇒ **`-1`**, unlike 45 where reachability is guaranteed) → **it is 45 Jump Game II with the ranges handed to you explicitly**: jump the covered frontier to the **furthest right end among all intervals that start at or before it**, each jump costing one tap, and bail with `-1` the moment nothing extends past the frontier. **O(n) bucket version** — available *only* because the taps sit on integer points with `n ≤ 10⁴`: collapse the `n+1` intervals into `maxReach[max(0, i − r[i])] = max(…, i + r[i])` (keep just the furthest-reaching interval per start — that `max` on write is what lets a flat array replace a list of lists), then run the literal 45 loop: `for (i = 0; i < n; i++) { farthest = max(farthest, maxReach[i]); if (i == curEnd) { if (farthest <= i) return -1; taps++; curEnd = farthest; } }`. **General O(n log n) form** for arbitrary intervals: sort by start, then `while (covered < n) { farthest = covered; while (i < m && range[i][0] <= covered) farthest = max(farthest, range[i++][1]); if (farthest == covered) return -1; covered = farthest; taps++; }` — **the scan pointer `i` never resets**, and that is the entire complexity argument: once an interval's start is `≤ covered` it stays usable forever because `covered` only grows, so each interval is consumed exactly once across the whole outer loop; re-scanning from the beginning each round is the natural-looking mistake and it's O(n²). **The #1 wrong turn: MERGING the intervals** — merging answers the **bool** "is `[0,n]` covered?", but a merged run of 5 intervals might need only 2 taps, so it can never produce the minimum **count**; the right frame is BFS levels on a line, where `covered` is 45's `curEnd`, `farthest` is 45's `farthest`, `taps` is the BFS depth, and first arrival is optimal by construction. **You never choose *which* tap** — commit to the next *window* and let `farthest` name the best member; grabbing the tap with the largest range, or the one reaching furthest before the window scan finishes, is the classic wrong greedy. Other traps: **looping to `n` instead of `n-1`** in the bucket version (index `n` is an **arrival**, not a departure — 45's signature bug, and it counts a tap you never open); **omitting the `-1` branch** or checking it *after* `taps++` (45 guarantees reachability, 1326 does **not**; the signal is `farthest <= i` / `farthest == covered`); **not clamping `i − ranges[i]` to 0** (left ends reach `−100`, so unclamped it's an out-of-bounds subscript ⇒ **UB, not a wrong answer**); treating `ranges[i] == 0` as "skip this tap" (it's a real but degenerate `[i,i]` interval and the `farthest` max already ignores it — the same non-trap as `nums[i] == 0` in **55**); passing a **non-`static` member function** as the comparator (hidden `this` ⇒ doesn't compile — use `static`, a lambda, or just the default `vector<int>` lexicographic sort, which already orders by start then end); and `>=` instead of `>` on the frontier test, which loops forever. Ladder: brute-force **every subset of taps O(2ⁿ·n)** (oracle at `n ≤ 15` only) → **interval DP** `dp[j] = min over intervals covering j of (dp[start] + 1)`, `dp[0] = 0`, `INT_MAX ⇒ -1`, **O(n·maxRange)/O(n)** — passes comfortably at `ranges[i] ≤ 100` (~10⁶ ops), and it's both the **safe fallback if you can't prove the greedy** and the **only survivor once taps gain costs**, where greedy dies outright (cheapest cover ≠ fewest) → **sorted sweep O(n log n)/O(n)** — say this first, it's the honest general minimum-interval-cover answer → **bucket + 45 loop O(n)/O(n)**, the expected optimum here. Follow-ups: "*which* taps?" → store the argmax beside `farthest` and backtrack; "intervals given as an arbitrary list?" → **LC 1024 Video Stitching**, identical sweep; "better than O(n)?" → no, every tap must be read. It's the **covering** dual of the **packing** greedies **435 / 452** (those sort by **end** to keep the most; this sorts by **start** to advance a frontier). Not Dijkstra and not a heap — every tap costs 1, so BFS-by-levels is already optimal |

---

# 14. Dynamic Programming — 1D

**When to use it (recognition cues):**
- "Number of ways" / "max value" / "min cost".
- At each step: choose / skip / take-multiple.
- Brute force has overlapping subproblems (Fibonacci-like).

**Core idea:** Define `dp[i]` for state `i`; write a recurrence; compute bottom-up.

**Template (C++):**
```cpp
vector<int> dp(n + 1);
dp[0] = base;
for (int i = 1; i <= n; ++i)
    dp[i] = /* combine dp[i-1], dp[i-2], ... */;
return dp[n];
```

**Problems:**

| ✓ | Date | # | Problem | Diff | Trigger phrase |
|---|------|---|---------|------|----------------|
| ✅ | Aug 17 | 70  | [Climbing Stairs](https://leetcode.com/problems/climbing-stairs/) | 🟢 | "**in how many distinct ways** can you climb, taking **1 or 2 steps** at a time" with a **single scalar `n`** as input (the answer counts **orderings** — `1+2` and `2+1` are different climbs ⇒ these are **compositions, not partitions**, so no `C(n,k)` combinatorics shortcut applies; and the choice set is identical at every position, which is what makes a single recurrence enough) → **the ROOT of the 1D DP pattern: `ways(i) = ways(i-1) + ways(i-2)`, i.e. Fibonacci shifted by one (`ways(n) = Fib(n+1)`)**. **Why — partition by the LAST move:** you arrived at step `i` either from `i-1` (a 1-step) or from `i-2` (a 2-step); those two sets of paths are **disjoint** (they differ in the final move) and **exhaustive** (there is no third option), so you may simply **add** them. That "disjoint + exhaustive ⇒ sum" sentence is the entire technique behind every counting DP — say it out loud before writing a line, and note the contrast that defines the pattern: **`+` counts, `min`/`max` optimises**, same skeleton either way. **`ways(0) = 1`, NOT 0** — *the* signature bug: there is exactly one way to climb zero steps (do nothing — it's the empty product, not "no ways"), and zeroing it collapses the whole chain (`n = 2` → 1 instead of 2). **The dependency window is 2, so the table collapses to two scalars** — `prev2 = prev1 = 1; for (i = 2..n) { cur = prev1 + prev2; prev2 = prev1; prev1 = cur; } return prev1;` — the temporary `cur` is mandatory (assigning `prev1` first destroys the value `prev2` still needs), and `n == 1` falls out for free since the loop never runs. **Checking the window before allocating a table is the single most reusable move in 1D DP.** **Overflow:** the answer is `Fib(n+1)`, so `n = 45` gives `1836311903 < 2³¹−1` — `int` fits with headroom and the constraint stops at 45 for exactly that reason; `long long` here is reflex, not reasoning (at `n ≤ 90` it'd be genuine, past that only modular arithmetic). **Top-down traps worth knowing rather than getting lucky on:** with `mem` sized `n+1` and a `helper(index + 2)` branch, `index` reaches `n+1`, so the memo access is in-bounds **only because both base cases (`index == n` → 1, `index > n` → 0) return before the `mem[index]` line** — reordering the guards below the lookup is an out-of-bounds read; and forgetting to *write* the memo silently degrades to O(2ⁿ) while still returning the right answer, so it surfaces as a **TLE, not a WA**. Ladder: naive recursion **O(2ⁿ)/O(n)** — state it, then kill it by naming the cause (the same subtree is recomputed from both branches; ~10⁹ calls at `n = 45`) → **memoized top-down O(n)/O(n)** (the mechanical translation of the recurrence, and the right first move when the bottom-up ordering isn't obvious) → **bottom-up table O(n)/O(n)** (drops the 45-deep stack) → **two rolling variables O(n)/O(1)** — the expected final answer, and **stopping at memoization is the real interview miss here** → **matrix exponentiation `[[1,1],[1,0]]ⁿ` O(log n)/O(1)**, irrelevant at `n ≤ 45` but the honest reply to "what if `n` were 10¹⁸ mod p". Mention **Binet's `φⁿ/√5`** only to reject it — `double` loses exactness around `n ≈ 71`, so it's a trap answer. Follow-ups: "1, 2 **or 3** steps" → **1137**, three rolling vars, base `1,1,2`; "**minimise cost** instead of counting" → **746**, identical recurrence with `min` and `+cost[i]`; "some steps are **broken**" → `dp[i] = 0` there, recurrence unchanged; "**k** step sizes" → sliding-window sum of the last `k`, O(n) with a running total; "return the ways themselves" → no longer O(n)-countable, the output is exponential. Nearest pair to distinguish: **198 House Robber** — same two-back window, but it **skips** a state (`dp[i] = max(dp[i-1], dp[i-2] + nums[i])`) because adjacency is forbidden rather than free. Contrast the greedy line-walkers **55 / 45**: same "1 or 2 moves along a line", but there the local choice is **provably forced** so no table is needed — here every path must be **counted**, which is precisely what greedy cannot do |
| ✅ | Aug 18 | 746 | [Min Cost Climbing Stairs](https://leetcode.com/problems/min-cost-climbing-stairs/) | 🟢 | "**minimum cost** to climb a staircase, **one or two steps** at a time, **starting at index 0 or 1**, to reach the **top of the floor**" → **[[1-70-climbing-stairs\|70 Climbing Stairs]] with the combine operator swapped**: identical two-back dependency window (`i-1`, `i-2`), but the objective is an **optimum rather than a count**, so `+` becomes **`min`**, and a **per-step weight `cost[i]`** rides along. `dp[i]` = **min cost to *reach* step `i`**, having **not yet paid** `cost[i]` ⇒ `dp[i] = min(dp[i-1] + cost[i-1], dp[i-2] + cost[i-2])`, base **`dp[0] = dp[1] = 0`** (both entry points are free), answer **`dp[n]`**. **Why — partition by the LAST move:** you arrived at `i` from either `i-1` or `i-2`; the two sets are **disjoint** (different final move) and **exhaustive** (no third option) — counting **sums** the branches (70), optimising takes the **better** one (here). That single operator swap is the entire difference between the two problems, and naming it is the fastest correct answer in the room. **Signature bug — returning `dp[n-1]` instead of `dp[n]`:** the "top of the **floor**" is the imaginary step **one past** the array, so the loop must run `i <= n`; on `[10,15,20]` `dp[n-1]` yields `10` (reach the last step) instead of `15` (reach the top), and this is exactly why `dp[n]` carries no cost of its own. **Second bug — the weight on the wrong state:** you're charged to **step onto** a step, so the cost belongs to the state you **leave** — `dp[i-1] + cost[i-1]`, never `dp[i-1] + cost[i]`; likewise `dp[0] = cost[0]` double-charges the first step, since `dp` means "cost to *reach*". Pick the prefix or the suffix formulation and don't mix them mid-solution. **Base is `0`, not `1`** — mirror of 70's `ways(0) = 1`: the empty answer is the **identity of the combine operator**, `1` for a count/product, **`0` for a sum of costs**. Same argument, opposite value; carry that rule to every DP in the pattern. **The window is 2 ⇒ two scalars:** `prev2 = prev1 = 0; for (i = 2; i <= n; i++) { cur = min(prev1 + cost[i-1], prev2 + cost[i-2]); prev2 = prev1; prev1 = cur; } return prev1;` — the temporary `cur` is mandatory, and `n == 2` needs no special case (one iteration gives `min(cost[0], cost[1])`). **Greedy is wrong, and pre-empting it out loud scores:** example 2 `[1,100,1,1,1,100,1,1,100,1] → 6` is the counterexample — a locally-cheapest walk gets trapped, because the cheap step now can force a `100` later; no exchange argument exists ⇒ you need the table. **Top-down / suffix variant:** `helper(i) = cost[i] + min(helper(i+1), helper(i+2))` with the answer `min(helper(0), helper(1))` — here the cost sits on the state you **enter**, the clean mirror of the above, and **one shared memo across both entry calls is safe only because `helper(i)` is a pure suffix answer**, independent of where you started; had the state carried anything start-dependent (a step budget, a parity), the shared memo would silently return stale values. Ladder: naive recursion **O(2ⁿ)/O(n)** (the same suffix recomputed from both branches) → **memoized top-down O(n)/O(n)** (the mechanical translation; right first move when the bottom-up order isn't obvious) → **bottom-up table O(n)/O(n)** → **two rolling variables O(n)/O(1)** — the expected final answer; **in-place** `cost[i] += min(cost[i-1], cost[i-2])`, return `min(cost[n-1], cost[n-2])` is also O(1) but **mutates the input**, so flag it rather than leading with it. **Overflow:** worst case `1000 × 999 < 10⁶` and it's a `min`, so `int` is ample — say the bound, don't reflex to `long long`. Follow-ups: "1, 2 **or 3** steps" → window of 3; "**k** step sizes" → `min` over the last `k`, O(nk) or O(n log k) with a monotonic deque; "**count** the ways instead" → swap `min` back to `+` and you're at **70**; "return the actual **path**" → keep a `parent[]` of the winning branch, O(n) extra; "costs may be **negative**" → the recurrence still holds (it's a DAG), unlike any greedy/Dijkstra argument. **The 70 / 746 / 198 triple to distinguish out loud:** same two-back window, three combines — **70** `dp[i] = dp[i-1] + dp[i-2]` (`+` **counts**) · **746** `min(dp[i-1] + w, dp[i-2] + w)` (`min` optimises, **every** branch pays) · **198** `max(dp[i-1], dp[i-2] + nums[i])` (`max` optimises but the branches are **asymmetric** — one takes the weight, one **skips** it, because adjacency is forbidden rather than free). Name the operator and the weight placement and you've named the problem |
| ✅ | Aug 19 | 198 | [House Robber](https://leetcode.com/problems/house-robber/) | 🟡 | "**maximum** amount you can rob" + "**adjacent houses** cannot both be robbed" (the same shape wherever it appears: no two **adjacent** elements, neighbouring seats, consecutive days) → **[[1-70-climbing-stairs\|70 Climbing Stairs]] with the branches made ASYMMETRIC**: identical two-back dependency window (`i-1`, `i-2`), `+` swapped for **`max`** (an **optimum, not a count**), and — the real difference from 746 — **only ONE branch carries the weight**. `dp[i]` = the best over the **prefix `0..i`** (not necessarily robbing `i`) ⇒ **`dp[i] = max(dp[i-1], nums[i] + dp[i-2])`**, base **`dp[0] = nums[0]`** and **`dp[1] = max(nums[0], nums[1])`**, answer **`dp[n-1]`**. **Why — partition by the LAST decision at house `i`:** you either **skip** it (answer is `dp[i-1]`, weight **not** added) or **rob** it (`i-1` is now forbidden, so add `nums[i]` to `dp[i-2]`); disjoint + exhaustive ⇒ `max`. **The asymmetry IS the adjacency constraint** — in **746 both** branches pay a cost, here the skip branch takes `dp[i-1]` **bare** while the rob branch reaches **two** back; the moment you write `max(dp[i-1] + nums[i], dp[i-2] + nums[i])` you have deleted the rule and are simply summing the array. **Second definitional trap — `dp[i]` means "best over the prefix", NOT "best if I rob house `i`":** that is exactly why `dp[i-2]` is enough (it already contains the option of *not* robbing `i-2`, so the recurrence never looks three back); define it the other way and you need a `max` over every `j ≤ i-2` and have accidentally written **O(n²)**. **Signature bug — `dp[1] = nums[1]` (or `nums[0]`) instead of `max(nums[0], nums[1])`:** the prefix definition means `dp[1]` must already be allowed to **decline** house 1; and touching `dp[1]` at all is **out of bounds when `n == 1`**, so the table form needs an `if (n > 1)` guard. Note the base-case contrast with 70/91, where the base is the **identity of the combine operator** (`1` for `+`) — here `max` runs over a **non-empty** prefix, so `dp[0]` is a real value, not an identity. **The window is 2 ⇒ two scalars, and the rolling form deletes both bugs:** `int prev2 = 0, prev1 = 0; for (int x : nums) { int cur = max(prev1, x + prev2); prev2 = prev1; prev1 = cur; } return prev1;` — starting both at `0` reproduces the recursion's `index < 0 → 0` base, so the first iteration yields `max(0, nums[0])` and **`n == 1` falls out free**; the temporary `cur` is mandatory. **Top-down needs no base cases at all** — `if (index < 0) return 0;` folds indices `-1` and `-2` — which is precisely why the **conversion** to tabulation is where this problem breaks people: **the recurrence survives untouched and only the base cases break**, so derive them from the recursion's guard conditions rather than from memory. Two more top-down notes: the `index < 0` guard must sit **above** the `mem[index]` lookup (same ordering trap as 70 and 91), and `mem[i] != -1` is a legal sentinel **only because `nums[i] >= 0`** — with negative values you'd need a separate `visited` array. **Greedy is wrong, and killing it out loud scores:** "take every other house" breaks on `[2,1,1,2]` (alternating → `3`, optimal `2 + 2 = 4`) and "take the largest, delete its neighbours, repeat" breaks on `[4,5,4]` (grabs `5`, optimal `4 + 4 = 8`) — one pick constrains **two** neighbours at once, so no exchange argument exists. Ladder: brute-force subset enumeration **O(2ⁿ·n)** → naive recursion **O(2ⁿ)/O(n)** (`f(i-2)` reached from both branches ⇒ Fibonacci-many recomputations) → **memoized top-down O(n)/O(n)** → **bottom-up table O(n)/O(n)** → **two rolling variables O(n)/O(1)**, the expected final answer. **Overflow:** `50 × 400 = 20000` — `int` is ample, say the bound instead of reflexing to `long long`. Follow-ups: "the street is a **circle**" → **213 House Robber II**, where `0` and `n-1` become adjacent, so run the same O(1) helper **twice** — over `[0, n-2]` and `[1, n-1]` — and take the `max`, special-casing `n == 1`; "the houses form a **binary tree**" → **337 House Robber III**, same take/skip split but the state becomes a **`(robbed, notRobbed)` pair per node** returned bottom-up in one DFS, O(n); "no two picks within **k**" → `dp[i] = max(dp[i-1], nums[i] + dp[i-k-1])`, window widens to `k+1`; "return **which** houses" → backtrack over `dp`, O(n) extra; "pick **exactly k**" → the state gains a dimension, `dp[i][k]`, O(nk). Textbook name worth dropping: this is **maximum-weight independent set on a path**. **The 70 / 746 / 91 / 198 quartet to distinguish out loud** — same two-back window, four merge rules: **70** `dp[i-1] + dp[i-2]` (`+` counts, branches **unconditional**) · **746** `min(dp[i-1] + w, dp[i-2] + w)` (`min` optimises, **both** branches pay) · **91** `dp[i-1] + dp[i-2]` (`+` counts, branches **conditional on a predicate**) · **198** `max(dp[i-1], dp[i-2] + nums[i])` (`max` optimises, branches **asymmetric** — one takes the weight, one **skips** it). Name the combine operator, the weight placement and the guard, and you have named the problem |
| ✅ | Aug 19 | 213 | [House Robber II](https://leetcode.com/problems/house-robber-ii/) | 🟡 | a **198-shaped** statement — "**maximum** amount you can rob, **adjacent houses** cannot both be robbed" — **plus** "all houses are **arranged in a circle**" / "the **first house is the neighbour of the last**" → **[[4-198-house-robber\|198 House Robber]] with the BOUNDARY broken, and the recurrence does not change by one character.** `max(skip, take + two-back)` is still the entire engine; closing the street into a ring adds exactly **one** new coupling — `nums[0]` and `nums[n-1]` are now adjacent — and the fix is **not a new recurrence but a case split on that one pair**: **`answer = max(rob198(nums[0 .. n-2]), rob198(nums[1 .. n-1]))`**. This is the pattern's first "**reduce to the already-solved problem**" problem — what's being tested is **decomposition, not DP**. **Why the two ranges are EXACT rather than a heuristic (the argument to say out loud):** in any valid circular selection houses `0` and `n-1` are **never both** robbed, so every valid answer omits at least one end and therefore lies entirely inside `[0, n-2]` **or** entirely inside `[1, n-1]` — the cases are **exhaustive**; and conversely any no-two-adjacent selection *inside* either range is automatically legal on the circle, because the wrap pair cannot both be present when one end is missing — so the cases are **sound**. Exhaustive + sound ⇒ the `max` of the two optima **is** the optimum. **The two cases overlap** (a solution using neither end is counted twice) and that is **harmless precisely because you take a `max`, not a sum** — pre-empt this, it is the objection an interviewer raises. **Mandatory `n == 1` guard:** with one house both ranges are **empty**, both passes return `0`, and you answer `0` instead of `nums[0]`; this is **not** fixable by adjusting bounds, since the decomposition assumes two distinct ends exist to drop. (`n == 2` needs no guard — the ranges collapse to `{0}` and `{1}` and `max` is right; `n == 3` returns the largest single element, because no two of three circular houses are non-adjacent.) **Signature bug in the memoized version — SHARING ONE MEMO ACROSS THE TWO PASSES:** `helper(index, lastIndex)` has a **two-part state** while `mem[index]` keys only **one** of them, so pass 2 silently reads answers pass 1 computed under a different right boundary. On `[1,1,1,5]` a shared memo returns **2** instead of **6** — pass 1 stores `mem[1] = 1` (best of `[1,1]` stopping before the last house) and pass 2, which needs `1 + 5 = 6` from index 1, reads that `1` from the cache and never explores house 3. Fix with `mem.assign(n, -1)` between the calls, two separate vectors, or a `mem[index][which]` key. **The rule worth carrying: a memo may be shared exactly when every dimension the answer depends on is part of the key** — the sharp contrast with **746**, where one memo across *both* entry points **was** legitimate because `helper(i)` was a **pure suffix** answer, independent of where you started. **Canonical O(1)-space shape:** a `robLinear(l, r)` helper over a **half-open** range — `int prev2 = 0, prev1 = 0; for (int i = l; i < r; i++) { int cur = max(prev1, nums[i] + prev2); prev2 = prev1; prev1 = cur; } return prev1;` — called as `max(robLinear(0, n-1), robLinear(1, n))`; the `0` seeds reproduce the recursion's out-of-range base, so an **empty range returns `0`** for free and no per-range base cases exist to get wrong. **Guard ordering (top-down):** `if (index >= lastIndex) return 0;` must sit **above** the `mem[index]` lookup — `index + 2` overshoots to `lastIndex + 1`, so the memo access is in bounds only because the guard fires first (same trap as **70**, **91**, **198**); and `mem[i] != -1` is a legal sentinel only because `nums[i] >= 0`. **Wrong fixes to name and reject:** "solve the street linearly, then subtract an end if both were taken" — a global optimum does not decompose that way, removing one end can make a completely different selection optimal; a `dp[i][tookFirst]` **boolean state** — correct and O(n), but it invents a dimension the boundary split hands you free, and reads as not seeing the reduction; `nums[0] + rob(nums[2 .. n-2])` vs `rob(nums[1 .. n-1])` — the *must-rob-house-0* split, **also correct** but with fiddlier bounds at `n == 2` and `n == 3`. Ladder: brute-force subsets **O(2ⁿ·n)** → naive recursion twice **O(2ⁿ)** → **memoized twice O(n)/O(n)** → **bottom-up table twice O(n)/O(n)** (the offset bases `dp[l] = nums[l]`, `dp[l+1] = max(nums[l], nums[l+1])` are where this rung bites) → **two rolling variables, twice — O(n)/O(1)**, the expected final answer. **Overflow:** `50 × 1000 = 50000`, `int` is ample — state the bound. **The general move to internalise: a linear DP plus ONE wrap-around coupling ⇒ enumerate how the coupling resolves and re-run the linear solver per case — a constant factor, never an extra state dimension**; the identical reflex solves **918 Maximum Sum Circular Subarray** (`max(Kadane, total − minSubarray)`, plus the all-negative guard). Follow-ups: "houses form a **binary tree**" → **337 House Robber III**, where the take/skip split survives but the state becomes a **`(robbed, notRobbed)` pair per node** returned bottom-up in one DFS, O(n) — **the ring trick does not transfer**, a tree has no single coupling to enumerate; "circular, no two picks within **k**" → `k` boundary cases, O(nk); "return **which** houses" → re-run the winning pass with a `parent[]`. Textbook object: **maximum-weight independent set on a cycle** (198 is the path version). **The 70 / 746 / 91 / 198 / 213 progression:** 70 establishes the two-back window (`+`, counts) · 746 swaps the combine to `min` with a weight on **both** branches · 91 keeps `+` but **gates** each branch on a predicate · 198 swaps to `max` with **asymmetric** branches (one skips the weight) · **213 changes nothing about the recurrence at all — it changes the DOMAIN**, and is solved by calling 198 twice. Recognising which dial a new problem turns *is* the pattern |
| ✅ | Aug 20 | 53  | [Maximum Subarray](https://leetcode.com/problems/maximum-subarray/) | 🟡 | "find the **contiguous subarray** with the **largest sum**" — the words **contiguous / subarray / consecutive elements** sitting next to an **optimum**, **with negative values in the constraints** (`-10⁴ ≤ nums[i]`; if every value were positive the answer would trivially be the whole array, so the negatives *are* the problem) → **Kadane's algorithm — and the first problem in the pattern that changes the STATE DEFINITION rather than the combine operator, the weights, the guards or the domain.** Every earlier rung defined `dp[i]` as "**the answer for the prefix `0..i`**"; here that definition **fails outright**, and understanding *why* is the entire content of the problem: knowing "the best subarray inside `[-2,1,-3]` is `1`" tells you **nothing** about `[-2,1,-3,4]`, because the winning subarray **may not touch the boundary** and therefore **cannot absorb** the next element. Contiguity means only a subarray that **reaches index `i`** is extendable. So you **constrain** the state — **`dp[i]` = the best subarray ENDING EXACTLY at `i`**, with `nums[i]` mandatorily included — which restores composability, and you **pay** for it by losing the answer from the state, which you **buy back with a separate running global max**. **`dp[i] = max(nums[i], dp[i-1] + nums[i])`** (restart \| extend), base **`dp[0] = nums[0]`**, answer **`max over ALL i of dp[i]`** — **never `dp[n-1]`**. *The transferable rule: when the natural state doesn't compose, add a constraint until it does, then aggregate the constrained states.* **The recurrence factors as `nums[i] + max(0, dp[i-1])`**, which is the sentence to say out loud: the decision is **not about `nums[i]` at all**, it is "**is the baggage I'm carrying helping or hurting?**" — if the running sum is negative, every subarray continuing through it starts at a deficit, so **cutting it away strictly improves** everything that follows. **Reset on the running SUM, never on the current ELEMENT** — `[4,-1,2,1] → 6` is the counterexample the problem hands you in Example 1: a negative element inside a strong run belongs in the optimum; a negative *run* never does. **Signature bug — initialising the answer to `0`, or resetting before recording.** In the reset formulation `sum += num; best = max(best, sum); if (sum < 0) sum = 0;` the **order of the last two lines is load-bearing**: record **then** reset. Swap them (or seed `best = 0`) and you have silently admitted the **empty** subarray as a candidate, so every **all-negative** array returns `0` instead of the largest single element — `[-3,-1,-2]` → `0` instead of `-1`. Seed with `INT_MIN` or `nums[0]`. **The canonical form `cur = max(nums[i], cur + nums[i])` cannot express that bug at all**, because `cur` always denotes a real **non-empty** subarray — which is the reason to prefer it under pressure; the two are mathematically identical (`cur` equals `sum` *after* the reset), and knowing that they agree is worth stating. **Second signature bug — returning `dp[n-1]` in the table form:** the state is *"ending at `i`"*, so the answer lives in the **aggregate**, not the final cell — on `[5,4,-1,7,8,-100]`, `dp[n-1] = -77` while the answer is `23`. This is the precise structural break from 198/213, where the last cell **was** the answer because the state was a prefix optimum. **Do not reach for two pointers / sliding window:** negatives make the running sum **non-monotonic**, so no valid shrink rule exists — sliding window needs monotonicity, Kadane doesn't. Ladder: brute force over all `(i, j)` **O(n³)**, **O(n²)** with a running inner sum or prefix sums — at `n = 10⁵` that's 10¹⁰ ops, so **state the bound and kill it** → **prefix-sum reformulation** `answer = max over j of (P[j] − min over i<j of P[i])`, **O(n)/O(1)** — Kadane in a different coordinate system, and the **cleanest bridge to 121**, which is *literally* "max `P[j] − P[i]` for `i < j`" → **Kadane, one pass, O(n)/O(1)** — the expected answer → **divide & conquer O(n log n)/O(log n)**, the problem's own stated follow-up: split at `mid`, answer is `max(bestLeft, bestRight, bestCrossing)` where the crossing sum is the **best suffix of the left half + best prefix of the right half**, computed by **two outward scans from `mid`** (re-scanning the whole range instead degrades the recurrence); strictly worse than Kadane, **but know it**, because it is exactly the **merge rule of a segment tree** storing `(total, bestPrefix, bestSuffix, best)` per node — the only way to answer *"max subarray sum in range `[l, r]` with point updates"* in O(log n), and the real reason the hint calls it "more subtle". **Overflow:** `10⁵ × 10⁴ = 10⁹ < 2³¹−1 ≈ 2.1×10⁹` ⇒ **`int` fits** with ~2× headroom; state the bound rather than reflexing to `long long` (**but do** reach for it in 152, where products explode). **Edges:** `n == 1` → `nums[0]` **even if negative**; **all-negative** → the **largest single element**, because the subarray must be **non-empty** — that is the case every buggy Kadane fails, and it is why the two canonical forms seed from `nums[0]`. Follow-ups: "**return the subarray, not the sum**" → track `start` at every restart and record `(start, i)` whenever `best` improves, O(n), no second pass; "the array is **circular**" → **918**, `max(Kadane, total − minSubarrayKadane)` with a **mandatory all-negative guard** (otherwise the second term picks the empty subarray and returns `0`) — the identical "ring ⇒ two cases" reflex as **213**; "**product** instead of sum" → **152**, where you must carry **`maxEnd` and `minEnd`** because multiplying by a negative swaps them, and single-state Kadane is simply wrong — carrying the habit across is the trap; "**at most k** elements" → monotonic deque over prefix sums, O(n); "**at least k**" → prefix sums plus a running min of `P[i−k]`, O(n); "**2D**, maximum-sum submatrix" → fix a top/bottom row pair, compress each column to one sum, run Kadane on the compressed row, **O(rows²·cols)**; "allow the **empty** subarray" → the answer becomes `max(0, kadane)` — which is exactly the assumption the buggy orderings accidentally make. **The 70 / 746 / 91 / 198 / 213 / 53 progression to state out loud** — same skeleton, six dials: **70** `dp[i-1] + dp[i-2]` (`+` **counts**, branches unconditional) · **746** `min`, **both** branches pay · **91** `+`, branches **conditional on a predicate** · **198** `max`, branches **asymmetric** (one skips the weight) · **213** the recurrence is untouched, the **domain** changes (circle ⇒ run the linear solver twice) · **53** the recurrence *and* domain are ordinary, the **state definition** changes — and therefore it is the first problem where **the answer is not the last cell of the table**. Naming which dial a new problem turns *is* the pattern |
| ✅ | Aug 21 | 152 | [Maximum Product Subarray](https://leetcode.com/problems/maximum-product-subarray/) | 🟡 | "find the **contiguous subarray** with the **largest PRODUCT**" — the word **product** where 53 said sum, with **negatives and zeros guaranteed** by the constraints (`-10 ≤ nums[i] ≤ 10`) → **Kadane's state design kept verbatim, but the OPERATOR's algebra changes, and the state must widen from a POINT to an INTERVAL.** **53** taught the move — define `dp[i]` = *"best subarray **ending exactly** at `i`"* and aggregate a **separate global max**. 152 keeps that move untouched and breaks the assumption nobody notices they were relying on: Kadane's correctness under `+` rests on **monotonicity** — `a > b ⇒ a + x > b + x`, so the **best** predecessor stays the **best** successor and it is safe to discard everything else. **`×` has no such guarantee, because a negative multiplier REVERSES the order.** So a single state is not merely weaker, it is **provably insufficient**: on `[-2,3,-4]` the best-ending-here at index 1 is `3` and the worst is `-6`, and the answer **`24 = -6 × -4`** is built **entirely out of the state that single-state Kadane threw away**. Carry **both extremes** — **`maxEnd` AND `minEnd`** — and **swap them on a negative**. *The transferable rule to say out loud: when the combine operator is **not order-preserving**, the state must carry the whole **reachable interval**, not just its optimum.* Canonical, `O(n)/O(1)`: `if (x < 0) swap(maxEnd, minEnd); maxEnd = max(x, maxEnd*x); minEnd = min(x, minEnd*x); best = max(best, maxEnd);` — seed **all three at `nums[0]`** (non-emptiness becomes structural, `n == 1` falls out free), answer is the **max over ALL i**, **never the last cell** (inherited from 53). The `swap` *is* the insight written as code: a negative maps the top of the interval to the bottom. **In the swap-free three-candidate form** `maxEnd = max({x, prevMax*x, minEnd*x})` **you MUST stage `prevMax` in a temporary** — otherwise `minEnd` is computed from the **new** `maxEnd` and the whole thing rots silently; note the **table** form has no such bug (`mx[i-1]` is a different cell from `mx[i]`), which is exactly why the temporary is the thing you mention **at the moment you collapse the table** — the same discipline 70 and 746 already charged you for. **Zero is a third case hiding in plain sight:** it **annihilates**, it **splits the array into independent segments**, and `0` is itself a legitimate answer — Example 2 `[-2,0,-1] → 0`, **not `2`**, because `[-2,-1]` is **not contiguous**. Both canonical forms handle it for free through the **restart** candidate. **Second optimal solution worth owning — the prefix/suffix sweep:** the optimal subarray is always **bounded by zeros or by the array ends**, and inside a single zero-free segment the product is either **positive** (⇒ take the whole segment) or **negative** (⇒ **exactly one negative must be dropped**, and cutting at it leaves either a **prefix** or a **suffix**). Therefore running a **prefix product left→right and a suffix product right→left in ONE loop**, resetting each to `1` at a zero, is **guaranteed to see the answer** — `O(n)/O(1)`, and that is a real proof, not a trick. **Record BEFORE resetting** there — `maxi = max(...)` must sit **above** `if (p == 0) p = 1;` or `[-2,0,-1]` returns `-1`; and seed **`INT_MIN`, never `1`**, since `1` pre-loads the **empty** subarray (`[-3] → 1` instead of `-3`). **That is the identical record-then-reset trap as 53, wearing a different identity element** — `1` here, `0` there. **Overflow — the mirror image of 53:** the problem *states* that every subarray product fits in a 32-bit int, so **quote the guarantee** to justify `int` instead of reflexing to `long long` (in 53 you justified `int` by **deriving** the bound yourself). Without that clause `10^(2×10⁴)` overflows `long long` too, and the fix is a **log transform** — `log\|x\|` turns `×` into `+` so plain Kadane runs over magnitudes, with the **sign handled separately** by the negative-parity argument and zeros as their own case, trading away exactness. **Prefix products + division does NOT transfer** from the `+` world — there is no inverse across a zero; mention it only to reject it. **Do not carry single-state Kadane over from 53** — that habit *is* the trap the 53 note warned about. **Edges:** `n == 1` → `nums[0]` **even if negative or zero** · **all-negative `[-2,-3,-4] → 12`** — an **even** run of negatives is a *large positive*, so "all negatives ⇒ negative answer" is false · `[0,2] → 2` · `[-3] → -3`. Ladder: brute force over all `(i, j)` **O(n³)**, **O(n²)** with a running inner product — 4×10⁸ at `n = 2×10⁴`, name the bound and kill it → **two-state Kadane O(n)/O(1)** (the expected answer) ≡ **prefix/suffix sweep O(n)/O(1)** ≡ **split-on-zeros then count negatives per segment** (the most *explanatory* form, and the cleanest thing to say when asked *why prefix-or-suffix is enough*) → **D&C O(n log n)**, carrying **both** extremes across the merge — strictly worse standalone, but it is the **segment-tree merge rule** once the follow-up adds point updates. Follow-ups: "**return the subarray**" → track `start` at each restart, record `(start, i)` when `best` improves · "**circular**" → the 213 ring reflex · "max product of **k elements / a subsequence**" → not this problem at all, sort and pair off the extremes, `O(n log n)` · "count subarrays with **positive** product" → running parity of negatives, `O(n)`. **The seven-rung progression to state out loud** — one skeleton, seven dials: **70** combine `+` (counting) · **746** `min` with weights on **both** branches · **91** `+` with **guarded** branches · **198** `max` with **asymmetric** branches · **213** the **domain** changes, the recurrence doesn't · **53** the **state definition** changes (and so the answer stops being the last cell) · **152** the **operator's algebra** changes (and so the state stops being a single value). Naming which dial a new problem turns *is* the pattern |
| ✅ | Aug 22 | 121 | [Best Time to Buy and Sell Stock](https://leetcode.com/problems/best-time-to-buy-and-sell-stock/) | 🟢 | "you may choose **a single day to buy** one stock and **a different day in the future to sell** it" + "return the **maximum profit**; if you cannot achieve any profit, return **0**" — the shape is an optimum over an **ordered pair**, **`max(a[j] − a[i])` subject to `i < j`**, not over a window or a subset. Two tells pin it down: "choose **ONE** day to buy and **ONE** day to sell" (exactly **one** transaction ⇒ this is *not* [[../11. Greedy & Intervals/11. 122. Best Time to Buy and Sell Stock II\|122]]'s delta-sum) and "**return 0** if you cannot profit" (the **empty transaction is legal**, which is precisely where it diverges from [[6-53-maximum-subarray\|53]]) → **[[6-53-maximum-subarray\|53 Maximum Subarray]] run on the DIFFERENCE ARRAY — the recurrence, the state and the combine operator are all Kadane's, untouched; the only thing that changes is the INPUT.** The unlocking identity is **telescoping**: `prices[j] − prices[i] = Σ_{k=i+1..j} d[k]` where `d[k] = prices[k] − prices[k-1]`, so **a difference of two elements is a sum of consecutive deltas**, every legal transaction is a **contiguous run** of `d`, and "best pair with `i < j`" ≡ "**maximum-sum contiguous subarray of `d`**". On `[7,1,5,3,6,4]`, `d = [-6,4,-2,3,-2]` and Kadane returns `4 + (−2) + 3 = 5`. ✅ **That single picture also hands you 122 for free** — unlimited transactions means you may take **any set** of non-overlapping runs, so you take **every positive delta**, `Σ max(0, d[k])`: **121 = the best single run · 122 = the sum of all positive runs.** What you actually **write** is the equivalent **prefix-extremum sweep**: fix the sell day `j` and the best partner is *forced* to be the **cheapest day seen so far**, so one pass suffices — `minSoFar = min(minSoFar, p); profit = max(profit, p − minSoFar);` with seeds `minSoFar = prices[0]`, `profit = 0`. ***The transferable rule: an answer shaped `max over pairs f(j) − g(i)` with `i < j` never needs two loops — sweep `j` forward carrying the running optimum of `g` over the prefix.*** That reduction (O(n²) pairs → O(n) with one carried extremum) is the most reused trick in the array section. **Signature bug — answering `max(prices) − min(prices)`:** it silently drops the ordering constraint, and Example 1 is engineered to punish it (`max = 7` sits at index 0, **before** `min = 1`, so it reports an illegal `6` instead of `5`). The problem is **not the array's spread**, it is **the largest rise that goes forward in time** — which is also why you must **never sort**: sorting produces exactly `max − min` and destroys the only structure that matters. **The inversion worth stating out loud: `profit = 0` is the SPECIFICATION here and was the signature BUG in 53.** 53 forbids the empty subarray, so seeding the answer at `0` wrongly returned `0` on all-negative input; 121 *asks* for the empty transaction ("if you cannot achieve any profit, return 0") — i.e. **121's answer is `max(0, kadane(d))`, which is precisely 53's stated "allow the empty subarray" follow-up**. Kadane's seed **encodes whether the empty selection is allowed**, and carrying `INT_MIN` across from 53 returns a negative profit on a falling market. Second seed trap: **`mini = prices[0]`, never `0`** — `0 ≤ prices[i]` is in the constraints, so a `0` sentinel collapses the answer to `max(prices)`. **Updating `mini` before reading it is safe — but say why, don't hand-wave:** it makes day `i` a candidate buy for a sale on day `i` itself, contributing `p − p = 0`, which the `0` seed already dominates. Under **2016 Maximum Difference Between Increasing Elements** ("return `-1` if no profit exists") that no-op is **forbidden**, the min must come from the **strict** prefix, and the order of those two lines becomes load-bearing — knowing which invariant your line order rests on is what makes a solution survive a follow-up. **Edges:** `n == 1` → `0`, free from the `prices[0]` seed (the sole iteration yields `0`) · strictly decreasing → `0` · all-equal → `0` · **the optimal buy is NOT the global minimum**, only the minimum of the prefix *before* the optimal sell — `[2,10,1,5] → 8` (buy `2`, sell `10`), while the global min `1` can only reach `5`. **Overflow:** profit ≤ `10⁴ − 0`, so `int` is trivially safe with no bound-hunting (the opposite of 53, where you had to compute `10⁵ × 10⁴`). Ladder: brute force over all ordered pairs **O(n²)/O(1)** — 5×10⁹ ops at `n = 10⁵`, name the bound and kill it → **suffix-max mirror** `max(suffMax[i] − prices[i])` **O(n)/O(n)** → **prefix-min sweep O(n)/O(1)** ≡ **Kadane on the deltas O(n)/O(1)** (the expected answer; the delta form needs the `cur = max(0, cur + d)` clamp or it can return a negative and violate "return 0") → **`hold`/`free` state-machine DP O(n)/O(1)** — `hold = max(hold, −prices[i])` (with **one** transaction you buy from a balance of `0`, **not** `free − prices[i]`) and `free = max(free, hold + prices[i])`, seeds `hold = −prices[0]`, `free = 0`, answer `free`; overkill standalone but **the only formulation that survives the follow-ups** → **divide & conquer O(n log n)/O(log n)**, `max(bestLeft, bestRight, maxRight − minLeft)`, which is the **segment-tree merge rule** over `(min, max, bestProfit)` and therefore the only route to *"best profit within day range `[l, r]` with price updates"*. **One line covering the whole stock family:** *the [[../11. Greedy & Intervals/11. 122. Best Time to Buy and Sell Stock II\|122]] greedy is the `k = ∞` case, 121 is the `k = 1` case, and the `hold`/`free` machine is what interpolates between them* — **123** (at most two transactions, four rolling states) · **188** (at most `k`, `2k` states, O(nk), with the `k ≥ n/2` shortcut falling back to 122's greedy) · **309** (cooldown, a third state) · **714** (fee: `free = max(free, hold + p − fee)`). **The #1 cross-wiring in this family fires in both directions:** applying 122's delta-sum here gives **7** on `[7,1,5,3,6,4]` when only one transaction is allowed and the answer is **5**; applying 121's single-rise logic to 122 undercounts. Follow-ups: *return the buy/sell days* → record `buyIdx` whenever `mini` improves and commit `(buyIdx, i)` whenever `profit` improves, O(n) with no second pass · *maximum drawdown* → the exact mirror, carry `maxSoFar` and take `max(maxSoFar − prices[i])` · *prices arrive as a stream* → the sweep is already online, two scalars with the answer available at every instant. **The 70 / 746 / 91 / 198 / 213 / 53 / 152 / 121 progression, same skeleton, seven dials:** **70** `+` counts, branches unconditional · **746** `min`, **both** branches pay · **91** `+`, branches **conditional** · **198** `max`, branches **asymmetric** · **213** the recurrence is untouched, the **domain** changes · **53** the **state definition** changes (prefix optimum → *ending exactly here*) ⇒ the answer is the aggregate, not the last cell · **152** the **operator's algebra** changes (a point state widens to an **interval**) · **121** the **INPUT** changes — same Kadane, run on the difference array, with the seed flipped because the empty selection is now legal. Naming which dial a new problem turns *is* the pattern |
| ✅ | Aug 22 | 322 | [Coin Change](https://leetcode.com/problems/coin-change/) | 🟡 | "return the **fewest number of coins** you need to make up that **amount**" + "you may assume you have an **infinite number** of each kind of coin" + "return **-1** if that amount cannot be made up" — a count of **items**, not of ways ⇒ combine with **`min`**; "infinite number of each" ⇒ **unbounded** knapsack; and the constraint shape (`amount ≤ 10⁴`, `≤ 12` coins, coin *values* to `2³¹−1`) is the tell that **pseudo-polynomial DP over the amount** is the intended solution → **the first problem in the pattern whose state is a VALUE rather than a POSITION: `coins` stops being the domain and becomes the TRANSITION SET.** `dp[a] = 1 + min over c ≤ a of dp[a − c]`, base **`dp[0] = 0`**, answer `dp[amount]` with the sentinel translated to `-1`. **Why — partition by the LAST coin spent:** the cases are **disjoint** (they differ in the final coin) and **exhaustive** (some coin must be last), so counting would *add* them and optimising takes the **min** — the same one-sentence technique as every other problem here, applied to a different kind of state. Two things change at once and both matter: the table is sized by a **constraint bound** (`amount + 1`), not by the input's length, and the branching factor grows from a fixed **2** to **`\|coins\|`** ⇒ **the two-rolling-variable collapse that closed all eight previous problems finally dies**, because `dp[a]` can reach back as far as `max(coins)`. **THE signature error: greedy.** "Take the largest coin that fits" gives **3** on `coins = [1,3,4], amount = 6` (`4+1+1`) where the optimum is **2** (`3+3`); the exchange argument behind greedy holds only for **canonical** coin systems, and the problem deliberately hands you an arbitrary set. Memorise that counterexample — it is the first thing an interviewer probes. **Sentinel bug: `INT_MAX` as ∞** — `INT_MAX + 1` wraps to `INT_MIN`, which then **wins** every `min` and silently poisons the table with negatives. Use **`amount + 1`**: every coin is `≥ 1`, so no real answer can exceed `amount`, and adding 1 to it is nowhere near overflow — ***pick your infinity from the problem's own bound, not from the type's***. Equally fatal: **extending an unreachable state** — `-1 + 1 = 0` reports an impossible amount as *free*, so either guard the read or make ∞ unbeatable, and hold **one** convention through the entire function rather than mixing `-1` and ∞. **"Unbounded" is precisely the statement that `dp[a]` may read `dp[a − c]` from the row it is currently writing** (0/1 knapsack must read the *previous* row, which is why its 1-D collapse iterates the weight **downward**) ⇒ **the loop order here is completely free**, `for a { for c }` and `for c { for a }` agree. That freedom **evaporates in 518 Coin Change II**, where `min` becomes `+`: coins-outer counts **combinations**, amounts-outer counts **permutations** (**377 Combination Sum IV**) — *loop order is meaningless for `min`/`max` and decisive for `+`*, and knowing **why** beats knowing the rule. **`coins[i] ≤ 2³¹−1` is a planted trap:** write `if (c <= a)`, never `if (a - c >= 0)` — the subtracted form survives by about ten thousand here and is genuine signed overflow in the general version; in the recursive form the `amount < 0` base **must sit above the `mem[]` lookup**, or a huge coin drives a **negative index** into the vector (the [[1-70-climbing-stairs\|70]] / [[3-91-decode-ways\|91]] guards-before-memo trap, but with a segfault instead of a benign over-read). **The memo's "unset" marker must live OUTSIDE the codomain:** `-1` is a *legal answer* here, so `mem[a] == -1` meaning "not computed" makes every impossible sub-amount recompute forever ⇒ exponential, and it surfaces as **TLE, not WA** — the nastiest failure mode. Use `-2`, a parallel `visited`, or keep the ∞ convention internally and translate once at the return. **Free credit: this is literally BFS.** Treat amounts `0…amount` as nodes with an edge `a → a + c` per coin; every edge costs exactly one coin, so **"fewest coins" = shortest path in an unweighted graph** and BFS from `0` returns the level at which `amount` is first reached — same `O(amount·n)` worst case but it **exits early** instead of filling the table, and it explains the DP for free (the BFS levels **are** the dp values, and the increasing-`a` sweep is that DAG's topological order). Give coins unequal costs and it becomes **Dijkstra** — which is what a `min`-DP already is. **Edges:** `amount == 0` → **`0`** for any coin set (the empty selection is **legal** — contrast [[6-53-maximum-subarray\|53]], matching [[8-121-best-time-to-buy-and-sell-stock\|121]]) · `[2]` with `amount = 3` → `-1` by **parity**, not difficulty · every coin larger than the amount → `-1` · the table is sized **`amount + 1`** because the answer lives at index `amount` — [[2-746-min-cost-climbing-stairs\|746]]'s "top of the floor" off-by-one wearing a new hat. Ladder: naive recursion **O(n^amount)** — name it, kill it → memoized top-down **O(amount·n)/O(amount)** (+ up to `10⁴` stack frames when `coins = [1]`) → **bottom-up table O(amount·n)/O(amount)** (the expected answer; `10⁴ × 12 ≈ 1.2×10⁵` ops) ≡ **BFS, identical bound with an early exit** → sorted-descending DFS with the `curCount + ceil(rem/c) >= best` prune (exponential worst case yet the fastest *accepted* solution in practice — mention it, never lead with it). **No O(1)-space form exists**, and noticing that is itself the insight. Follow-ups: recover the actual coins → store `from[a] = c` on every improvement and walk back from `amount` · count **combinations** → **518** · count **permutations** → **377** · the boolean version → **139 Word Break** (`min` → `\|\|`, with dictionary words for coins — the next problem on this roadmap) · a fixed coin set of perfect squares → **279** (an answer always exists; Lagrange caps it at 4) · `+1` → `+cost[c]` → **983 Minimum Cost For Tickets** · each coin usable **once** → 0/1 knapsack, amount loop **downward** (**416**, **494**) · `amount` up to `10⁹` → the pseudo-polynomial table dies and it stops being this problem. Textbook name: **the change-making problem** — NP-hard in the *number of bits* of `amount`, pseudo-polynomial `O(amount·n)` here, and the canonical illustration of *"greedy is optimal only for canonical coin systems"*. **Eight dials now:** **70** combine operator `+` · **746** weight placement · **91** guards · **198** branch asymmetry · **213** domain · **53** **state definition** · **152** **operator algebra** · **121** **the input transform** · **322** **what the state MEASURES** — position → value |
| ☐ |      | 300 | [Longest Increasing Subsequence](https://leetcode.com/problems/longest-increasing-subsequence/) | 🟡 |  |
| ☐ |      | 139 | [Word Break](https://leetcode.com/problems/word-break/) | 🟡 |  |
| ✅ | Aug 18 | 91  | [Decode Ways](https://leetcode.com/problems/decode-ways/) | 🟡 | "**how many ways** to decode a **digit string**" under `"1"→'A' … "26"→'Z'`, where "**some codes are contained in other codes**" (`"2"`/`"5"` vs `"25"`) → **[[1-70-climbing-stairs\|70 Climbing Stairs]] with the branches made CONDITIONAL**: identical two-back dependency window (`i-1`, `i-2`) and identical **`+` combine** (it's a **count of groupings**, so `+`), the sole change being that each term is **gated by a validity predicate** rather than always firing. `dp[i]` = ways to decode the **first `i` characters** ⇒ `dp[i] = dp[i-1]·[s[i-1] ≠ '0'] + dp[i-2]·[10 ≤ s[i-2..i-1] ≤ 26]`, base **`dp[0] = 1`** (the empty string decodes exactly one way — the identity of `+`, same rule as 70) and `dp[1] = (s[0] != '0')`, answer **`dp[n]`**. **The recurrence is free; the guards are the entire problem.** **Zeros ARE the problem — the one sentence to say out loud:** a `0` **cannot stand alone** and **cannot lead** a two-digit code, so the only way a `0` ever survives is as the **second digit of `10` or `20`**. Every test case reduces to that: `"30"` → 0, `"100"` → 0 (both `1\|00` and `10\|0` strand a zero), `"101"` → 1 (`10\|1`), `"10"` → 1, `"27"` → 1 (`2\|7` only, since `27 > 26`), `"06"` → 0, `"11106"` → 2 (`1\|1\|10\|6` and `11\|10\|6`, **never** `1\|11\|06`). **Signature bug — writing the two-digit guard as `two <= 26` only:** it accepts `"06"` (`two = 6`) and silently **overcounts**; the check is a **range**, `10 <= two && two <= 26`, and the **lower bound *is* the leading-zero rule** expressed arithmetically. That is precisely why Example 3 exists. **Never bolt on an explicit "if undecodable return 0"** — when both guards fail, `dp[i]` simply stays `0` and that zero propagates forward through every later term on its own; the impossibility handles itself. **The window is 2 ⇒ two scalars:** `prev2 = prev1 = 1; for (i = 2; i <= n; i++) { cur = 0; if (s[i-1] != '0') cur += prev1; int two = (s[i-2]-'0')*10 + (s[i-1]-'0'); if (two >= 10 && two <= 26) cur += prev2; prev2 = prev1; prev1 = cur; } return prev1;` — the temporary `cur` is mandatory, and `n == 1` falls out free (loop never runs). **Top-down / suffix variant:** `ways(i) = [valid 1-char]·ways(i+1) + [valid 2-char]·ways(i+2)` with `ways(n) = 1`; a unified `valid()` helper is a tidy way to express the rule (for one char `stoi ≤ 9` is always `≤ 26`, so only the leading-zero test bites; for two chars **both** bite). Two traps there: `mem` sized `n` is in-bounds **only because the `index == n` base case returns before the `mem[index]` line** — reorder those and it's an OOB read (**the same trap as 70**); and `s.substr(...)` **heap-allocates a `std::string` on every check** while `stoi` re-parses it, so swap both for digit arithmetic (`const string&`, not by-value, if you keep the helper). Ladder: naive recursion **O(2ⁿ)/O(n)** — the same suffix recomputed from both branches, `Fib(101)` calls on all-`1`s at `n = 100` → **memoized top-down O(n)/O(n)** (the mechanical translation; the right first move when the guards feel fiddly) → **bottom-up table O(n)/O(n)** → **two rolling variables O(n)/O(1)**, the expected final answer. **Overflow:** the problem *states* the answer fits in a 32-bit int — note that you checked and move on; `long long` here is reflex, not reasoning. Follow-ups: "**return the decodings themselves**" → no longer O(n)-countable, output is exponential ⇒ backtrack; "the string may contain **`*`** as a wildcard `1..9`" → **639 Decode Ways II**, same skeleton but each guard becomes a **weighted** count (`*` → 9 single-digit ways, `1*` → 9, `2*` → 6) mod `10⁹+7` — the classic hard follow-up; "**is it decodable at all**" → swap `+` for `\|\|` and you are at **139 Word Break**; "mapping runs to `'ZZ'` (1..702)" → window widens to 3. **The 70 / 746 / 91 / 198 quartet to distinguish out loud** — same two-back window, four merge rules: **70** `dp[i-1] + dp[i-2]` (`+` counts, branches **unconditional**) · **746** `min(dp[i-1] + w, dp[i-2] + w)` (`min` optimises, **both** branches pay) · **91** `dp[i-1] + dp[i-2]` (`+` counts, branches **conditional on a predicate**) · **198** `max(dp[i-1], dp[i-2] + nums[i])` (`max` optimises, branches **asymmetric** — one skips the weight). Name the combine operator, the weight placement and the guard, and you have named the problem. *(Done as a **Mixed-Practice drill** — cold, out of pattern context.)* |
| ☐ |      | 416 | [Partition Equal Subset Sum](https://leetcode.com/problems/partition-equal-subset-sum/) | 🟡 |  |

---

# 15. Dynamic Programming — 2D / Grid / Strings

**When to use it (recognition cues):**
- Two strings → edit distance, LCS, regex match.
- Grid paths / unique paths / min path sum.
- Palindromic substrings / partitions.

**Core idea:** `dp[i][j]` = answer for prefix-i of one input and prefix-j of the other (or row i, col j of grid).

**Template (C++):**
```cpp
vector<vector<int>> dp(m + 1, vector<int>(n + 1, 0));
for (int i = 1; i <= m; ++i)
    for (int j = 1; j <= n; ++j)
        dp[i][j] = /* recurrence using dp[i-1][j], dp[i][j-1], dp[i-1][j-1] */;
return dp[m][n];
```

**Problems:**

| ✓ | Date | # | Problem | Diff | Trigger phrase |
|---|------|---|---------|------|----------------|
| ☐ |      | 62  | [Unique Paths](https://leetcode.com/problems/unique-paths/) | 🟡 |  |
| ☐ |      | 63  | [Unique Paths II](https://leetcode.com/problems/unique-paths-ii/) | 🟡 |  |
| ☐ |      | 64  | [Minimum Path Sum](https://leetcode.com/problems/minimum-path-sum/) | 🟡 |  |
| ☐ |      | 1143| [Longest Common Subsequence](https://leetcode.com/problems/longest-common-subsequence/) | 🟡 |  |
| ☐ |      | 72  | [Edit Distance](https://leetcode.com/problems/edit-distance/) | 🟡 |  |
| ☐ |      | 583 | [Delete Operation for Two Strings](https://leetcode.com/problems/delete-operation-for-two-strings/) | 🟡 |  |
| ☐ |      | 5   | [Longest Palindromic Substring](https://leetcode.com/problems/longest-palindromic-substring/) | 🟡 |  |
| ☐ |      | 647 | [Palindromic Substrings](https://leetcode.com/problems/palindromic-substrings/) | 🟡 |  |
| ☐ |      | 516 | [Longest Palindromic Subsequence](https://leetcode.com/problems/longest-palindromic-subsequence/) | 🟡 |  |
| ☐ |      | 221 | [Maximal Square](https://leetcode.com/problems/maximal-square/) | 🟡 |  |
| ☐ |      | 10  | [Regular Expression Matching](https://leetcode.com/problems/regular-expression-matching/) | 🔴 |  |
| ☐ |      | 312 | [Burst Balloons](https://leetcode.com/problems/burst-balloons/) | 🔴 |  |

---

# 16. Tries & Union-Find

**When to use it (recognition cues):**
- Prefix lookup / autocomplete / dictionary search → **Trie**.
- "Number of provinces", "redundant connection", dynamic connectivity → **Union-Find**.
- Word search with many queries.

**Core idea (Trie):** Tree of characters with children map + isEnd flag.
**Core idea (Union-Find):** `parent[]` + `rank[]`; path compression on `find`, union by rank.

**Template (C++) — Union-Find:**
```cpp
struct DSU {
    vector<int> p, r;
    DSU(int n): p(n), r(n, 0) { iota(p.begin(), p.end(), 0); }
    int find(int x) { return p[x] == x ? x : p[x] = find(p[x]); }
    bool unite(int a, int b) {
        a = find(a); b = find(b);
        if (a == b) return false;
        if (r[a] < r[b]) swap(a, b);
        p[b] = a; if (r[a] == r[b]) ++r[a];
        return true;
    }
};
```

**Problems:**

| ✓ | Date | # | Problem | Diff | Trigger phrase |
|---|------|---|---------|------|----------------|
| ☐ |      | 208 | [Implement Trie (Prefix Tree)](https://leetcode.com/problems/implement-trie-prefix-tree/) | 🟡 |  |
| ☐ |      | 211 | [Design Add and Search Words Data Structure](https://leetcode.com/problems/design-add-and-search-words-data-structure/) | 🟡 |  |
| ☐ |      | 648 | [Replace Words](https://leetcode.com/problems/replace-words/) | 🟡 |  |
| ☐ |      | 677 | [Map Sum Pairs](https://leetcode.com/problems/map-sum-pairs/) | 🟡 |  |
| ☐ |      | 1268| [Search Suggestions System](https://leetcode.com/problems/search-suggestions-system/) | 🟡 |  |
| ☐ |      | 212 | [Word Search II](https://leetcode.com/problems/word-search-ii/) | 🔴 |  |
| ☐ |      | 547 | [Number of Provinces](https://leetcode.com/problems/number-of-provinces/) | 🟡 |  |
| ☐ |      | 684 | [Redundant Connection](https://leetcode.com/problems/redundant-connection/) | 🟡 |  |
| ☐ |      | 990 | [Satisfiability of Equality Equations](https://leetcode.com/problems/satisfiability-of-equality-equations/) | 🟡 |  |
| ☐ |      | 721 | [Accounts Merge](https://leetcode.com/problems/accounts-merge/) | 🟡 |  |
| ☐ |      | 305 | [Number of Islands II](https://leetcode.com/problems/number-of-islands-ii/) | 🔴 |  |
| ☐ |      | 952 | [Largest Component Size by Common Factor](https://leetcode.com/problems/largest-component-size-by-common-factor/) | 🔴 |  |

---

# 17. Bit Manipulation

**When to use it (recognition cues):**
- "Find the one number that appears once / odd times" → XOR.
- Count bits, swap without temp, check power of two, build from subsets via bitmask.
- Add/subtract without `+`/`-`, or O(1) extra space on a fixed-width integer.

**Core idea:** XOR cancels pairs (`a^a=0`); `n & (n-1)` drops the lowest set bit; shift + mask to read/write individual bits.

**Template (C++):**
```cpp
int x = 0;
for (int v : nums) x ^= v;        // lone element survives
int bits = __builtin_popcount(n); // count set bits
bool isPow2 = n > 0 && !(n & (n-1));
```

**Problems:**

| ✓ | Date | # | Problem | Diff | Trigger phrase |
|---|------|---|---------|------|----------------|
| ☐ |      | 136 | [Single Number](https://leetcode.com/problems/single-number/) | 🟢 | Every element twice except one → XOR all |
| ☐ |      | 338 | [Counting Bits](https://leetcode.com/problems/counting-bits/) | 🟢 | bits[i] = bits[i>>1] + (i&1) DP |
| ☐ |      | 191 | [Number of 1 Bits](https://leetcode.com/problems/number-of-1-bits/) | 🟢 | Pop lowest set bit with n & (n-1) |
| ☐ |      | 371 | [Sum of Two Integers](https://leetcode.com/problems/sum-of-two-integers/) | 🟡 | Add without + : XOR = sum, AND<<1 = carry |
| ☐ |      | 190 | [Reverse Bits](https://leetcode.com/problems/reverse-bits/) | 🟢 | Shift out of n, shift into result 32× |

---

# 18. Matrix Simulation

**When to use it (recognition cues):**
- Rotate / transpose / spiral-traverse a 2-D grid.
- Mark-then-sweep state changes on a board (in-place flags).
- "Do it in O(1) extra space" on a matrix.

**Core idea:** Layer-by-layer or boundary pointers (top/bottom/left/right); use the matrix itself (first row/col, or sign bits) as the marker to hit O(1) space.

**Template (C++):**
```cpp
// transpose then reverse each row = rotate 90° clockwise
for (int i = 0; i < n; i++)
    for (int j = i+1; j < n; j++) swap(m[i][j], m[j][i]);
for (auto& row : m) reverse(row.begin(), row.end());
```

**Problems:**

| ✓ | Date | # | Problem | Diff | Trigger phrase |
|---|------|---|---------|------|----------------|
| ☐ |      | 48  | [Rotate Image](https://leetcode.com/problems/rotate-image/) | 🟡 | Rotate 90° in place → transpose + reverse rows |
| ☐ |      | 54  | [Spiral Matrix](https://leetcode.com/problems/spiral-matrix/) | 🟡 | Four shrinking boundaries top/bottom/left/right |
| ☐ |      | 73  | [Set Matrix Zeroes](https://leetcode.com/problems/set-matrix-zeroes/) | 🟡 | O(1) space: use row0/col0 as zero markers |
| ☐ |      | 289 | [Game of Life](https://leetcode.com/problems/game-of-life/) | 🟡 | Encode old+new state in 2 bits, sweep twice |

---

# 19. Design

**When to use it (recognition cues):**
- "Implement a class with O(1) `get`/`put`/`insert`/`remove`."
- Cache eviction (LRU/LFU), random-access set, custom hash map.
- Combine two structures (hash map + linked list / heap) for the right complexities.

**Core idea:** Pick the pair of structures whose strengths cover every required operation in O(1)/O(log n): hash map for lookup + doubly linked list (order) or heap (priority) or array+swap (random).

**Problems:**

| ✓ | Date | # | Problem | Diff | Trigger phrase |
|---|------|---|---------|------|----------------|
| ✅ | Aug 18 | 146 | [LRU Cache](https://leetcode.com/problems/lru-cache/) | 🟡 | "**least recently used**" + "**evict**" + an explicit "**`get`/`put` in O(1) average**" → hash map (lookup) + **doubly** linked list (recency) with head/tail **sentinels**; front = MRU, `tail->prev` = victim. **Node stores the KEY** so eviction can `mpp.erase(victim->key)` in O(1) (else an O(n) map scan) — copy/erase it **before** `delete`. `put` on an existing key = **update + move-to-front, never evict**. Doubly because unlinking needs the predecessor |
| ☐ |      | 460 | [LFU Cache](https://leetcode.com/problems/lfu-cache/) | 🔴 | Freq → DLL buckets + key→node map, track minFreq |
| ☐ |      | 380 | [Insert Delete GetRandom O(1)](https://leetcode.com/problems/insert-delete-getrandom-o1/) | 🟡 | Array + value→index map; swap-with-last to delete |
| ☐ |      | 706 | [Design HashMap](https://leetcode.com/problems/design-hashmap/) | 🟢 | Buckets + chaining; modulo hash |

---

# 20. Math / String

**When to use it (recognition cues):**
- Implement an arithmetic/string primitive: power, sqrt, parse an integer, convert numerals.
- Cycle detection on digit transforms (Happy Number → fast/slow).
- Edge-case-heavy parsing (overflow, sign, whitespace).

**Core idea:** Fast exponentiation (halve the power), binary search for sqrt, careful state machine for parsing; watch INT overflow and sign.

**Template (C++):**
```cpp
double myPow(double x, long n) {       // fast power
    if (n < 0) { x = 1/x; n = -n; }
    double r = 1;
    while (n) { if (n & 1) r *= x; x *= x; n >>= 1; }
    return r;
}
```

**Problems:**

| ✓ | Date | # | Problem | Diff | Trigger phrase |
|---|------|---|---------|------|----------------|
| ☐ |      | 50  | [Pow(x, n)](https://leetcode.com/problems/powx-n/) | 🟡 | Fast exponentiation by squaring |
| ☐ |      | 69  | [Sqrt(x)](https://leetcode.com/problems/sqrtx/) | 🟢 | Binary search on answer |
| ☐ |      | 202 | [Happy Number](https://leetcode.com/problems/happy-number/) | 🟢 | Digit-square cycle → fast/slow pointers |
| ☐ |      | 8   | [String to Integer (atoi)](https://leetcode.com/problems/string-to-integer-atoi/) | 🟡 | Skip ws, sign, clamp to INT range |
| ☐ |      | 13  | [Roman to Integer](https://leetcode.com/problems/roman-to-integer/) | 🟢 | Add, but subtract when smaller precedes larger |

---

# 🎲 Mixed Practice — Pattern Recognition Drill

> Do this **after** you complete all 20 patterns above. For each problem, **predict the pattern** before expanding the answer. Aim for a 60-second decision.

1. [Longest Substring Without Repeating Characters](https://leetcode.com/problems/longest-substring-without-repeating-characters/) — 🟡  <details><summary>Pattern</summary>Sliding Window</details>
2. [Word Ladder](https://leetcode.com/problems/word-ladder/) — 🔴  <details><summary>Pattern</summary>Graph BFS</details>
3. [House Robber](https://leetcode.com/problems/house-robber/) — 🟡  <details><summary>Pattern</summary>DP — 1D</details>
4. [Linked List Cycle II](https://leetcode.com/problems/linked-list-cycle-ii/) — 🟡  <details><summary>Pattern</summary>Fast & Slow Pointers</details>
5. [Course Schedule II](https://leetcode.com/problems/course-schedule-ii/) — 🟡  <details><summary>Pattern</summary>Topological Sort (Graph)</details>
6. [Find Median from Data Stream](https://leetcode.com/problems/find-median-from-data-stream/) — 🔴  <details><summary>Pattern</summary>Two Heaps</details>
7. [Daily Temperatures](https://leetcode.com/problems/daily-temperatures/) — 🟡  <details><summary>Pattern</summary>Monotonic Stack</details>
8. [Subsets II](https://leetcode.com/problems/subsets-ii/) — 🟡  <details><summary>Pattern</summary>Backtracking</details>
9. [Koko Eating Bananas](https://leetcode.com/problems/koko-eating-bananas/) — 🟡  <details><summary>Pattern</summary>Binary Search on Answer</details>
10. [Merge Intervals](https://leetcode.com/problems/merge-intervals/) — 🟡  <details><summary>Pattern</summary>Greedy / Intervals</details>
11. [Subarray Sum Equals K](https://leetcode.com/problems/subarray-sum-equals-k/) — 🟡  <details><summary>Pattern</summary>Prefix Sum + Hashing</details>
12. [Edit Distance](https://leetcode.com/problems/edit-distance/) — 🟡  <details><summary>Pattern</summary>DP — 2D</details>
13. [Container With Most Water](https://leetcode.com/problems/container-with-most-water/) — 🟡  <details><summary>Pattern</summary>Two Pointers</details>
14. [Number of Provinces](https://leetcode.com/problems/number-of-provinces/) — 🟡  <details><summary>Pattern</summary>Union-Find</details>
15. [Binary Tree Maximum Path Sum](https://leetcode.com/problems/binary-tree-maximum-path-sum/) — 🔴  <details><summary>Pattern</summary>Tree DFS</details>
16. [Right Side View](https://leetcode.com/problems/binary-tree-right-side-view/) — 🟡  <details><summary>Pattern</summary>Tree BFS</details>
17. [Reverse Nodes in k-Group](https://leetcode.com/problems/reverse-nodes-in-k-group/) — 🔴  <details><summary>Pattern</summary>Linked List Manipulation</details>
18. [Top K Frequent Elements](https://leetcode.com/problems/top-k-frequent-elements/) — 🟡  <details><summary>Pattern</summary>Heap / Top-K</details>
19. [Word Search II](https://leetcode.com/problems/word-search-ii/) — 🔴  <details><summary>Pattern</summary>Trie + Backtracking</details>
20. [Longest Palindromic Substring](https://leetcode.com/problems/longest-palindromic-substring/) — 🟡  <details><summary>Pattern</summary>DP — 2D (or expand-around-center)</details>
21. [Min Stack](https://leetcode.com/problems/min-stack/) — 🟡  <details><summary>Pattern</summary>Stack</details>
22. [Minimum Window Substring](https://leetcode.com/problems/minimum-window-substring/) — 🔴  <details><summary>Pattern</summary>Sliding Window</details>
23. [Jump Game II](https://leetcode.com/problems/jump-game-ii/) — 🟡  <details><summary>Pattern</summary>Greedy</details>
24. [Validate BST](https://leetcode.com/problems/validate-binary-search-tree/) — 🟡  <details><summary>Pattern</summary>Tree DFS</details>
25. [Search in Rotated Sorted Array](https://leetcode.com/problems/search-in-rotated-sorted-array/) — 🟡  <details><summary>Pattern</summary>Binary Search</details>
26. [Number of Islands](https://leetcode.com/problems/number-of-islands/) — 🟡  <details><summary>Pattern</summary>Graph DFS/BFS</details>
27. [Coin Change](https://leetcode.com/problems/coin-change/) — 🟡  <details><summary>Pattern</summary>DP — 1D</details>
28. [3Sum](https://leetcode.com/problems/3sum/) — 🟡  <details><summary>Pattern</summary>Two Pointers</details>
29. [Permutations](https://leetcode.com/problems/permutations/) — 🟡  <details><summary>Pattern</summary>Backtracking</details>
30. [K Closest Points to Origin](https://leetcode.com/problems/k-closest-points-to-origin/) — 🟡  <details><summary>Pattern</summary>Heap</details>
31. [Trapping Rain Water](https://leetcode.com/problems/trapping-rain-water/) — 🔴  <details><summary>Pattern</summary>Two Pointers (or Monotonic Stack)</details>
32. [Implement Trie](https://leetcode.com/problems/implement-trie-prefix-tree/) — 🟡  <details><summary>Pattern</summary>Trie</details>
33. [Decode Ways](https://leetcode.com/problems/decode-ways/) — 🟡  <details><summary>Pattern</summary>DP — 1D</details>
34. [Find Peak Element](https://leetcode.com/problems/find-peak-element/) — 🟡  <details><summary>Pattern</summary>Binary Search</details>
35. [Lowest Common Ancestor](https://leetcode.com/problems/lowest-common-ancestor-of-a-binary-tree/) — 🟡  <details><summary>Pattern</summary>Tree DFS</details>
36. [Rotting Oranges](https://leetcode.com/problems/rotting-oranges/) — 🟡  <details><summary>Pattern</summary>Multi-source BFS</details>
37. [Maximum Subarray](https://leetcode.com/problems/maximum-subarray/) — 🟡  <details><summary>Pattern</summary>DP — 1D (Kadane)</details>
38. [Generate Parentheses](https://leetcode.com/problems/generate-parentheses/) — 🟡  <details><summary>Pattern</summary>Backtracking</details>
39. [Largest Rectangle in Histogram](https://leetcode.com/problems/largest-rectangle-in-histogram/) — 🔴  <details><summary>Pattern</summary>Monotonic Stack</details>
40. [LRU Cache](https://leetcode.com/problems/lru-cache/) — 🟡  <details><summary>Pattern</summary>Hash Map + Doubly Linked List</details>

---

# 📈 Progress Tracker

**Current streak:** 0 days · **Longest streak:** 0 days · **Total solved:** 0 / ~200

### Weekly review (Sundays — redo 1 random problem from the past week)

| Week | Date | Pattern in focus | Problems solved | Re-solved problem |
|------|------|------------------|-----------------|-------------------|
| 1    |      | Two Pointers     | / 7             |                   |
| 2    |      | Two Pointers + Sliding Window | / 7 |                   |
| 3    |      | Sliding Window + Fast/Slow | / 7 |                   |
| 4    |      | Prefix Sum       | / 7             |                   |
| …    |      |                  |                 |                   |

> Tip: At the end of each week, scan the **Trigger phrase** column you filled in. If two patterns share similar trigger phrases, that's a sign you need to refine them — pattern recognition lives in the *differences*.

---

# 🧭 Daily routine cheat sheet

```
06:30 - 08:30  Mornings:     DSA daily problem (May 12 → Sep 21) → LLD problem (Sep 22 → Nov 2) → DSA upkeep + mocks (Nov 3+)
08:30 - 10:00  Commute out:  LLD concept videos → switches to HLD videos (Sep 22+)
10:00 - 20:30  Office
20:30 - 22:00  Commute back: continue LLD/HLD videos
22:00 - 23:30  Eve (non-class): Project work (3 nights/wk × 1.5 hr)
Class evenings (4 nights/wk): Class — recovery
Saturday + Sunday: Project (~5 hr each) + 1 weekly DSA review slot
1 full off-period/week: Sunday afternoon (mandatory)
```

**Red lines** (never break):
- DSA morning slot is sacred until Sep 21 — never lose to project work.
- One full off-period/week — burnout is the #1 reason 6-month plans fail.
- Project work only in evenings/weekends — never push it into DSA time.
