# Graph — Index

60-second whole-pattern recall. Read the **Trigger** column and try to recall the **Idea** before peeking.

**Core skeleton (grid flood-fill):** scan every cell; on an unvisited land cell `++count` and DFS/BFS-sink its whole component so it's never re-counted. **Mark visited on discovery, not on pop.** BFS/queue when recursion depth (up to `m·n`) could overflow; Union-Find when components merge dynamically.

| # | Problem | LC | Diff | Trigger | Idea | TC | SC |
|---|---------|----|------|---------|------|----|----|
| 1 | [Number of Islands](https://leetcode.com/problems/number-of-islands/) | 200 | M | "count connected blobs/regions of `1`s in a grid" | Grid = implicit graph, **#islands = #connected components**. Scan cells; each unvisited land cell is a fresh component → `++count`, then flood-fill (DFS sink to `'0'`, or BFS) every 4-adjacent land cell so the rest of that island is absorbed. **Mark visited on entry/push**, never on pop (else double-count/TLE). 4 dirs only — diagonals don't connect. 300×300 all-land ⇒ 90k-deep DFS can blow the stack → prefer BFS. Dynamic-add variant → Union-Find (305) | O(m·n) | O(m·n) |
| 2 | [Max Area of Island](https://leetcode.com/problems/max-area-of-island/) | 695 | M | "largest island / biggest connected region / max area of `1`s" | **Same scaffold as 200, but the flood returns a *size*.** DFS seeds `area = 1` for the current cell and **adds children's returns** (`1 + Σ dfs(neighbour)`); outer loop keeps `max` over every island. "Count components" → "measure each, keep the biggest." Mark visited on entry so each cell counts once; seed `maxArea = 0` (no-island answer). Sink-in-place = O(1) aux; `visited` grid = non-destructive. 50×50 so depth safe | O(m·n) | O(m·n) |
| 3 | [Rotting Oranges](https://leetcode.com/problems/rotting-oranges/) | 994 | M | "every minute all rotten cells infect neighbours **simultaneously**; min minutes till none fresh" | **NOT DFS — multi-source BFS, level = time.** Seed queue with **all** rotten cells (minute-0 frontier) + count `fresh`; expand **ring by ring**, each drained ring = 1 minute. Freeze `sz = q.size()` before draining so this minute's cells don't mix with next. Mark rotten **on discovery** (`grid=2`, the visited marker — `==` vs `=` bug = infinite loop). `fresh==0` up front → `0`; end `fresh>0` → `-1` (unreachable). DFS fails: sequential spread + first-writer-wins mis-times cells | O(m·n) | O(m·n) |
| 4 | [Surrounded Regions](https://leetcode.com/problems/surrounded-regions/) | 130 | M | "capture regions **fully enclosed** by `X`; a region touching an **edge** survives" | **Invert the problem: flood from the border, not the interior.** An `O` is *safe* iff connected to a border `O`. Seed BFS/DFS from every border `'O'`, tag reachable ones with a temp marker `'#'`; then one sweep: untouched `'O'`→`'X'` (captured), `'#'`→`'O'` (restore). Marker = the visited set (no extra grid). Prefer BFS at 200×200 — deep DFS can overflow. Same border-flood trick as Pacific Atlantic (417) | O(m·n) | O(m·n) |
| 5 | [Clone Graph](https://leetcode.com/problems/clone-graph/) | 133 | M | "**deep copy** a graph with **cycles / shared neighbours**" | **DFS + `oldToNew` map (Node*→clone).** Undirected ⇒ cycles everywhere, so memoize: on first visit make `new Node(val)` and **store it in the map BEFORE recursing** on neighbours — that entry is both the memo *and* the cycle-breaker (store *after* the loop → infinite recursion). Recurse each neighbour, link into `copy->neighbors`. `node? clone : nullptr` for empty graph. Key by pointer, not val. BFS variant = same map, queue instead of stack | O(V+E) | O(V) |
| 6 | [Course Schedule](https://leetcode.com/problems/course-schedule/) | 207 | M | "given **prerequisites / dependencies**, can you finish all / is there a valid order?" | **Topological sort = cycle detection (Kahn's BFS).** Edge `b → a` for each `[a,b]` (b before a); `indegree[a]++`. Seed queue with all **indegree-0** nodes (no prereqs), peel one at a time (`count++`), decrement each dependent's indegree, push when it hits 0. `count == numCourses` ⇒ acyclic ⇒ `true`; a cycle never reaches indegree 0 so it's left out. You need the *count*, not the order. Alt: DFS 3-color, in-progress neighbour = back edge = cycle | O(V+E) | O(V+E) |
| 7 | [Course Schedule II](https://leetcode.com/problems/course-schedule-ii/) | 210 | M | "output **a valid order** to do everything (empty if impossible)" | **Same `b → a` DAG as 207 — now emit the order.** **Kahn's BFS:** append each node **on pop** (`topo.push_back` when dequeued) — the pop sequence *is* the topo order; `topo.size() != numCourses` ⇒ cycle ⇒ `{}`. **DFS alt:** 3-color, collect **post-order** (push on finish), then **reverse** it (prereqs finish first); back edge (in-progress neighbour) ⇒ cycle ⇒ `{}`. BFS = append-on-pop, no reverse; DFS = post-order + reverse. Binary visited can't detect cycles — DFS needs the in-progress color | O(V+E) | O(V+E) |
| 8 | [Is Graph Bipartite?](https://leetcode.com/problems/is-graph-bipartite/) | 785 | M | "split nodes into **two groups** so **every edge crosses** between them (no edge inside a group)" | **Bipartite ⇔ 2-colorable ⇔ no odd cycle.** Color a start node `0`, force each neighbour to the **opposite** color (`!color`); a neighbour **already carrying your color** = odd cycle ⇒ `false`. `colors` init `-1` (uncolored ≠ color). **Loop over all nodes** — graph is **disconnected**, seed each component. Conflict test is *same-color*, not just "visited" (opposite color is fine). DFS (recursive, small) or BFS (iterative, no depth risk) — both O(V+E). Even cycles OK; only **odd** cycles break it. Alt: Union-Find (node same set as own neighbour ⇒ not bipartite) | O(V+E) | O(V) |
| 9 | [Pacific Atlantic Water Flow](https://leetcode.com/problems/pacific-atlantic-water-flow/) | 417 | M | "cells from which water reaches **both** oceans / two targets on opposite borders" | **Reverse the flow + intersect two floods.** Simulating each cell → ocean is O((m·n)²) TLE. Instead flood **inward from each ocean's border**, keeping only equal-or-**higher** neighbours (guard `heights[r][c] < prevH → return`) — those cells can drain *into* that ocean. Two visited grids `pac`/`atl` (a cell may reach both); seed Pacific from top+left, Atlantic from bottom+right, each with its **own height**. Answer = cells with `pac && atl`. **Bounds-check before index**; DFS or BFS (no levels needed, order irrelevant → BFS only for stack safety at 200×200). Same border-anchored inward flood as Surrounded Regions (130) | O(m·n) | O(m·n) |
| 10 | [Shortest Path in Binary Matrix](https://leetcode.com/problems/shortest-path-in-binary-matrix/) | 1091 | M | "shortest clear path corner→corner, move **8-directionally**, count **cells**" | **Single-source BFS, unweighted grid.** Seed `{(0,0), 1}` (length = cells, so start at **1**), expand all **8** neighbours ring by ring; first *pop* of `(n-1,n-1)` = shortest. Guard blocked start/end → `-1` up front; **mark visited on push**; bounds before value; queue drains → `-1`. **DFS + backtracking is exponential ⇒ TLE** and no memo fixes it (shortest dist depends on the path's visited set) — shortest path + unweighted ⇒ BFS, always. A\*/bidirectional BFS = "beat BFS" answers, overkill at n≤100 | O(n²) | O(n²) |
| 11 | [Word Ladder](https://leetcode.com/problems/word-ladder/) | 127 | H | "transform word→word one letter at a time, each step a dictionary word; **length of shortest** chain" | **BFS on an *implicit* unweighted graph** (words = nodes, one-letter edits = edges). Don't build the graph or compare pairs (O(N²·L)); generate neighbours by **mutating each position over 'a'–'z'** and hash-looking-up — O(26·L) per word. The **set is dictionary + visited**: `erase` a word on enqueue (not pop → else TLE). Length counts **words**, start level at **1**; `endWord ∉ set` → `0` up front; first *pop* of endWord = answer. Optimize with **bidirectional BFS** (grow both ends, expand smaller frontier). Word Ladder II (126) = reconstruct all paths | O(N·L²·26) | O(N·L) |
| 12 | [Alien Dictionary](https://leetcode.com/problems/alien-dictionary/) | 269 | H | "words **sorted** in an **unknown alphabet**; recover the **letter order**" | **Derive a DAG from sorted words, then topo-sort (Kahn's).** Only **adjacent** word pairs constrain order, and each gives **one** edge — the **first differing char**: `s1[j] → s2[j]`, then break. Seed queue with **present** indegree-0 letters, peel & append; not all peeled ⇒ **cycle** ⇒ `""`. Two silent traps: **prefix rule** — `s1` longer than `s2` with `s2` a prefix (`["abc","ab"]`) ⇒ **invalid** `""`; and **index by letter `ch-'a'` (0–25), size to 26** — sizing by "distinct count" but indexing by letter is OOB UB. Only present letters go in the answer. Same engine as 207/210, new part = *building* the edges | O(N·L+V+E) | O(V+E) |

## When to reach for Graph
1. **Grid / matrix** with 4- or 8-directional movement → flood-fill (islands, regions, rotting), often BFS for shortest/level.
2. **Connected components / bipartite / cycle** detection → DFS or Union-Find.
3. **Dependencies / ordering / "course schedule"** → topological sort (Kahn's BFS or DFS post-order).
4. **Shortest path in an *unweighted* graph** → plain BFS; weighted → Dijkstra/Bellman-Ford (breadth block).

## Common Pitfalls Across the Pattern
- **Mark visited on discovery** (enqueue/enter), not when you dequeue — the classic re-count / blow-up bug.
- **Bounds before index** — order the guard so out-of-range short-circuits before `grid[nr][nc]`.
- **Recursion depth** — DFS on large grids/graphs can overflow the native stack; offer BFS or an explicit stack.
- **Chars vs ints in grids** — compare against `'1'`/`'0'`, not `1`/`0`.
