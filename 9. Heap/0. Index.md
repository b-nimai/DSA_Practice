# Heap — Index

60-second whole-pattern recall. Read the **Trigger** column and try to recall the **Idea** before peeking.

**Core skeleton (Top-K):** to keep the **k largest**, maintain a **min-heap of size k** — push each element, pop when size exceeds k; the top is the kth largest. Flip to a **max-heap of size k** for the k smallest. Heap ops are O(log k), space O(k). For a static array, **quickselect / `nth_element`** beats the heap at average O(n).

| # | Problem | LC | Diff | Trigger | Idea | TC | SC |
|---|---------|----|------|---------|------|----|----|
| 1 | [Kth Largest Element in an Array](https://leetcode.com/problems/kth-largest-element-in-an-array/) | 215 | M | "kth **largest** value (dupes count), avoid sorting / works on a stream" | **Size-K min-heap holds the k largest so far → top = kth largest.** Push each `x`; if `size() > k` pop the min (evicts current smallest of the top-k). O(n log k)/O(k), and streams naturally. For a **static** array, **quickselect**/`nth_element` around index **n-k** is average **O(n)** (randomize pivot; worst O(n²)). Brute = sort, `nums[n-k]`, O(n log n). Gotchas: min-heap for *largest* (max-heap for smallest); cap heap at **k not n**; `size()` is unsigned (cast k); kth largest ≠ kth distinct | O(n log k) | O(k) |
| 2 | [Kth Largest Element in a Stream](https://leetcode.com/problems/kth-largest-element-in-a-stream/) | 703 | E | "class returning the **kth largest so far** after each new value / live cut-off" | **215 made online** — keep the size-K min-heap **as a member** across `add` calls; `top()` = kth largest. `add`: push, then pop **only if `size() > k`**. **The bug trap:** unconditional pop shrinks the heap below k when it holds `< k` elements — and `k ≤ len+1` means it can start with `k-1` (or empty when `nums=[]`), so `top()` on empty = UB. Must **store `k`** as a member (215 was stateless). Min-heap for *largest*; cast unsigned `size()`. Re-sort per add = O(n log n)/call → TLE | add O(log k) | O(k) |
| 3 | [Top K Frequent Elements](https://leetcode.com/problems/top-k-frequent-elements/) | 347 | M | "k **most frequent**, any order, beat O(n log n)" | **215 keyed by frequency** — hash-map count `{num→freq}`, then size-K min-heap of **`{freq, num}` pairs** (freq FIRST — pq orders by `.first`); survivors = k most frequent. Heap runs over the **m unique** elements, not n raw values. O(n + m log k)/O(n). **Optimal O(n): bucket sort** — `bucket[f]` = nums with freq f (size **n+1**), walk high→low until k collected. Gotchas: `{num,freq}` swap silently ranks by value; skipping the count step = different problem | O(n + m log k) | O(n) |
| 4 | [Top K Frequent Words](https://leetcode.com/problems/top-k-frequent-words/) | 692 | M | "k most frequent **words**, ties broken **lexicographically**" | **347 + custom comparator** — order matters now. Size-K min-heap needs "**worst** on top": lower freq on top (`a.first > b.first`) AND tie → **larger** word on top (`a.second < b.second`) — **both keys invert** vs output order, because the comparator picks who's *evicted*. Pops come worst→best → **fill `res` backwards**. Default `pair` max-heap fails ties (`["love","i"]` not `["i","love"]`). Lambda mechanics: `auto cmp`, `decltype(cmp)`, `pq(cmp)`. Bucket sort is NOT O(n) here (must sort inside buckets) | O(n log k) | O(n) |

## When to reach for Heap
1. **"Top K / K largest / K closest / K most frequent"** → size-K heap (min-heap for largest, max-heap for smallest/closest), O(n log k).
2. **Running median / balance two halves of a stream** → two heaps (max-heap low half + min-heap high half).
3. **Repeatedly pull the current min/max** (Dijkstra, merge K lists, scheduling) → priority queue as the frontier.
4. **One-shot kth order statistic on a static array** → quickselect / `nth_element`, average O(n).

## Common Pitfalls Across the Pattern
- **Wrong heap polarity** — kth *largest* → **min**-heap of size k; kth *smallest* → **max**-heap of size k. Getting this backwards is the #1 bug.
- **Letting the heap grow to n** — cap it at k for O(k) space and O(log k) ops.
- **`priority_queue` default is a max-heap** — pass `greater<T>` (and the `vector<T>` middle arg) to get a min-heap.
- **Unsigned `size()`** — comparing `heap.size() > k` with `int k` warns/surprises; cast or use `size_t`.
